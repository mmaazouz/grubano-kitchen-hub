# GO-LIVE TICKETS — closed beta transactionnelle (état au 2026-08-27)

> Issus de l'audit A–H réconcilié (97 constats) après application des 7 lots de
> correctifs. **Tous les P0 identifiés sont fermés.** Restent : P1 acceptables en
> petite bêta surveillée, P2 après bêta, et les FOUNDER FACTS (pack séparé).
> Format court : ID · priorité · portée · résumé · fichiers · acceptation.

## P1 — avant l'ouverture PUBLIQUE (acceptables en closed beta surveillée)

| ID | Titre | Évidence | Fix attendu | Acceptation |
|---|---|---|---|---|
| T-01 | **Modes servables invisibles côté fiche** : la fiche resto n'expose que `fulfillment.delivery` — un établissement sans mode servable (pickup OFF + flag livraison OFF) se découvre par un 403 au moment de commander | `app/api/restaurants/[id]/route.ts` (~:305) n'expose pas pickup ; `lib/fulfillment.ts` | Exposer `fulfillment.pickup` (même calcul que le gate serveur) + griser le CTA commande quand aucun mode servable + avertissement onboarding « livraison indisponible pendant le pilote » | La fiche d'un resto non commandable ne propose plus « Commander » |
| T-02 | **Minimum de commande jamais affiché avant l'erreur** | `Restaurant.minOrder` appliqué serveur seulement (`app/api/orders/route.ts`) | Ligne info fiche resto + nudge panier quand subtotal < minOrder (calqué sur le nudge small-order-fee) | Le client voit le minimum avant de commander |
| T-03 | **Déclaration allergènes non obligatoire à la création de plat** (0 plat renseigné sur staging à date) | `app/api/menu/route.ts:29` (`.default([])`) ; le gate existe déjà côté flux créateur (`creators/dishes/[id]/submit:53-59`) | Décision produit : rendre la déclaration explicite obligatoire (avec choix « Aucun ») calquée sur le pattern créateur ; en bêta : mitigation par checklist admin 7.1 du runbook | Aucun plat publiable sans déclaration explicite |
| T-04 | **Vocabulaires allergènes divergents** : chips FR accentuées vs prompt scan-IA non accentué vs ids INCO créateur — valeur IA fantôme non décochable | `app/[locale]/menu/page.tsx:114` ; `app/api/menu/scan-dish/route.ts:26` ; `lib/dish-sheet.ts:22-25` | Normaliser au stockage sur les 14 ids INCO + aligner le prompt scan + normaliser accents à la réception `/api/menu` | Une seule taxonomie en base ; le scan IA produit des valeurs décochables |
| T-05 | ✅ **RÉSOLU (mission Money Flow, décisions D3/D4)** : cible bêta = `REFUNDS_ENABLED=true` + `ADMIN_AUDIT_ENABLED=true` + `ALERT_EMAIL` posé + `INTERNAL_CRON_TOKEN` absent ; `CLAIMS_ENABLED=false` FINAL (support humain, backlog `POST-BETA-CLAIMS-BACKLOG.md`). Reste l'ACTION ENV fondateur (pack). | — | — | Procédure complète : `MONEY-FLOW-CLOSED-BETA-RUNBOOK.md` §3-4 |
| T-06 | **ALERT_EMAIL + crons staging** : l'alerte ghost-order est un no-op silencieux sans ALERT_EMAIL ; confirm-sweep/reconcile-digest inertes hors main | `lib/admin-alerts.ts:30-31` ; `docs/ops/crons.md` | Poser ALERT_EMAIL sur staging + appel manuel quotidien `GET /api/admin/reconcile-ghost-orders` (runbook) pendant la bêta | Un paiement fantôme déclenche une alerte réellement reçue |
| T-07 | ✅ **RÉSOLU AU CODE (D5, branche connect-gate)** : refus serveur `409 restaurant_not_payable` à la création de commande carte ET aux deux routes `/pay` quand le resto n'est pas Connect-actif ; danger-flag QA `ALLOW_PLATFORM_FALLBACK` (jamais en prod). | `lib/connect-gate.ts` | — | Un resto non Connect-ready ne peut plus recevoir une vraie commande payante |
| T-08 | ✅ **RÉSOLU (décision fondateur D1)** : le hold 10 € RESTE pour la bêta, capture punitive VERROUILLÉE (aucune voie atteignable, prouvé M1) ; wording D2 aligne (lot money-wording). Dossier hold-vs-SetupIntent au pack (réévaluation post-pilote). | `lib/deposit.ts` | — | Autorisation temporaire uniquement, jamais une pénalité |
| T-09 | **Page « Fonctionnement de la plateforme » absente** (transparence classement/rôle) | Aucune page ; tri réel honnête (distance sinon nouveauté, note gated avis réels) — contenu 100 % dérivable du code | Page factuelle /eat + lien pied de page (matière : F-03/F-09 de l'audit) | Un consommateur peut lire les critères de classement réels |
| T-10 | **Réponse aux demandes de droits** : contact `privacy.dpoContact` placeholder ; runbook manuel livré mais canal officiel manquant | `lib/legal-info.ts` ; `docs/ops/DATA-SUBJECT-RIGHTS-RUNBOOK.md` | FOUNDER FACT (adresse) puis publication dans la politique de confidentialité | Une demande d'accès/suppression a un canal publié et un processus |

## P2 — après la bêta

| ID | Titre | Note |
|---|---|---|
| T-11 | Idempotence de POST /api/orders (retry réseau = commandes dupliquées impayées, expirent en 24 h ; le PAIEMENT est idempotent) | Header Idempotency-Key ou dédup consumerId+hash(items) |
| T-12 | `LoyaltyTransaction` sans `@@unique([orderId,type])` — course théorique de double débit/crédit au webhook | Contrainte + create-catch-P2002 ; fenêtre étroite, absorbée par les gardes d'état |
| T-13 | Crédit fidélité peut créer une commande < 0,50 € impayable (plancher Stripe) — coincée puis expirée | Capper le crédit en gardant la conversion points↔€ cohérente (lib/loyalty) — PAS de patch naïf |
| T-14 | Sweep d'expiration centralisé + `cancelIntent` du PI des commandes expirées (le lazy-expiry dépend de l'ouverture de la page /orders du resto) | La source « expiré payable » est déjà fermée côté /pay |
| T-15 | Fiche détail lisible pour un resto non approuvé (`isActive` non filtré sur GET /api/restaurants/[id]) — la commande re-bloque | 404 ou payload restreint hors owner/admin |
| T-16 | Restos sans coordonnées invisibles en listing géolocalisé (géocode BAN soft-fail) | Annexer en fin de liste ou backfill géocode post-approbation ; couvert en bêta par la checklist 7.1 |
| T-17 | Flux admin REJECT/SUSPEND : /admin/approvals n'a pas de refus (dossier refusé reste 'pending' sans notification) ; « Suspendre » inerte | Brancher sur le PATCH existant (publication-rule couvre déjà l'admin) + email statut |
| T-18 | Logos/couvertures partenaires = URLs externes arbitraires rendues au consommateur (fuite IP vers hôte tiers ; cassera au passage CSP enforce) | Proxifier/re-héberger (rail Cloudinary existant) ; risque accepté en bêta vettée |
| T-19 | Rétention : durées à arbitrer (comptes, commandes, résa/allergies, EmailLog) puis purges calquées sur le sweep géoloc | Les purges sans ambiguïté (EmailOtp, tokens reset) sont déjà en place |
| T-20 | Reçu/TVA du parcours pickup (« Voir le reçu — Bientôt ») — le rail reçu dine-in + lib/tax existent | REVIEW comptable/juriste puis branchement |
| T-21 | E-mails : identité société + lien légal en pied (attend les FOUNDER FACTS) | `lib/transactional-emails.ts` — base URL déjà corrigée |
| T-22 | Consentement consommateur : base légale compte/fidélité à qualifier (contrat vs consentement) → checkbox + consentAt éventuels | Pattern partenaire réutilisable tel quel |
| T-23 | Query strings sensibles (reset `?token=&email=`) dans les access logs Apache non rotatés | Retirer `email=` de l'URL (lookup par hash token) + politique de rotation logs (ticket ops o2switch) |
| T-24 | Registre des versions de consentement (`policyVersion`) inexistant | Utile dès que CGU/CGV existent |
| T-25 | `/eat/search` : filtres livraison résiduels en pilote retrait (« Livraison offerte », tri « Plus rapides ») | Masquer quand DELIVERY_FULFILLMENT_ENABLED OFF |

## Tickets ajoutés par la mission MONEY FLOW FINAL (2026-08-27)

| ID | Prio | Titre | Note |
|---|---|---|---|
| T-26 | **FOUNDER** | Remise de bienvenue créateur (10 %, cap 5 €) ACTIVE par défaut et financée de facto par le RESTO au /pay — contredit la doctrine B0 (« financée par Grubano ») et le commentaire du /pay qui la croit OFF | Options : couper (customerDiscountPct=0 en base), assumer le financement resto (corriger doctrine+commentaires), ou restaurer la déduction Grubano avec spec fraîche. M2-02 — au pack |
| T-27 | P1 | Aucune garde croisée DB entre les deux moteurs de refund — deux PARTIELS concurrents s'empilent (plafonnés par Stripe, jamais > total) | Refund row partagée (curseur) aussi sur le chemin refundPayment, ou verrou applicatif par PI. M6-02 |
| T-28 | P1 | /api/partner/balance rôle restaurant sur-affiche : les lignes ROUTÉES (déjà versées par Stripe) sont comptées comme dues | Filtrer routed=false pour le « dû par Grubano ». M5-02 |
| T-29 | P1 | requirements Stripe Connect (currently_due/past_due/disabled_reason) ni lus ni stockés — « restricted » aveugle pour le partenaire et l'admin | Lire acct.requirements au webhook + GET connect, stocker disabled_reason/currently_due, afficher ConnectCard+admin. M4-P1-1 |
| T-30 | P1 | Refund > J+1 = solde Express négatif (payouts quotidiens) — aucun monitoring, politique de couverture indéfinie | Doc runbook faite ; reste : lecture de balance dans l'outil admin + clause CGU partenaire + éventuel delay_days. M4-P1-2/FF4 — volet politique au pack |
| T-31 | P2 | Holds immobilisés : résa expirée sans statut / clôture manual+none → expiry Stripe ~7 j sans sweep | Sweep périodique releaseHold (délai = décision fondateur) ou assumer l'expiry documentée. M1-03 |
| T-32 | P2 | PATCH opérateur réservations et PATCH statut commande en read-check-write — courses état (arrived vs cancelled ; cancelled vs preparing) sans effet money direct | updateMany gardé sur le statut lu (calque self-cancel conso). M6-04/M6-05 |
| T-33 | P2 | Preuves Stripe best-effort jamais complétées (chargeId/transferId/fee NULL sur échec retrieve) + ligne ledger perdue si échec d'écriture (200 rendu) | Job de complétion idempotent + heal-on-replay ledger / cron backfill-ledger. M5-04/M2-09 |
| T-34 | P2 | Emails transactionnels ARGENT en FR uniquement pour des clients EN/ES/IT/AR | Porter sur le patron localisé claim-emails (locale + RTL). M7-07 |
| T-35 | P2 | Chaîne de preuve : pas d'orderId sur LedgerEntry (jointure par PI, rompue pour un orphelin), pas de paidAt/chargeId sur Order ; résumé checkout sans lignes small-fee/tip | Colonnes additives + affichage. M2-11 |
| T-36 | P2 | /api/admin/ledger/check comptera FAUX dès qu'un rail B2B (supply/service) émettra des PIs succeeded (pas de metadata.restaurantId) | Exclure les PIs à metadata supplyOrderId/serviceInvoiceId au moment d'activer ces rails. M5-06 |
| T-37 | P2 | Fenêtre d'annulation CLIENT d'une commande : inexistante (décision produit fondateur avant tout build) | FOUNDER-M3-01 — au pack (décision, pas un bug) |

## Tickets ajoutés par le préflight final Phase 2 (2026-09-05)

| ID | Prio | Titre | Note |
|---|---|---|---|
| T-38 | P1 (Product Cleanup) | **Pourboire livreur NON conditionné au mode de livraison** : si `TIPS_ENABLED` passe à `true`, le panier affiche le sélecteur de pourboire coursier pour une commande **à emporter** (`app/[locale]/eat/cart/page.tsx:797`, hors de la ternaire `fulfillment === 'delivery'`) et `POST /api/orders` honore `tipCents` sans condition de mode (`route.ts:489`) → pourboire retenu par la plateforme sans coursier (D-1 fonds tiers). Invariant requis : le chemin pourboire coursier exige un contexte de livraison/coursier (mode `delivery` + `DELIVERY_FULFILLMENT_ENABLED`). `TIPS_ENABLED` reste **FALSE** (runtime prouvé 2026-09-05) ; ne pas activer. | Isolé, corrigeable en Product Cleanup ; tests : pickup + flag ON → tip refusé/ignoré côté serveur et masqué côté UI. |
| T-39 | P1 pré-prod | **Auth interne non déterministe** : `GET /api/admin/ledger/check` → 401 avec le token canonique de `.env.local` (v3 mesuré) — le processus compare une autre valeur (injection hébergeur ou fichier `.env*` ombrant). Contrat cible et fix : `docs/ops/RUNTIME-SECRET-SOURCE-MATRIX.md` §2/§3. Non bloquant pour la réconciliation financière (directe, opérateur v4). | Décision après la mesure v4 `INTERNAL_CRON_TOKEN PRESENT BEFORE ENV LOAD`. |
| T-40 | P2 doc | `CLAUDE.md §9` décrit le `server.js` racine comme entrée Passenger ; le déploiement livre `.next/standalone/server.js` (Next) — le chargeur d'env réel est `@next/env` (dotenv). Corriger la doc (fondateur). | Mesuré par lecture du workflow + exclusions FTP. |

| T-41 | **P1 pré-prod (pré-existant)** | **Fichiers de la racine d'application servis publiquement** (mesuré HEAD 2026-09-05 staging) : `/server.js` 200, `/package.json` 200, `/prisma/schema.prisma` 200 (schéma complet), `/tmp/restart.txt` 200, `/scripts/server/*.js` 200 ; `/.env.local` et `/.htaccess` 404 (aucun secret). Cause : DocumentRoot Apache = racine Passenger. Remédiation : règles `.htaccess` serveur (`RUNTIME-SECRET-SOURCE-MATRIX.md §5`), à appliquer par le fondateur ou par un opérateur dédié après décision ; vérifier ensuite `/fr/eat` 200. | Divulgation d'information, pas de fuite de secret ; non bloquant TEST ; bloquant prod. |
| T-41 (mise à jour 2026-09-05) | **P1 PRÉ-PRODUCTION — bloquant prod, train infra SÉPARÉ** | **APP ROOT SOURCE EXPOSURE HARDENING** — classes secret toutes 404 (mesuré : `.env*` ×8 dont 4 sauvegardes `.env.local.bak*` testées par le fondateur, `.htaccess`, `.git/*`, `*.sql`, logs) ⇒ **aucune fuite de secret confirmée** ; classes source 200 (`/server.js`, `/package.json`, `/prisma/schema.prisma`, `/tmp/restart.txt`, `/scripts/server/*.js`, `/lib/ledger-check-core.js`). Handoff complet : `docs/ops/WEB-ROOT-HARDENING-HANDOFF.md` (sauvegarde, protection dotfiles existante à étendre, plus petit changement, baseline, vérifications ≤ 60 s, **rollback automatique**, jamais le fondateur à la main). Promotion P0 uniquement si un fichier portant un identifiant devient lisible. | `.htaccess` **non modifié** par le train financier Phase 2 ; l'opérateur v5 remesure à chaque run et passe en FAIL P0 si une classe secret répond 200. |
| T-42 | **P1 PRÉ-LIVE — politique financement refunds (OPEN, décision fondateur)** | **Porte de solde Connect calculée sur le NET (473) alors que Stripe exige le BRUT** : mesuré le 2026-09-09 sur la répétition GR-N5TSM0 (refund 500 c) — `Transfer.amount_reversed` +500 (brut), `application_fee` refunded 27 crédité séparément, effet net compte connecté −473 (2668 → 2195). Le moteur/ledger expriment le net (`restaurantReverseCents 473`), correct pour la comptabilité ; la **disponibilité Connect requise doit être le montant cash brut** (= 500 pour ce partiel, 1450 pour BZE1X full). À intégrer dans `REFUND-FUNDING-PRE-LIVE-DECISION.md` avant tout passage LIVE ; ne pas résoudre la politique commerciale dans le closeout Phase 2. **CONFIRMÉ par la répétition FULL du 2026-09-09** : refund 1450 c ⇒ `Transfer.amount_reversed` +1450 (BRUT) et `application_fee` refunded 116 crédité séparément ⇒ effet net −1334 (available 2195 → 861). La règle mesurée est donc : `AVAILABLE ≥ montant cash BRUT`. Appliquée dans `scripts/server/phase2-refund-gate.js` (`REQUIRED CONNECT FUNDING (GROSS transfer reversal — T-42)`), harnais nc6 mis à jour. **STATUT : OPEN** — la politique commerciale de financement pré-LIVE reste une décision fondateur (`REFUND-FUNDING-PRE-LIVE-DECISION.md`). | Aucun écart financier : réconciliation directe DB ↔ Stripe PASS ; il s'agit d'une règle de porte de financement, pas d'un défaut moteur. |
| T-43 | **P2 ops — OPEN (remédiation MANUELLE, à relancer après CHAQUE fenêtre)** | **Sauvegarde restaurable avec `REFUNDS_ENABLED=true`** : l'opérateur `phase2-refund-gate.js window` sauvegarde `.env.local` avant chaque écriture ; la sauvegarde prise juste avant le RE-GEL (`.env.local.bak-…ZZ`) contient donc `true`. Remédiation : `scripts/server/phase2-backup-neutralize.js` (archive manifeste sha256 + copie neutralisée sous `~/.grubano/phase2-evidence`, supprime la copie app-root, ne touche jamais `.env.local`, prouve la porte 403 ; harnais nc7). À exécuter par le fondateur après chaque fenêtre ; amélioration future : le window operator peut archiver lui-même hors racine. **EXÉCUTÉ après les deux répétitions** : neutralisation lancée par le fondateur → `LIVE REFUNDS_ENABLED = false`, `PROCESS REFUND GATE = 403`, `RESTORABLE TRUE-FLAG BACKUP IN ACTIVE APP AREA = NO`, `BACKUP SAFETY = PASS`. Amélioration future possible (non bloquante) : que l'opérateur de fenêtre archive lui-même hors racine au lieu de laisser la copie restaurable. | Non financier. |
| T-44 | **OPEN — PRE-LIVE TEST DEBT (fidélité)** | **`recoveryOffsetPoints` jamais exercé en conditions réelles de staging.** Invariant (Phase 1 D3) : lors d'un remboursement, les points GAGNÉS sur la commande sont reversés au prorata ; si le solde visible courant est insuffisant pour absorber ce reversement, la partie irrécupérable est portée dans `recoveryOffsetPoints` (solde visible plancher 0), et les gains futurs remboursent d'abord cet offset (`applyEarnWithOffsetRepay`). Répétition 1 (GR-N5TSM0) : balance 20 ≥ 5 → offset 0. Répétition 2 (GR-GBZE1X full, MESURÉE le 2026-09-09) : solde visible 18 ≥ 14 points à reverser ⇒ **offset 0 → 0, branche NON exercée** (18 − 14 = 4). Les deux répétitions autorisées sont donc épuisées sans avoir couvert cette branche ; sa clôture demande un scénario NATUREL, jamais une mutation artificielle. Couverture automatisée mesurée 2026-09-09 : `tests/loyalty-refund.test.ts` matrice D3 L/M/N + contrôle négatif, `tests/loyalty-refund-apply.test.ts` (plancher 0), `tests/rehearsal-vector-n5tsm0.test.ts` (delta conditionnel) — 40/40 verts. NO ARTIFICIAL BALANCE MUTATION = YES. Clôture PRE-LIVE la plus sûre : attendre un scénario NATUREL (client ayant dépensé ses points gagnés avant un remboursement) sur staging ou en bêta fermée TEST, puis réconciliation directe ; à défaut, première exposition argent réel = RISQUE DOCUMENTÉ (branche couverte par tests unitaires uniquement). Ne bloque PAS la répétition full-refund. | Distinct de T-42 (financement Connect brut). |
| T-45 | **P1 bêta produit — état conso après remboursement (constat 2026-09-09, répétition FULL GR-GBZE1X)** | **Aucune représentation du remboursement côté client.** Le rail admin (`executeRefund`) n'écrit ni `Order.status` ni `Order.paymentStatus` (restent `delivered` / `paid`) ; l'API conso `GET /api/orders/[id]` n'expose que `paymentStatus` (pas de lignes `Refund`, pas de montant remboursé) ; la liste `/eat/orders` affiche l'étape finale « Récupérée » et le total, la page de suivi affiche « Total payé ». Après un remboursement TOTAL de 14,50 €, la commande reste présentée comme payée ; seul l'e-mail de confirmation (et, pour les points, l'historique fidélité si l'événement `earn_reversal` y est listé) informe le client. Décision produit requise : (a) état de paiement `refunded` / `partially_refunded` porté par le rail A (modèle d'état à revoir, voir constat runbook §11) ou (b) exposition des lignes `Refund` succeeded dans l'API conso + libellé « Remboursé {montant} » dans la liste et le suivi. Correction UI seule = présentation ; ne pas toucher au rail financier fermé. **RENFORCÉ par la revue adversariale du 2026-09-09** : (a) **aucune** route ou page conso ne lit le modèle `Refund` — le remboursement n'existe que dans Stripe, la table `Refund` et le ledger, qu'aucune surface `/eat` ne touche ; (b) l'API conso ne se contente pas d'omettre l'information, elle **affirme** `paymentStatus: 'paid'` ; (c) la page de suivi continue d'annoncer au client les points fidélité « crédités » alors que le reversal les a repris — `Order.pointsEarned` n'est jamais décrémenté ; (d) l'historique fidélité expose bien la ligne `earn_reversal` via `/api/loyalty/history`, mais **aucune page ne l'appelle** : le solde du client baisse sans explication ; (e) autres surfaces concernées non listées à l'origine : pass de retrait, écran de notation, écran d'aide par commande, qui réaffichent tous le total d'une commande remboursée. Seule surface conso VRAIE aujourd'hui : l'e-mail de confirmation (reçu et vérifié pour les deux répétitions). | Non financier ; le refund lui-même est PASS (Stripe/DB/ledger). Constat établi par lecture du code (session conso non disponible côté CC). |
| T-46 | **P1 pré-prod — reporting argent opérateur aveugle aux remboursements (trouvé par la revue adversariale du closeout, 2026-09-09)** | `GET /api/finance/summary` calcule le CA brut 30 jours comme `Σ Order.subtotal` sur les commandes non annulées (`app/api/finance/summary/route.ts`), donc **un remboursement ne le diminue jamais** : après le remboursement TOTAL de GR-GBZE1X, la commande reste comptée à 100 %. Sur le même écran la commission est dérivée du ledger, donc **refund-aware** — les deux moitiés du P&L ne parlent plus de la même réalité, et le net restaurant bouge dans le MAUVAIS sens quand un remboursement arrive (le CA reste, la commission baisse). Aucun impact sur l'argent réellement mouvementé (Stripe/ledger/DB = PASS) : c'est un défaut de **restitution**. Remédiation : dériver le CA du ledger comme la commission, ou soustraire les lignes `refund` de la fenêtre. À trancher avant le GO bêta technique (un restaurateur lit cet écran). | Hors rail financier fermé ; ne pas corriger pendant une fenêtre de remboursement. |
| T-47 | **P2 produit — deux remboursements du MÊME montant sur la MÊME commande suppriment le second e-mail client** | La clé d'idempotence argent est `refund:<orderId>:<alreadyRefundedCents>` : 500 c puis encore 500 c sur la même commande sont **deux remboursements légitimes** (clés différentes). Mais l'e-mail de confirmation est dédupliqué sur `order:<orderId>:<amountCents>` (`EmailDispatch`, `@@unique([trigger,dedupeKey])`) : les deux partagent **une seule** clé, donc le **second e-mail n'est jamais envoyé** — le client est remboursé sans en être informé. Découvert en corrigeant l'opérateur de preuve (la même ambiguïté empêche toute corrélation par remboursement). Remédiation possible : inclure l'identifiant du remboursement dans la clé de dédup (`order:<id>:<refundId>`) plutôt que le montant. `scripts/server/phase2-email-timeline.js` signale explicitement ce cas au lieu de rendre un verdict confiant. | Non rencontré par les deux répétitions (montants différents). Aucun impact sur l'argent. |

## Claims — batch 2 (2026-09-10)

L'audit adversarial exécuté **avant** livraison du batch 2, les défauts qu'il a trouvés **dans mon
propre travail**, les correctifs et ce qui reste OPEN sont consignés dans
`docs/ops/CLAIMS-BATCH-2-AUDIT-RECORD.md`. Ce registre ne doit pas être réécrit pour faire
disparaître un constat.

| ID | Prio | Titre | Note |
|---|---|---|---|
| T-49 | **P1 (relevé de P2 le 2026-09-10)** | **`stale_refunding_no_refund_row` : réclamation en `refunding` sans LIAISON, sans sortie manuelle.** ⚠️ **LE TEXTE D'ORIGINE DE CE TICKET ÉTAIT FAUX** (conservé à la ligne suivante, jamais effacé). Il décrivait l'état comme « une réclamation en `refunding` **sans aucune ligne `Refund`** » et concluait « **Aucun impact argent : rien ne bouge dans cet état** ». Les deux affirmations sont RÉFUTÉES par le code. **État réel** : `status='refunding'` ET `refundId IS NULL` ET `refundError IS NULL` — l'absence d'une **LIAISON**, pas l'absence d'une ligne `Refund`. `executeRefund` crée la ligne `Refund` **AVANT** d'appeler Stripe (`lib/refund.ts:808` puis `:355`) et la liaison `claim.refundId` n'est écrite qu'**APRÈS** le retour d'`executeRefund` (`lib/claims.ts:371/379/396/404`) : une ligne `Refund`, `pending` ou déjà `succeeded`, existe donc couramment dans cet état. **Impact argent possible** : si l'interruption survient dans `finalizeRefund` après un `succeeded` Stripe, le client A ÉTÉ REMBOURSÉ, la ligne `Refund` reste `pending`, et la réclamation ne porte aucune liaison — de l'argent parti sans trace côté réclamation. **Entrée** : uniquement par mort de processus / exception non rattrapée / échec d'écriture DB entre le CAS `lib/claims.ts:343` (seul écrivain de `refunding` du dépôt) et l'une des cinq écritures compensatoires. Aucun retour COMPLET ne laisse cet état. Un échec de remboursement ORDINAIRE ne le produit PAS : les erreurs Stripe repassent par `fatal()` et prennent la branche gérée qui repositionne la réclamation en `approved` + `refundError`, que l'échappatoire existante SAIT clôturer. **Impasse** : `arbitrateClaim` répond 409, `isStuckResolvable` exige `refundError` non nul donc `resolveStuckClaim` refuse, le balayage l'exclut (`refundId: { not: null }`), `reconcileClaimForRefund` joint par `refundId` et ne le trouve pas, et `activeOrderKey` reste posé — le client ne peut plus re-déposer sur cette commande. **Risque humain** : la carte admin affirme « Montant réellement remboursé : aucun (rien n'a encore atteint le client) » alors que le classificateur n'interroge JAMAIS `Refund` par `orderId` ni Stripe — précondition d'un second paiement par le rail admin, qui ne lit pas la table `Claim`. | Non joignable tant que `REFUNDS_ENABLED` est fermé (double garde : les trois portes sont gatées CLAIMS, et `lib/claims.ts:338` précède le CAS). Fermer la porte ne SOIGNE rien : une ligne T-49 antérieure persiste. Population staging **NON MESURÉE** (precheck lecture seule disponible). **Décision fondateur en attente** — gate T-49 du 2026-09-10. |
| T-49 (texte d'origine, **RÉFUTÉ** — historique d'audit, ne pas effacer) | — | **`stale_refunding_no_refund_row` sans sortie manuelle** : une réclamation en `refunding` sans aucune ligne `Refund` ni `refundError` n'est clôturable par aucune action admin. L'échappatoire `resolveStuckClaim` est volontairement étroite (`refundError` non nul + statut `approved`/`refunding`) et la liste admin le déclare désormais ligne par ligne (`resolvable`), donc aucun bouton n'est offert là où le serveur refuserait. Élargir l'échappatoire à cet état est une décision de **sémantique argent**.  /  Trouvé par ma propre relecture du câblage des contrôles, batch 2. Aucun impact argent : rien ne bouge dans cet état.  /  | Réfuté le 2026-09-10 : la ligne `Refund` existe couramment dans cet état, et l'argent PEUT avoir bougé. |
| T-50 | P2 | **Sélection d'articles non persistée / pas de suivi de consommation par article** : deux réclamations successives peuvent viser la même ligne, chacune sous le plafond global. Le plafond cumulatif en argent (DB ∧ Stripe) empêche toute sur-restitution ; c'est la traçabilité **par ligne** qui manque. | Exige un changement de schéma Prisma → non entrepris (porte « STOP avant migration »). |
| T-51 | ✅ **FERMÉ 2026-09-10** (était P1) | **RESUME-FIRST attribue une réclamation à un remboursement qu'une AUTRE voie a créé.** La reprise ne compare que des MONTANTS : `lib/refund.ts:778` (`input.amountCents !== pending.amountCents`) et `lib/claims.ts:392` (`result.amountCents !== claim.requestedAmountCents`). L'identité de la ligne reprise — son `reason`, donc la réclamation ou le rail qui l'a CRÉÉE — n'est jamais vérifiée. Quand une ligne `Refund` interrompue en `pending` sur la MÊME commande porte le même `amountCents` que le montant demandé par la réclamation, **aucun `resume_mismatch` n'est levé** : la réclamation est liée à une ligne qu'elle n'a pas créée puis rapportée `refunded`. Le client est correctement remboursé une seule fois, mais la réclamation revendique le remboursement d'un autre (rail admin, ghost-order, ou une réclamation antérieure). Chemin concret : un échec de clawback 502 laisse une ligne `pending` portant `claim:A` ; `resolveStuckClaim` clôt A et libère `activeOrderKey` ; une NOUVELLE réclamation B sur la même commande reprend alors la ligne de A. | Trouvé par l'analyse adversariale du gate T-49 (2026-09-10), hors périmètre de ce gate. Aucune sur-restitution : Σ remboursements reste borné par le plafond cumulatif Stripe. C'est un défaut d'ATTRIBUTION et de véracité, pas de montant. Corriger en comparant l'identité de la ligne reprise, pas seulement son montant. |
| T-52 | ✅ **FERMÉ 2026-09-10** (était P2) | **`Refund.reason` est écrit mais lu par personne.** `lib/refund.ts:820` écrit `claim:<claimId>` à la création de chaque ligne `Refund` du rail réclamations, mais `REFUND_SELECT` (`lib/refund.ts:270-274`) l'omet et aucune des lectures Prisma du modèle ne le sélectionne : ni route, ni UI, ni opérateur. La seule trace qui relierait une réclamation à son remboursement après une interruption n'est donc pas un contrôle. Elle n'est de surcroît PAS fiable seule : `reason` identifie le CRÉATEUR de la ligne, jamais celui qui l'a PILOTÉE (voir T-51), et le `reason` du rail admin est du texte libre non validé (`z.string().max(500)`), donc falsifiable. | Constat du gate T-49. À corroborer (commande + montant + fenêtre temporelle + métadonnée Stripe `grubano_refund_row`) avant tout usage comme liaison. |
| T-53 | ✅ **FERMÉ 2026-09-10** (était P2) | **La fenêtre CLAIMS n'a pas d'équivalent du bail T-48.** `isRefundsEnabled()` exige `REFUNDS_ENABLED=true` ET un `REFUNDS_WINDOW_UNTIL` futur ≤ 30 min, revérifié par l'application à chaque appel : une fenêtre argent meurt de vieillesse même après un SIGKILL. `isClaimsEnabled()` (`lib/claims.ts:103`) ne lit qu'un booléen. L'opérateur `phase2-claims-gate.js` arme bien une fermeture d'urgence sur signaux et le dit honnêtement (« NOT proof against SIGKILL/host crash »), mais un SIGKILL pendant une répétition laisse `CLAIMS_ENABLED=true` sur disque **sans expiration**. | Aucun argent ne peut bouger de ce seul fait (le rail exige AUSSI `REFUNDS_ENABLED`, et `lib/claims.ts:338` précède le seul écrivain de `refunding`). L'exposition est une surface réclamations ouverte : dépôts clients, revues restaurant, approbations qui reposent en `approved` sans paiement. Constat du gate T-49. |
| T-54 | ✅ **FERMÉ 2026-09-10** (était P2) | **Le neutraliseur de sauvegardes ne couvre pas `CLAIMS_ENABLED`.** `scripts/server/phase2-backup-neutralize.js` traite les `.env.local.bak*` dont le `REFUNDS_ENABLED` effectif est `true` (remédiation de T-43). L'opérateur claims sauvegarde `.env.local` avant CHAQUE écriture, donc la sauvegarde prise juste avant la fermeture contient `CLAIMS_ENABLED=true` et resterait **restaurable** dans la racine applicative. C'est exactement la classe de défaut T-43, transposée au flag réclamations. | Non financier. À traiter avant, ou avec, la première répétition Mode A. Constat du gate T-49. |
| T-55 | ✅ **FERMÉ 2026-09-10** (était P3) | **La fermeture d'urgence de l'opérateur claims n'a aucun test.** L'équivalent remboursement a `tests/phase2-refund-gate-emergency-refreeze.test.ts` (7/7 verts) ; l'opérateur claims expose les mêmes coutures (`armedClose`, `writeFlag`, `touchRestart`) mais rien ne les exerce. Un mécanisme de sûreté non testé n'est pas un mécanisme de sûreté. | Constat du gate T-49. À écrire avant de faire confiance au Mode A en usage réel. |
| T-49 (mise à jour 2026-09-10 — DÉCISION FONDATEUR APPLIQUÉE) | **P1 → implémenté, en attente de l'audit et de l'autorisation Mode A** | **Politique retenue : PREUVE UNIQUEMENT, FERMÉ PAR DÉFAUT SUR L'AMBIGUÏTÉ.** Si la preuve établit ce qui s'est passé, le système applique cette vérité et rien d'autre : liaison sur l'identité exacte du remboursement, réconciliation, montant RÉEL. Si la preuve ne l'établit pas, il ne clôt pas, ne dit pas « payé », ne dit pas « non payé », n'autorise aucun nouveau remboursement, ne libère pas la commande pour une seconde réclamation, et ne laisse personne deviner : la réclamation est garée en `financial_verification` et escaladée. **Sûreté argent** : `reconcileClaimEvidence` n'appelle jamais le moteur, n'écrit jamais chez Stripe, ne relance rien ; la seule branche qui rouvre une tentative exige une preuve POSITIVE d'absence (aucune ligne Refund sur la commande ET Stripe ne rapporte ni remboursement abouti ni en attente). **Vivacité** : file admin durable **non gatée** (`/api/admin/claims/financial-verification` + section montée même quand `CLAIMS_ENABLED` est OFF), comptée dans le badge admin non gaté, plus une alerte à l'entrée dédupliquée par réclamation ET par cause. L'e-mail est au mieux-effort : s'il échoue, la file reste. **Fenêtre de crash** : un marqueur `reconcile_required` est écrit dans le MÊME CAS atomique que le passage en `refunding` — aucune écriture supplémentaire, donc aucune nouvelle fenêtre. Il ne dit PAS que le remboursement a échoué, et il est exclu de l'échappatoire par assertion admin. | Reste : audit adversarial, mesure de la population staging, autorisation fondateur du Mode A. |
| T-49 (rond 6 → 7, 2026-09-10) | **P1 → corrigé, en audit (rond 7)** | **Sortie manquante pour un remboursement fait depuis le Dashboard Stripe.** Déposé P0 au rond 6, ramené P1 : `financial_verification` n'avait AUCUNE sortie quand la commande ne porte aucune ligne `Refund` locale (un remboursement Dashboard n'en laisse pas) — `attributeClaimRefund` exige une ligne existante, la réconciliation re-gare, l'assertion admin refuse le statut. Corrigé par `adoptStripeRefundForClaim` (même route `/attribute`, corps `{ stripeRefundId }`) : preuve chez Stripe (PaymentIntent de la commande ET `latest_charge`), refus des remboursements moteur / pending / failed, une seule écriture = ligne `Refund` MIROIR (`idempotencyKey external:<re_>`, découpes 0, `reason = claim:<id>`), puis la queue auditée existante ; `dryRun` = lecture seule. **T-52 : ce mécanisme est le 2ᵉ écrivain du stamp `claim:<id>`**, listé à côté de `claimRefundReason`. Aucun appel moteur, aucune écriture Stripe. | 6 autres P1 corrigés en même temps (writer `stripe_failed` ramené à la ligne, garde `resume_mismatch` sur la branche FAILED, cause `reconcile_not_applied`, montant nul sur liaison désavouée, carte d'arbitrage en 3 cas, épingle de classe). Registre : `CLAIMS-T49-FAIL-CLOSED-RECOVERY.md` §12. |
