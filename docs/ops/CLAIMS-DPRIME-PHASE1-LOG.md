# CLAIMS D′ — PHASE 1 LOG (L0 → L2 → L1 → L3a) — base `dab754d`

Journal factuel des lots autorisés le 2026-09-22 (GO Phase 1). Toute valeur ci-dessous est MESURÉE et
datée ; « NOT MEASURED » quand elle ne l'est pas. Aucune écriture DB, aucune gate, aucun Stripe
pendant cette phase.

## L0 — Recensement staging MESURÉ (2026-09-22T17:11:18Z, `claims-census.yml` run 35759026504, HTTP 200)

```json
{"measuredAt":"2026-09-22T17:11:18.585Z",
 "claims":{"total":9,"active":0,"nonTerminal":3,
  "byStatus":{"refunded":4,"refused":3,"refused_final":2},"byStatusMeasured":true,
  "refunding":0,"restaurantReview":0,"arbitration":0,"silenceExpired":0,
  "financialVerification":0,"reconcileMarked":0,"t49Shape":0,
  "legacy":{"legacyPayableProofs":0,"refundedBoundToFailedRow":0,"refundedRowUnproven":0,
   "ownRowResumeMismatch":{"nonTerminal":0,"terminal":0},"terminalDeclarationWithArbitrationReason":0,
   "refundedAfterContradictionAttribution":0,"refundedBoundToOtherClaimStamp":0,"rowsBoundToMultipleClaims":0,
   "pendingRowsOver20hWithSettledRoyalty":0,"approvedUnpaid":0,"voidedRefundRows":0},
  "closure":{"missing":0,"terminalWithoutRecord":4}},
 "gates":{"claimsEnabled":false,"claimsGate":"CLOSED (flag_off)","refundsEnabled":false}}
```

| Point de compatibilité (spec v2 §9) | Hypothèse | Mesure | Verdict |
|---|---|---|---|
| Distribution des statuts | 4 refunded / 3 refused / 2 refused_final | 4 / 3 / 2 | ✅ |
| `approvedUnpaid` (forme E-10 / héritée à ratifier) | 0 | 0 | ✅ |
| `refunding`, FV, forme T-49, marquées | 0 | 0 / 0 / 0 / 0 | ✅ |
| active | 0 | 0 | ✅ |
| `closure.missing` / `terminalWithoutRecord` | 0 / 4 (population pré-R13, informative) | 0 / 4 | ✅ |
| Gates | CLOSED / CLOSED | `flag_off` / false | ✅ |

Staging au moment de la mesure : `version.json` = `dab754de25ad024ea41b4b81220f4793bf8ea75d` (develop, run 35615258398) ; `POST /api/claims {}` → 403 ; `POST /api/admin/refunds/run {}` → 403 (sondes non authentifiées, lecture seule). Aucune contradiction avec la spec → L2 autorisé.

## L0 — Baseline typecheck

`npm run typecheck` (`tsc --noEmit --pretty false`, sortie normalisée `tr -d '\r'`, lignes `error TS` triées) à `dab754d` : **39 erreurs, toutes sous `tests/`** (0 erreur produit). Fichier de référence : `docs/ops/TSC-BASELINE-dab754d.txt`. Règle de certification par lot : (a) `grep -v '^tests/'` de la liste courante est VIDE ; (b) `comm -13 baseline courant` est VIDE (aucune nouvelle erreur).

## L0 — Documents amendés
- `docs/ops/CLAIMS-DPRIME-SPEC-v2.md` (nouveau, spec figée).
- `docs/ops/CLAIMS-T49-ROUND13-SPEC-v1.md` : bloc « v1.1 AMENDMENTS — D′ » + pointeurs sur D1, D2, D13/AM-B3, E-10, F13, H02, H09 (§23→§26). Textes gelés conservés.
- `docs/ops/LOYALTY-REFUND-CONTRACT.md` : amendement D-15 (§24), §11/E-P2d/résiduel supersédés, §9 inchangé.
- `docs/ops/POST-BETA-CLAIMS-BACKLOG.md` : D4 abandonnée ; dette ANTI-REPEAT ITEM CLAIM POLICY — POST-BETA.
- Docs périmées : `CLAUDE.md` §7/§9/§10 (`prisma-push.sh`), `docs/ops/redeploiement.md`, `docs/ops/PHASE1-STAGING-PROCEDURE.md` (artefact SQL non livré), `docs/ops/CLAIMS-DELTA-CHECK-2026-09-07.md` (population legacy mesurée vide), commentaire d'en-tête `app/api/orders/[id]/status/route.ts` (« any state → cancelled » faux). `prisma/schema.prisma:2107/2249` : différé à L3b (aucune modification de schema.prisma avant L3b).
- `package.json` : script `typecheck`.

## L0 — déployé
Commit `b5ff49c4d924c79cd3a3c2d629960a52e464c663` ; CI staging run 35760935576 = success ; `https://app.grubano.com/version.json` → `b5ff49c` (buildDate 2026-09-22T17:35:57Z). Docs seulement : 0 changement de comportement.

## L2 — approve = décision métier seule (S-02/S-03/S-13, D1 v1.1)
**Périmètre livré** : `arbitrateClaim(approve)` n'appelle plus ni `triggerClaimRefund` ni `executeRefund` (0 alerte « refunds_disabled », plus de champ `refund` dans la réponse, audit `moneyMoved:false`, e-mail `claim_decision_approved` sans montant) ; ratification d'une approuvée non payée sans réécriture des décisions (S-06) ; `approveClaim` supprimé ; balayage → `routeClaimToArbitration` (jamais d'approbation machine, étape 2 supprimée) ; `autoResolveSmallClaim` inerte ; cause `refunds_disabled` retirée ; table des sorties v1.1 (`ratify` / `withdraw`+`pay`, `APPROVE_ALREADY_SET`) ; copies v1.1 (AM-B3, C4/D14, F15, G8, F14 `said.*`, T-56) sans « approuvez-la à nouveau » / « nouvelle approbation » / « sera payée » ; toast nominal `approvedNotSent` ×5, clé morte `admin.approved` supprimée ×5. `lib/refund.ts` intact (SHA épinglé vert). 0 schéma, 0 flag.

**Preuves** : `tests/claims-dprime-l2-approve-decision-only.test.ts` 15/15 (moteur RÉEL derrière un espion, 2 baux OUVERTS : approve → 0 executeRefund / 0 refunds.create / 0 ligne Refund ; contrôle négatif : `triggerClaimRefund` à la main → 1/1/1, réclamation `refunded`). 90 pins hérités inversés dans 23 fichiers, chacun avec contrôle négatif prouvant que l'ancien comportement (appel moteur inline après le CAS, étape 2 du balayage, copie v1) serait rouge. Suite complète : **465 fichiers / 6227 tests verts** (11 skipped, 17 todo — préexistants). `tsc` : 39 = 39 (baseline dab754d, 0 nouvelle erreur, 0 erreur produit). `next lint` 0 ; `check:i18n` 5/5 complètes ; `check:flags` OK ; build à froid OK.

**Constats de la revue (groupe B) corrigés dans le même lot** : (1) la première rédaction v1.1 de AM-B3 / `APPROVE_ALREADY_SET` promettait « elle sera payée par le rail financier » — phrase interdite par le pin gelé J-M31 → « elle relève du rail financier » (amendement R13 corrigé et tracé, J-M31 inchangé) ; (2) `said.no_refund_proven` / `said.no_refund_proven_rail_locked` (lib/claim-console-copy.ts) et le texte T-56 (`deadText`) nommaient encore « une nouvelle approbation » comme voie de paiement → rail financier (amendement v1.1 F14 tracé, valeurs v1 conservées) ; le scan statique L2 couvre désormais TOUTES les sources de copie réclamations + messages ×5 (commentaires exclus).

**Écarts de spec** : aucun sur spec v2. Sur R13 v1.1 : correction de wording AM-B3 (J-M31) et F13 (le toast livré est plus long que l'échantillon de l'amendement ; spec v2 §7 n'impose que « réécrit ») — tous deux tracés dans le bloc « v1.1 AMENDMENTS ».

## L1 — flags produit (SURFACE / INTAKE), spec v2 §3
**Périmètre livré** : nouveau `lib/claim-flags.ts` (lit `process.env` SEULEMENT, n'importe rien) — `isClaimsSurfaceEnabled`, `isClaimsIntakeEnabled`, `claimsSurfaceOpen`, `claimsIntakeOpen`, `claimNoticeGate(cls)`, `claimsFlagsSnapshot`, plus le bail legacy (`isClaimsEnabled`, `claimsGateState`, `CLAIMS_WINDOW_MAX_MS`) DÉPLACÉ là et ré-exporté par `lib/claims.ts` (mêmes fonctions). 25 sites migrés : POST /api/claims (SURFACE puis INTAKE → 403 `{error, gated:false, enabled:true, intakeOpen:false, reason:'intake_closed'}`, lu AVANT l'auth, sonde = UNKNOWN jamais CLOSED), GET /api/claims (+ overlay `intake_closed` sauf `not_owner`, `existingClaim`/`scope` conservés), contest / restaurant / respond / stale-alerts / arbitrate (SURFACE), GET /api/admin/claims **scindé** (fermé ⇒ `enabled:false`, listes workflow vides, **`actionableRefunds` toujours renvoyée** et comptée ; non-admin fermé ⇒ `{enabled:false}`), pages /orders et /admin/claims (console montée INCONDITIONNELLEMENT avec `surfaceOpen`), `lib/admin-overview` (états workflow sous SURFACE, états argent jamais gatés), `lib/admin-establishments` (idem, `MONEY_CLAIM_STATUSES` toujours comptés), route statut commande (réclamation SYSTÈME sous SURFACE, variante e-mail au send sous `claimNoticeGate('pre_money')`), census (tous les états de portes). E-mails FIN-EMAIL-01 : `pre_money` (ack, décisions, resto) suit la surface ; `closure` (closure-notice, reconcile, attribute, resolve-stuck) TOUJOURS envoyable (S-25). `auto-approve` reste le SEUL lecteur du bail legacy (S-13). Opérateurs : Mode A, Mode B et `refund-gate` (precheck ET window) REFUSENT en NOMMANT le flag ; Mode A distingue `UNKNOWN(403) = intake_closed` ; `refund-gate` sonde désormais `POST /api/claims` ; `WATCHED_SECRET_KEYS` +2. `check-flags` : ERREUR `CLAIMS_INTAKE_ENABLED ⇒ CLAIMS_SURFACE_ENABLED` (21 règles), WARNING bail legacy inerte sous SURFACE, WARNING valeur ≠ `'true'`. i18n ×5 : `eat.help.claimIntakeClosed`, `claims.admin.surfaceClosedEmpty`. Docs : `flags.md`, précheck opérateur R13, runbook répétition refund (en-têtes « valides uniquement SURFACE/INTAKE absents », historique figé Mode A/B).

**Preuves** : `tests/claims-dprime-l1-flags.test.ts` 30/30 — matrice pure, **S-12** (flags produit absents ⇒ `claimsSurfaceOpen ≡ claimsIntakeOpen ≡ isClaimsEnabled` sur 7 états de bail, raisons T-53 préservées), **S-13** (auto-resolve inerte sous SURFACE+INTAKE+flag+plafond ; auto-approve 403 ; 0 couplage à REFUNDS), **S-14** (refus opérateurs par nom, pins statiques), **S-23** (403 `intake_closed` avant auth + overlay + `not_owner` intact), **S-24** (workflow complet sous SURFACE+INTAKE=false), **S-25** (`post_money`/`closure` toujours vrais), `'TRUE'`/`'1'`/`''` = OFF, scan statique des 25 sites et des classes d'e-mail par fichier. 69 pins hérités inversés dans 15 fichiers (chacun avec contrôle négatif sur la forme 05152b6). Suite complète **466 fichiers / 6284 tests verts** ; `tsc` 39 = 39 (baseline dab754d, 0 erreur produit) ; `next lint` 0 ; `check:i18n` 5/5 ; `check:flags` OK et `CLAIMS_INTAKE_ENABLED=true` seul ⇒ **exit 1** ; build à froid OK.

**Écarts de spec** : aucun. Précision tracée : `CLAIMS_SURFACE_ENABLED='false'` (explicite) n'est pas « flag produit présent » au sens de la matrice — seul `'true'` rend le bail legacy inerte ; le kill-switch exige donc bien SURFACE=false **et** INTAKE=false **et** bail absent (procédure §3.5 inchangée), et le test le pin.

## L3a — MIGRATION STAGING EXÉCUTÉE ET PASS (2026-09-22, sortie fondateur)
**RESULT: PASS** · hash `43cdb3bb51fbe95b` (identique au SQL compilé) · base `deyi0010_grubano_staging` · build déployé `9c86fb3` · sauvegarde VÉRIFIÉE `staging-pre-dprime-2026-09-22-20-23-52.sql.gz` (65,5 Ko gz, 44 INSERTs, dump complet) · baseline `{"claim":9,"order":67,"refund":6,"ledgerEntry":76}` · MIGRATION APPLIED (+Claim.approvedAmountCents +Claim.selection +Order.deliveredAt, **all NULL**) · POST-MIGRATION INTEGRITY PASS · MONEY MOVED: NO. Les trois colonnes EXISTENT donc en base staging depuis cette date ; toute exécution ultérieure de l opérateur rend ALREADY_APPLIED_AND_VERIFIED.

## L3a — opérateurs serveur (aucune migration exécutée)
**Périmètre livré** : `scripts/server/dprime-staging-migrate.js` et `scripts/server/dprime-regen-client.js` UNIQUEMENT. **`prisma/schema.prisma` n'est PAS touché** (c'est L3b) et **aucune migration n'a été exécutée** : rien n'a été lancé sur la DB staging.

`dprime-staging-migrate.js` — fail-closed, idempotent, STAGING seulement. SQL COMPILÉ dans le fichier (hash `43cdb3bb51fbe95b`) : exactement 3 `ALTER TABLE … ADD COLUMN IF NOT EXISTS … NULL` (`Claim.approvedAmountCents` INTEGER, `Claim.selection` JSON, `Order.deliveredAt` DATETIME(3)). Étapes : 1 env + auto-contrôle du SQL (toute instruction non purement additive ⇒ refus AVANT toute connexion) · 2/3 preuve STAGING (refus si nom de base OU URL production, refus si ambigu) · 4 épingles fondateur `DPRIME_EXPECT_DB` / `DPRIME_EXPECT_SHA` (lit `public/version.json`) · 5 tables présentes · 6 état partiel ⇒ refus, état complet ⇒ `ALREADY_APPLIED_AND_VERIFIED` · 7 sauvegarde mysqldump VÉRIFIÉE et horodatée (taille, marqueur « Dump completed », ≥ 1 INSERT, présence de `CREATE TABLE Claim` et `Order`, aller-retour gzip) · 8 baseline · 9 application · 10 vérification `information_schema` (présente, NULLABLE, sans défaut, type attendu — MariaDB `longtext` accepté pour JSON) · 11 préservation des comptes + **chaque nouvelle colonne NULL partout** (aucun backfill). Jamais `--accept-data-loss`, jamais `db push`, jamais `prisma-push.sh`. Aucun flag, aucune gate, aucun Stripe, aucun e-mail, aucune écriture dans `.env.local` ; DSN masquée dans toute sortie.

`dprime-regen-client.js` — vérifie que le schéma DÉPLOYÉ porte les 3 champs **sur leur propre modèle** (refus « D′ L3b non déployé » au lieu de certifier un client périmé), régénère avec `prisma@5.22.0` (la commande exacte du workflow, sans shell), exige le marqueur « Generated Prisma Client », **prouve** les champs dans `index.d.ts` via `ClaimScalarFieldEnum` / `OrderScalarFieldEnum` (un simple `includes` passerait sur un client périmé), puis touche `tmp/restart.txt`. N'emprunte rien au contrat Phase 1 (les champs fidélité sont absents du fichier).

**Preuves** : `tests/claims-dprime-l3a-operators.test.ts` 16/16 — les DEUX opérateurs exécutés en VRAIS processus enfants dans un faux monde isolé (racine temporaire, `mysqldump` qui ne doit jamais exister, DSN jamais joignable) : refus base production / URL production / cible ambiguë / base ≠ épingle / `version.json` absent / SHA ≠ épingle / `DATABASE_URL` absente, DSN masquée (ni utilisateur ni mot de passe imprimés), **0 sauvegarde écrite et `mysqldump` jamais exécuté** ; SQL compilé conforme + contrôle négatif (DROP / NOT NULL / DEFAULT / CREATE INDEX rejetés par le garde) ; regen : refus d'un schéma pré-L3b, refus d'un champ posé sur le MAUVAIS modèle (qu'un scan par sous-chaîne aurait laissé passer), jamais de PASS sans génération, contrôle négatif `DPRIME_VERIFY_FIELDS`. Suite complète **467 fichiers / 6300 tests verts** ; `tsc` 39 = 39 (baseline `dab754d`) ; `next lint` 0 ; `check:i18n` 5/5 ; `check:flags` OK ; build à froid OK.

**⛔ ARRÊT OBLIGATOIRE ICI** (autorisation fondateur limitée à L0 → L2 → L1 → L3a) : la migration staging n'est pas exécutée, la DB staging n'est pas modifiée, aucune gate n'est ouverte, aucune opération Stripe n'a eu lieu.

## L3b — schéma + schemaReady (aucune migration, aucune écriture DB)
**Périmètre livré** : `prisma/schema.prisma` déclare EXACTEMENT les trois colonnes additives nullables de la spec v2 §9 — `Claim.approvedAmountCents Int?`, `Claim.selection Json?`, `Order.deliveredAt DateTime?` — sans défaut, sans index, sans attribut, sur leurs propres modèles. Commentaires de schéma devenus faux corrigés : l'en-tête `Claim` disait « no deliveredAt column exists », « AUTO-APPROVED by the internal cron » et « ACCEPT → triggers the P4.5-A engine » (tous faux depuis D′ L2), et annonçait le gating `CLAIMS_ENABLED` (faux depuis D′ L1) ; `responseDeadlineAt` et `decidedBy` suivent. Nouveau `lib/schema-ready.ts` : `clientSchemaReady()` (synchrone, SANS base — lit les énumérations `ClaimScalarFieldEnum` / `OrderScalarFieldEnum` du client GÉNÉRÉ, donc un champ posé sur le mauvais modèle est refusé) et `schemaReady()` (ajoute une lecture LIMIT 1 par modèle **via l'API Prisma, aucun SQL brut** — CLAUDE.md §12 ; P2022 = colonne absente). Fail-closed, jamais d'exception, mise en cache : verrouillée une fois prête, re-sondée toutes les 30 s tant qu'elle ne l'est pas (un regen + restart est donc vu sans redéploiement). Exposée dans le recensement (`schema`), booléens et noms de champs uniquement.

**Ce que L3b ne fait PAS, délibérément** : aucune route ne se gate encore sur `schemaReady` (la spec §9 nomme pay/withdraw/approve/POST claims — les deux premières n'existent pas avant L4/L5, et le fondateur a exclu toute autre fonctionnalité de ce lot) ; aucun code ne lit ni n'écrit les trois colonnes ; zéro migration, zéro écriture DB, zéro gate, zéro flag, zéro Stripe, zéro e-mail. `lib/refund.ts` byte-identique.

**Preuve que les colonnes SERVEUR correspondent au schéma attendu** : la chaîne est fermée en quatre maillons vérifiés par test — l'opérateur L3a a vérifié la base RÉELLE via `information_schema` (présence, NULLABLE, sans défaut, type attendu) et a rendu PASS le 2026-09-22 ; le test `PROOF — schema ⇄ L3a migration SQL ⇄ regen operator ⇄ schema-ready` exige que les quatre artefacts nomment les mêmes trois colonnes, sur les mêmes tables, avec des types compatibles (`Int?`↔`INTEGER`, `Json?`↔`JSON`/`longtext` MariaDB, `DateTime?`↔`DATETIME(3)`) et épingle le hash `43cdb3bb51fbe95b` de la migration appliquée.

**Preuves** : `tests/claims-dprime-l3b-schema-ready.test.ts` 22/22, `tests/claims-dprime-l3a-operators.test.ts` 22/22 (dont 6 nouveaux sur l'épingle SHA du regen), recensement 20/20 (3 nouveaux, dont un contrôle négatif : sonde impossible ⇒ `schema.ready false` ET le recensement répond quand même ses comptes). Suite complète **468 fichiers / 6331 tests verts** ; `tsc` 39 = 39 (0 erreur produit) ; `next lint` 0 ; `check:i18n` 5/5 ; `check:flags` OK ; build à froid OK. Diff `schema.prisma` : 3 lignes structurelles ajoutées, 2 commentaires de champ corrigés, rien d'autre.

**Revue adversariale** (4 lentilles × vérification adversariale de chaque constat, 29 agents) : 25 constats bruts, 24 réfutés preuve à l'appui, **1 confirmé et corrigé** — mon commentaire de `approvedAmountCents` citait « D-1 STRICT » et disait « borné par le plafond de réclamation, jamais le montant demandé ». Les deux sont faux : D-1 est la règle d'ancre `deliveredAt` (spec v2 :18), et la borne réelle est T-07 / S-10 `1 ≤ approvedAmountCents ≤ requestedAmountCents` avec `reduceReason` obligatoire si `<` — le plafond Stripe (E6) est AFFICHÉ, jamais la borne, et il est PLUS LARGE que le montant demandé. Un auteur de L4 lisant ce commentaire aurait pu valider contre le plafond et approuver plus que ce que le client a demandé. Corrigé, et la formulation exacte est désormais épinglée avec contrôle négatif sur l'ancienne.

**Addition hors liste, requise par votre stop-gate** : `dprime-regen-client.js` gagne l'épingle `DPRIME_EXPECT_SHA` (étape 0, lit `public/version.json`, refuse si absent ou différent — avant toute génération), la PREUVE du touch de redémarrage (relecture du contenu + mtime fraîche) et une mention explicite que le rechargement Passenger est asynchrone et **n'est pas prouvé** par le script : c'est le recensement (`schema.ready true`) qui le prouve. Sans cette addition la commande unique demandée n'aurait pas pu vérifier le SHA L3b.

## L3b — CLÔTURE RUNTIME (2026-09-23, après regen fondateur)
**Regen exécuté une seule fois** (commande cPanel épinglée) : RESULT PASS · DEPLOYED BUILD `e2635e2` · route `local CLI node_modules/prisma/build/index.js` · CLIENT FIELDS VERIFIED (`Claim.approvedAmountCents`, `Claim.selection`, `Order.deliveredAt`, model-scoped) · restart touché, relu, mtime fraîche · MONEY MOVED: NO.

**Vérification runtime read-only** — recensement staging `claims-census.yml` run 35837286413, HTTP 200, 2026-09-23T08:28:18Z :
- `schema` = `{"ready":true,"clientReady":true,"dbReady":true,"missingClient":[],"missingDb":[],"why":null}` → les trois colonnes sont reconnues par le client GÉNÉRÉ (énums model-scoped) ET lues réellement en base.
- **Preuve du rechargement Passenger** : le SSH post-deploy de la CI a de nouveau expiré (`dial tcp 109.234.165.222:22: i/o timeout`, 08:19:07Z) — la CI n'a donc rien régénéré. Le client chargé au démarrage du process est celui que lit `clientReady` ; avant le regen il ne connaissait pas ces champs. `clientReady:true` à 08:28:18 prouve donc que le process servant la requête a chargé le NOUVEAU client, donc qu'il a démarré APRÈS l'écriture du regen. Le rechargement est prouvé, pas supposé.
- `gates` = `{claimsEnabled:false, claimsGate:"CLOSED (flag_off)", claimsSurfaceEnabled:false, claimsIntakeEnabled:false, claimsSurfaceOpen:false, claimsIntakeOpen:false, refundsEnabled:false}` → les flags PRODUIT sont désormais mesurés DIRECTEMENT dans le process (ferme le trou signalé à L3a : la valeur littérale de `CLAIMS_INTAKE_ENABLED` n'était pas distinguable de l'extérieur).
- Sondes externes : `POST /api/claims` 403 `{gated:true}` · `POST /api/admin/refunds/run` 403 `{gated:true}` · `/fr/eat` 200 · `/api/restaurants` 200 (lignes réelles).
- `version.json` = `e2635e279683904183d357e4696d5285f6479b29` (build 08:06:08Z, ciRunId 35834575806).
- **Population réclamations IDENTIQUE au recensement L0 mesuré à `dab754d`** : total 9, `{refunded:4, refused:3, refused_final:2}`, `closure.terminalWithoutRecord:4`, `approvedUnpaid:0`, `active:0`, tous les compteurs d'anomalie héritée à 0. Aucune ligne de réclamation n'a bougé sur L0 → L2 → L1 → migration → L3b → regen.

**Preuve Prisma 5.22.0 (équivalente, pas une lecture de version)** : le serveur n'est pas lisible d'ici. La route utilisée est le CLI PROPRE à l'app (`node_modules/prisma/build/index.js`), et `package.json` épingle `prisma` et `@prisma/client` à exactement `5.22.0` (sans caret) — c'est la règle CLAUDE.md, contre le v7 global du serveur. `dbReady:true` prouve que le client régénéré EXÉCUTE réellement des requêtes via le `@prisma/client` 5.22.0 chargé et son moteur ; un client d'un autre majeur échouerait au chargement ou à la requête. Une lecture directe de la version exigerait un `cat node_modules/.prisma/client/package.json` sur le serveur.

**Limites nommées** : le recensement n'expose pas les totaux `Order` / `Refund` / `LedgerEntry` (67 / 6 / 76 prouvés égaux avant/après par l'opérateur de migration lui-même, pas re-mesurables ici) ; aucune lecture Stripe n'est possible depuis ce poste (le recensement n'en fait aucune par construction) — l'évidence est que la gate remboursements est fermée dans le process, qu'aucune réclamation n'est dans un état argent et que tous les compteurs d'anomalie sont à 0.

**VERDICT : L3b RUNTIME VERIFICATION = PASS · SCHEMA READY = TRUE · PRISMA CLIENT = PASS · L3b = CLOSED.**

## L4 — approbation avec montant, ratification, retrait, file « À rembourser » (aucun argent)
**Périmètre livré** (spec v2 T-07/T-08/T-09, §4, §7.4, §8.3, §8.5, §6.4) :

*Décision.* `arbitrateClaim` prend désormais `approvedAmountCents`, `confirm` et `reduceReason`. Le serveur valide TOUT avant le moindre CAS : `confirm` doit valoir exactement `APPROUVER`, le montant doit être un entier strictement positif **≤ `requestedAmountCents`** (S-10 — la borne est le montant DEMANDÉ, jamais le plafond Stripe qui est plus large et seulement AFFICHÉ), et un montant strictement inférieur exige un motif de réduction ≥ 10 caractères. Le corps de la requête n'est jamais une autorité. Après succès : `status='approved'`, `arbitrationDecision='approved'`, `approvedAmountCents`, `arbitratedBy/At`, `decidedBy/At`. Toujours **zéro** `triggerClaimRefund`, `executeRefund`, `stripe.refunds.create`, ligne `Refund` — y compris avec le bail REFUNDS OUVERT dans le faux monde.

*Ratification (T-08).* Les deux CAS (première décision et ratification) épinglent `approvedAmountCents: null` (S-29) : un montant déjà fixé n'est JAMAIS réécrit. Une seconde approbation répond 409 `APPROVE_ALREADY_SET`. La seule voie légitime pour changer un montant décidé est retrait → arbitrage → nouvelle décision.

*Retrait (T-09, §4).* Nouvelle route `POST /api/admin/claims/[id]/withdraw-approval` + `withdrawClaimApproval` : `resolveAdmin()`, gate `claimsSurfaceOpen()`, `rateLimit` 10/60, `confirm='RETIRER'`, motif ≥ 10. **409 `audit_disabled` si `ADMIN_AUDIT_ENABLED !== 'true'`, AVANT toute lecture** (S-30). Refus si une frontière financière est franchie : `refundAttempted`, `refundId`, tout `refundError` hors preuve sans-moteur (`no_refund_proven:v13:`, `no_refund_proven_rail_locked:`), ou une ligne `Refund` estampillée `claim:<id>` — et **refus aussi si ces lignes ne sont pas lisibles** (ne pas savoir n'est pas une permission). CAS épinglant tout le pré-état (dont `refundError` et `approvedAmountCents`) donc exclusif avec T1 (S-09). **Transaction unique** : CAS + `adminAuditLog.create` ; échec d'audit ⇒ rollback complet. Retour `status='arbitration'`, décision et montant remis à null, `activeOrderKey` conservé, **jamais `refused_final`**. Aucune limite de 24 h.

*Amendement T1 (§8.3, seul delta du moteur).* `T1_SELECT` += `approvedAmountCents`, `arbitrationDecision`. Après la porte RE et la lecture du pré-état : `payableCents = before.approvedAmountCents` ; si `arbitrationDecision !== 'approved'` ou le montant est nul / non entier / ≤ 0 / > demandé ⇒ **`{state:'failed', error:'amount_not_ratified'}` avec ZÉRO écriture** (S-27, aucun repli sur `requestedAmountCents`). Le CAS d'attente épingle `arbitrationDecision:'approved'` et `approvedAmountCents: payableCents` (S-11). Les 6 usages du montant lisent `payableCents`. T2/T3/T4 inchangés. **`lib/refund.ts` byte-identique** (pin SHA vert).

*Plafond.* Nouvelle route lecture seule `GET /api/admin/claims/[id]/ceiling` : réutilise `buildClaimScopeForOrder` (UNE définition du plafond dans le code), renvoie `requestedAmountCents`, `maxRefundableCents`, `alreadyRefundedCents`, `ceilingVerified` (T-59 : faux ⇒ Stripe non lu ou paiement contesté, le nombre est une estimation qui peut être TROP HAUTE) et `approvalBoundCents` = le montant demandé. Un plafond incalculable est rendu INCONNU (409 `ceiling_unreadable`), jamais un nombre.

*Files (§8.5) — LECTURE SEULE dans ce lot.* `listApprovedAwaitingPayment()` (forme exacte que le rail L5 sélectionnera, FIFO `arbitratedAt`, plafonné à 20, projection sans PII) et `listAwaitingRatification()` (approuvées héritées sans montant, non payables). Exposées par `GET /api/admin/claims` comme `awaitingPayment` / `awaitingRatification` + compteurs — **y compris dans la branche kill-switch** pour `awaitingPayment` : une décision non payée est de l'ARGENT et reste visible flag fermé. Relabel `legacy_pending_money_decision` → `awaiting_payment`. **Aucun bouton fonctionnel de paiement n'existe** : le « Payer le lot » de L5 n'est qu'un placeholder visiblement désactivé, sans `onClick` et sans endpoint.

*Console (§7.4).* Dialogue d'approbation OBLIGATOIRE (référence commande, montant demandé, déjà remboursé, reste remboursable + phrase neutre quand non vérifié, montant approuvé, case réduction + motif, confirmation à saisir) portant la phrase **« Cette approbation ne déclenche pas encore le remboursement. »** ; l'avertissement « au-dessus du reste » est **bloquant quand un plafond a été lu** (le plafond DB est une sur-estimation : le dépasser rend la décision impayable) et seulement informatif si le plafond n'a pas pu être lu. Files « À rembourser (n) » et « À ratifier (n) », retrait en ligne. 37 clés i18n × 5 locales.

*E-mail (D-11, §6.4).* `sendClaimDecisionEmail` gagne `approvedCents` (**relu en base après le CAS**, jamais le corps de requête, jamais le montant demandé) et `decisionStamp` ; dédup **par décision** (`claim:<id>:approved:<ISO>` / `:withdrawn:<ISO>`) via `decisionDedupeKey`, donc un cycle approuver 500 → retirer → approuver 400 envoie DEUX avis d'approbation distincts + un avis de retrait. Nouveau genre `approval_withdrawn`. Corps `approved` réécrit ×5 : il nomme le montant, dit que le remboursement « sera exécuté séparément par notre équipe », ne promet aucun délai bancaire et ne dit jamais « déjà remboursé ».

*schemaReady (S-27).* Les deux routes qui ÉCRIVENT `approvedAmountCents` (approve, withdraw) répondent **503 `{reason:'schema_not_ready', schemaReady:false}`** si la sonde n'est pas prête. Correction issue de la revue : le gate porte sur TOUT le POST d'arbitrage, **y compris `refuse_final`** — un refus n'écrit aucune colonne neuve, mais `arbitrateClaim` les LIT (son unique `findUnique` sélectionne `approvedAmountCents` avant toute branche), et un client Prisma périmé rejette cette requête : gater la seule approbation aurait laissé le refus planter en 500 au lieu de répondre « pas maintenant ». `GET /api/admin/claims` applique la même règle à ses deux files D′ : sonde pas prête ⇒ elles ne sont pas lues et le payload porte `schemaReady:false`, pour que la console distingue « rien à payer » de « je ne peux pas lire ».

**Preuves ciblées** : `tests/claims-dprime-l4-approved-amount.test.ts` 28/28 (les neuf contrôles A→I, moteur RÉEL derrière l'espion sur le même monde en mémoire — « aucune écriture Stripe » est donc MESURÉ, et chaque contrôle porte son contrôle négatif prouvant que le monde PEUT bouger), `tests/claims-dprime-l4-withdraw-route.test.ts` 9/9, `tests/claims-dprime-l4-ceiling-route.test.ts` 8/8. Suite complète **471 fichiers / 6405 tests verts** (0 échec) ; `tsc` 39 = 39 (baseline `dab754d`, aucune erreur nouvelle — seul un numéro de ligne a bougé dans un fichier de test agrandi) ; `next lint` 0 erreur ; `check:i18n` 5/5 complet (7963 clés) ; `check:flags` OK ; build à froid OK, les deux nouvelles routes compilées. `lib/refund.ts` byte-identique au baseline (blob git `e2dc42b8eb89cd3249a4497682b8bc94080ef725` inchangé).

**Revue adversariale L4** (5 lentilles × vérification adversariale de chaque constat, 34 agents) : 29 constats bruts, 15 confirmés et TOUS corrigés avant commit, dont **2 P1 distincts** :
1. *Moitié réconciliation restée sur le montant DEMANDÉ (S-11).* `reconcileNoRowByDerivation` jugeait `requestedAmountCents` alors que le rail paie désormais `approvedAmountCents`, puis ÉCRIVAIT ce verdict dans la réclamation comme preuve d'argent. Une réclamation approuvée à 10 € sur une commande où il reste 20 € aurait été verrouillée par un verdict calculé pour les 40 € demandés, et le texte de preuve aurait nommé un montant que personne n'a décidé. Corrigé : `judged = approvedAmountCents ?? requestedAmountCents` alimente le chargeur ET le texte de preuve ; `reconcileClaimEvidence` charge la colonne (sans quoi le repli était systématique) ; l'alerte FV nomme aussi le montant décidé (`approvedCents`).
2. *Files D′ lues sans garde.* `GET /api/admin/claims` appelait les deux nouveaux lecteurs sans consulter la sonde : sur un process au client périmé, toute la route admin — y compris l'argent legacy — serait tombée en 500. Corrigé (garde + `schemaReady` dans les deux payloads), avec contrôle négatif.
Plus 7 P2 actionnables corrigés : parité de contrôles D0 (`acceptedExits` n'offrait « payer » / « retirer » que les serveurs auraient refusés — `railWouldAttempt` / `withdrawWouldAccept` miroitent désormais les refus réels), `ratifyRefusal` porté par chaque ligne « À ratifier » (la console désactive exactement ce que le serveur déclinerait, avec son message), avertissement « au-dessus du reste » rendu bloquant quand un plafond a été lu, garde `ceiling.claimId === approveTarget.id` (un plafond d'une AUTRE réclamation ne peut plus valider un montant), formatage monétaire français (`5,00 €`), et le gate `refuse_final` ci-dessus.

### L4 — déploiement staging vérifié (2026-09-23)
Commit `fc5a9a0b4cddc7d76aa211ae451a303dfdbf5926`, poussé sur `develop`. CI « Deploy to Staging » run 35854888120 : job `test` **success**, job `deploy` **success**. `public/version.json` servi par staging = `fc5a9a0…` (build 11:36:25Z, ciRunId 35854888120) — le SHA déployé est EXACTEMENT celui de L4. Santé : `/fr/eat` 200, `/api/restaurants` 200 (lignes réelles).

Recensement read-only `claims-census.yml` run 35858074959, HTTP 200, mesuré 2026-09-23T12:03:00Z :
- `schema` = `{ready:true, clientReady:true, dbReady:true, missingClient:[], missingDb:[], why:null}` — la sonde reste verte après le déploiement L4 (aucun regen supplémentaire n'a été nécessaire, aucune migration n'a été exécutée).
- `gates` = `{claimsEnabled:false, claimsGate:"CLOSED (flag_off)", claimsSurfaceEnabled:false, claimsIntakeEnabled:false, claimsSurfaceOpen:false, claimsIntakeOpen:false, refundsEnabled:false}` — toutes les portes fermées, les deux flags PRODUIT toujours OFF.
- **Population réclamations INCHANGÉE** depuis le recensement L0 mesuré à `dab754d` : total 9, `{refunded:4, refused:3, refused_final:2}`, `active:0`, `nonTerminal:3`, `approvedUnpaid:0`, `closure.terminalWithoutRecord:4`, **tous les compteurs d'anomalie héritée à 0**. Aucune ligne n'a bougé.

Sondes externes des trois routes d'écriture/lecture D′ (non authentifiées, surface fermée) : `POST /api/admin/claims/<id>/arbitrate` 403 `{"error":"Réclamations indisponibles","gated":true}` · `POST /api/admin/claims/<id>/withdraw-approval` 403 idem (la nouvelle route est bien déployée et refuse à la porte de surface, AVANT toute lecture) · `GET /api/admin/claims/<id>/ceiling` 403 idem · `POST /api/claims` 403 `{gated:true}` · `POST /api/admin/refunds/run` 403 `{"error":"Remboursements indisponibles","gated":true}`.

**Aucune migration, aucun regen Prisma, aucun accès cPanel, aucun flag modifié, aucune opération Stripe, aucun e-mail envoyé pendant ce lot.**

## L5 — le rail financier séparé (aucun argent déplacé, aucune fenêtre ouverte)
**Périmètre livré** (spec v2 §8.1–§8.8, invariants S-04/S-05/S-06/S-07/S-08/S-09/S-10/S-11/S-14/S-14b/S-27/S-30) :

*Une seule définition de « payable ».* Nouveau `lib/claims-payable-core.js` (+ `.d.ts`) — CommonJS pur, sur le modèle de `lib/ledger-check-core.js` : la sélection §8.5 (`status`/`arbitrationDecision` approved, `refundAttempted` false, `refundId` null, `refundError` null, `approvedAmountCents` not null), l'ordre FIFO (`arbitratedAt` puis `createdAt`), la projection sans PII, le plafond de lot 20, la borne S-10 et l'identité `{claimId, approvedAmountCents, arbitratedAt}` qu'un lot signe. **Trois lecteurs, une phrase** : la file admin de `lib/claims.ts`, le dryRun du rail, et le précheck de l'opérateur — qui `require` le MÊME fichier, expédié par le déploiement (`cp lib/claims-payable-core.js`). Le fichier est gelé (`Object.freeze`), n'importe rien, ne lit aucune variable d'environnement, n'écrit rien.

*Le jeton dryRun → PAYER.* Nouveau `lib/claims-pay-token.ts` : `base64url(JSON) + '.' + HMAC-SHA256` avec une clé **dérivée** de `NEXTAUTH_SECRET` par le contexte `claims-pay-approved-v1` — un jeton du rail ne peut jamais être rejoué comme un lien magique, ni l'inverse. La signature lie la version, l'admin, le SHA déployé (`public/version.json`), `iat`/`exp` (10 min, vérifiés dans les deux sens), le bail lu au moment du dryRun (porté, **jamais** une autorité) et, par réclamation, `{claimId, approvedAmountCents, arbitratedAt}`. La vérification ne croit RIEN du contenu avant d'avoir vérifié la signature. Sans secret : la frappe **lève**, la vérification répond `no_secret` — un rail qui distribue une autorité non signée est pire qu'un rail qui refuse. Le mint refuse aussi un lot vide, un lot au-delà de 20 et un item mal formé : les deux moitiés du fichier ne peuvent pas se contredire.

*Les décisions, pures.* Nouveau `lib/claims-pay-rail.ts` : le vocabulaire FERMÉ des issues de §8.6, `classifyRailResult`, `preflightVerdict`, `payableShapeRefusal`, `leaseUsable` (marge 60 s), `bucketOf`, `noticeDue`, `tally`, `PAY_BUDGET_MS` 40 s. Aucun Prisma, aucun Stripe, aucune horloge propre : la route fait les entrées-sorties, ce module dit ce qu'elles signifient.

*Le rail.* Nouvelle route `POST /api/admin/claims/pay-approved` — `resolveAdmin()` **seul** (aucun chemin `INTERNAL_CRON_TOKEN` n'existe dans le fichier), `rateLimit` 5/60, 503 si la sonde de schéma n'est pas prête. **dryRun** : lecture seule, non gaté sur le bail ni sur le flag produit — un admin doit pouvoir voir ce qui attend avant que quiconque ouvre quoi que ce soit ; sélection §8.5 (ou des `claimIds` explicites, dédupliqués) ; préflight par réclamation (`preflightRefundFunding`, reste remboursable **live** via l'unique constructeur de plafond, ligne `Refund` en attente sur la commande) ; **seules les réclamations réellement payables sont signées**. **PAYER** : mot `PAYER` saisi, puis `isRefundsEnabled()` ET `isClaimsSurfaceEnabled()` — le **bail legacy n'ouvre jamais le rail** (S-14) — puis 409 `audit_disabled` sans journal d'audit (S-30), puis le jeton ; ensuite, par item et **dans l'ordre signé, sans jamais re-sélectionner** : budget 40 s, `refundGateState()` relu avec sa marge de 60 s, relecture de la réclamation, et un seul appel : `triggerClaimRefund`. Un corps contradictoire (`dryRun` + autorisation de paiement) est refusé au lieu d'être tranché.

*Deux refus distincts, deux mots distincts.* `skipped:stale_dryrun` = la DÉCISION a bougé sous le lot (montant ou instant) ; `skipped:not_selectable` = la décision est intacte mais un état argent a été enregistré depuis, et la clause qui refuse est nommée. Les deux avec **zéro écriture**.

*La classification d'un refus moteur ambigu.* `triggerClaimRefund` rend `{state:'failed', error:<phrase du moteur>}` pour DEUX situations très différentes : le moteur a refusé **avant** de créer quoi que ce soit (0 € a bougé), ou sa ligne existe / n'a pas pu être lue (**l'argent a peut-être bougé**) — le littéral `engine_own_row` n'atteint jamais la valeur de retour. Plutôt que de modifier le moteur gelé, le rail **relit ce que la tentative a écrit** : seule la branche « rien créé » estampille la réclamation `approved` avec le préfixe `engine_failed: `. Tout le reste — y compris une réclamation qu'on n'a **pas pu** relire — est classé `review:engine_own_row`, le seau qui dit « un humain doit établir ce qui a été versé ». Deviner dans l'autre sens imprimerait « 0 € » au-dessus d'un remboursement peut-être parti. C'est aussi ce qui range correctement la phrase du moteur « Remboursement émis, reprise de la royalty franchisé en échec », qui arrive comme un ÉCHEC alors que le client a été remboursé.

*L'avis client.* Sur `paid` uniquement (§8.6 : `accepted_pending` n'envoie RIEN, aucun refus ne notifie), via `sendClaimClosureEmail` avec la preuve Stripe relue dans la requête — le montant que le client lit est celui que Stripe a réglé, jamais le montant approuvé ni la prédiction de la ligne. Classe **`post_money`** : le rail est le premier fichier de cette classe, exactement comme §6.1 l'annonçait. Best-effort de bout en bout : l'argent est déjà parti, un e-mail ne fait jamais échouer un paiement.

*L'audit.* `claim.pay` par réclamation (issue, seau, `moneyMoved`, montant approuvé, montant moteur, ligne `Refund`, id Stripe, erreur, évidence, sort de l'avis) — y compris pour les items ignorés, car ce que le rail a regardé et refusé fait partie de la trace ; `claim.pay_batch` pour le lot (demandé, payé, en attente, retenu, à revoir, échoué, ignoré, non atteint, `stoppedBy`, expiration du bail) ; et `claim.pay_dry_run` pour ce qui a été MONTRÉ, à qui.

*L'opérateur, livré REFUSANT.* Nouveau `scripts/server/phase2-claims-pay-window.js` (718 lignes) — **jamais exécuté en conditions réelles dans ce lot**. `CERTIFIED_SHAS` est **vide** : tout run s'arrête au précheck de certification, sans rien écrire, en sortie non nulle. Il réutilise `writeFlag` / `armRefreeze` / `emergencyRefreeze` de `phase2-refund-gate.js` (jamais une copie) derrière un **garde de clés compilé** : les quatre flags CLAIMS sont refusés par construction, seules `REFUNDS_WINDOW_UNTIL` et `REFUNDS_ENABLED` sont écrivables. Prechecks : staging seul (racine, `NEXTAUTH_URL` **du fichier**, nom de base, Stripe TEST), `CLAIMS_SURFACE_ENABLED='true'`, `CLAIMS_ENABLED` absent/false, `ADMIN_AUDIT_ENABLED='true'`, sept flags machine dangereux, plafond d'auto-approbation, `schema.ready` du process, sélection **recalculée avec le core expédié**, **T-42 par compte Connect** (`available` ≥ somme brute approuvée du compte ; solde illisible = manque ; charge contestée, paiement sans destination, devise non EUR et livemode écartés avec anomalie), fenêtre ≤ 28 min. Ouverture : **bail d'abord**, puis flag, redémarrage **prouvé par la sonde**. Fermeture : **bail expiré d'abord**, puis flag, redémarrage, sonde 403, `backup-neutralize`. Seul `PASS` sort 0 ; un précheck vert sort 1, parce qu'il n'a rien ouvert.

*La console.* La file « À rembourser » porte maintenant « Simuler le lot » → résumé (nombre, total, retenues avec leur cause en français, expiration du bail ou « aucune fenêtre ouverte ») → « Payer le lot (n) », activé **seulement** si un jeton existe, le bail est utilisable, la surface est ouverte et l'audit est actif (sinon chaque condition manquante est dite en mots) → dialogue de confirmation dans l'idiome du dialogue L4, mot `PAYER` à saisir → rapport par réclamation. Les tables d'issues et de retenues sont des `Record` **totaux** : une nouvelle issue sans sa phrase est une erreur de compilation, jamais un littéral brut à l'écran. `PAY_CONFIRM_WORD` est désormais exporté par `lib/claim-action-rules` et **importé** par la route et la console — plus aucun mot de confirmation retapé.

**Répétition MariaDB — la condition bloquante du fondateur, exécutée.** `tests/claims-pay-race.db.test.ts` sur MariaDB **12.3.2** jetable (`127.0.0.1:3310`, base `claims_race`), **DEUX PROCESSUS** système, chacun avec son client Prisma, exécutant le code produit (`triggerClaimRefund` complet, `withdrawClaimApproval` avec sa transaction) via un bundle esbuild où le moteur, Stripe et l'expéditeur d'alertes sont remplacés à la compilation — aucune clé, aucun client, aucune requête : la répétition **ne peut pas dépenser**. Le garde de cible est le MÊME que celui de la répétition d'attribution, désormais dans `tests/support/rehearsal-target.ts` (une seule définition de « ne jamais pointer ceci sur des données réelles »).
- **A (S-08)** — deux rails sur UNE réclamation, 20 itérations : **20/20** exactement **un** appel moteur (compté dans un journal auquel les deux processus ajoutent, pas déduit de l'un d'eux) et exactement un paiement ; gagnants processus A ×9 / B ×11, et le test **exige** que les deux aient gagné au moins une fois — « un seul gagnant » serait aussi vrai si un seul contendant démarrait jamais.
- **B (S-09)** — un retrait contre le rail, 20 itérations : **20/20** exactement un gagnant, et là encore chaque camp doit avoir gagné au moins une fois (rail ×5, retrait ×15). Rail gagnant ⇒ réclamation `refunded`, 1 appel moteur, retrait refusé. Retrait gagnant ⇒ retour en `arbitration`, montant et décision à null, **0** appel moteur, **0** ligne `Refund`, **1** ligne d'audit (retrait et audit = une transaction, S-30).
- **C (contrôle négatif)** — un rail SYNTHÉTIQUE sans compare-and-swap, dans le même harnais, les deux contendants relâchés sur le même instant absolu après leur lecture : il paie deux fois et le harnais le voit. Sans lui, A et B ne prouveraient rien.
Consigné dans `docs/ops/REFUND-FINANCIAL-CONTRACT.md` §26 avec la version du serveur, les compteurs et le résidu nommé (build Windows local, réglages d'installation neuve).

**Preuves ciblées** : `tests/claims-dprime-l5-pay-rail.test.ts` 48, `-pay-token.test.ts` 43, `-payable-core.test.ts` 14, `-pay-window-operator.test.ts` 33 (l'opérateur exécuté en VRAIS processus enfants dans un monde factice — racine temporaire, `.env.local` relu octet par octet après chaque run, aucune sauvegarde produite, **aucun run ne contacte staging**), `claims-pay-race.db.test.ts` 4. Suite complète **476 fichiers / 6 544 tests verts** ; `tsc` **39 = 39** (baseline `dab754d`, ensemble d'erreurs IDENTIQUE) ; `next lint` **0 erreur** ; `check:i18n` 5/5 (8 044 clés) ; `check:flags` OK ; **build à froid OK** ; `lib/refund.ts` byte-identique (blob `e2dc42b8`) ; `middleware.ts` inchangé.

**Revue adversariale L5** (5 lentilles × 3 réfutateurs par constat, 122 agents) : **39 constats bruts, 15 confirmés, 24 réfutés preuve à l'appui** — tous les 15 corrigés AVANT le commit, plus 7 constats réfutés que j'ai corrigés quand même parce qu'ils fermaient un vrai écart. Les trois qui comptent :

1. **P1 — « 0 € » affirmé sur un remboursement Stripe qui avait été CRÉÉ puis échoué.** `triggerClaimRefund` écrit le préfixe `engine_failed:` pour DEUX sous-cas : le moteur n'a rien créé, et le moteur a créé une ligne que Stripe a ensuite mise en échec. Sur une charge routée, ce second cas a **déjà débité le restaurant** et Stripe ne restaure pas le transfert inversé. Le rail classait les deux en `not_paid:engine_failed` et la console imprimait « rien n'a été envoyé (0 €) ». Corrigé : la troisième condition, `after.refundId === null` — le moteur ne lie une ligne que dans le second cas, donc seule l'absence de liaison établit que rien n'est parti. Le cas ligne-échouée part en `review:engine_own_row`, le seau qui demande une preuve.
2. **P1 — `moneyMoved` binaire dans l'audit.** Chaque issue « l'argent a peut-être bougé » (`review:*`, `crashed`, `accepted_pending`, `superseded`) était journalisée `moneyMoved:false`. Corrigé : `moneyMovedOf()` à **trois valeurs** — `true`, `false` (établi), `'unknown'` (NON établi) — épinglée issue par issue. Une trace qui écrit « false » là où la réponse est « je ne sais pas » invite exactement la conclusion que cette architecture existe pour empêcher.
3. **P1 — l'opérateur pouvait redémarrer Passenger au milieu d'un appel Stripe en imprimant qu'il ne l'avait pas fait.** Le contrôle « ne jamais couper une tentative en vol » et le drain étaient bornés à l'instantané pris AVANT l'ouverture ; or décider est 24/7, donc le rail paie aussi les réclamations arrivées pendant la fenêtre. Corrigé : les deux sont désormais des lectures **LIVE et non filtrées** (n'importe quelle ligne `pending`, n'importe quelle réclamation `refunding`), et la portée de la garantie T-42 est dite explicitement dans le rapport.

Les douze autres : la sonde de plafond acceptait un plafond dérivé de la base comme « lu » (T-59 appliqué au paiement : un plafond non confirmé par Stripe RETIENT désormais) ; la console affirmait « refusé avant toute lecture » pour **toute** réponse non-2xx, y compris un 504 arrivé après des paiements ; une réclamation **illisible** était rapportée comme « la décision a changé » (nouveau mot `skipped:claim_unreadable`) ; `superseded` était raconté comme un non-événement alors que le CAS perdu peut suivre un remboursement abouti ; les jetons de clause internes (`refund_error`, `not_found`…) s'affichaient en anglais dans les cinq locales ; l'opérateur ne croisait pas le `DATABASE_URL` ni la clé Stripe du SHELL avec ceux des fichiers (il le faisait pour `NEXTAUTH_URL`) ; son en-tête pouvait dire « rien n'a été écrit dans .env.local » après une écriture partielle ; une entrée malformée dans `CERTIFIED_SHAS` aurait certifié **tous** les builds ; les flags dangereux n'étaient lus que dans les fichiers ; S-14b n'était pas ré-appliqué au moment du paiement (le jeton signe désormais si le lot a été **nommé**) ; un échec d'écriture d'audit était silencieux (compté et rapporté) ; et deux tests de la répétition MariaDB seraient passés même si un seul contendant avait jamais joué (les deux camps doivent désormais avoir gagné).

**Correction d'un constat périmé du rapport L4.** Le §11 du rapport L4 mentionnait « deux sauvegardes `bak-modeb-gate` restent à neutraliser côté serveur ». C'est **faux et périmé** : T-43 est CLOS et PASS — aucune `.env.local.bak*` ne subsiste dans `~/app.grubano.com`, les deux anciennes sauvegardes Mode B ont été neutralisées et archivées hors racine applicative dans `~/.grubano/phase2-evidence` (700/600, empreintes vérifiées, copies à `CLAIMS_ENABLED=false` / `REFUNDS_ENABLED=false`, dry-run final PASS). Aucune mesure serveur postérieure à T-43 ne dit le contraire ; la ligne était un résidu documentaire. T-43 n'est pas rouvert, rien n'a été neutralisé, aucune commande cPanel n'a été demandée.

### L5 — déploiement staging vérifié (2026-09-23)
Commit `41d4fd7f9eb812048f16591ccee9e3db5de3aa56`, poussé sur `develop`. CI « Deploy to Staging » run 35883053887 : job `test` **success**, job `deploy` **success**. `version.json` servi par staging = `41d4fd7…` (build 15:46:15Z, ciRunId 35883053887) — le SHA déployé est EXACTEMENT celui de L5. Santé : `/fr/eat` 200, `/api/restaurants` 200.

Recensement read-only `claims-census.yml` run 35887946737, mesuré 2026-09-23T16:19:36Z :
- `schema` = `{ready:true, clientReady:true, dbReady:true, missingClient:[], missingDb:[], why:null}` — inchangé, aucune migration ni regen n'a été nécessaire.
- `gates` = `{claimsEnabled:false, claimsGate:"CLOSED (flag_off)", claimsSurfaceEnabled:false, claimsIntakeEnabled:false, claimsSurfaceOpen:false, claimsIntakeOpen:false, refundsEnabled:false}` — **toutes fermées**, les deux flags produit toujours OFF, le bail REFUNDS jamais ouvert.
- **Population réclamations INCHANGÉE** depuis le recensement L0 (`dab754d`) : total 9, `{refunded:4, refused:3, refused_final:2}`, `active:0`, `nonTerminal:3`, `approvedUnpaid:0`, `closure.terminalWithoutRecord:4`, **tous les compteurs d'anomalie héritée à 0**.

Sondes externes du rail déployé (non authentifiées) : `POST /api/admin/claims/pay-approved {"dryRun":true}` → **403 « Accès refusé »**, et `{"confirm":"PAYER","token":…}` → **403 « Accès refusé »** : `resolveAdmin()` répond AVANT toute porte, ce qui est le contrat (§8.1) — aucun chemin machine n'existe dans ce fichier. `POST /api/admin/refunds/run` 403 `{gated:true}` · `POST /api/claims` 403 `{gated:true}`.

**Aucune fenêtre ouverte, aucun flag modifié, aucune migration, aucun regen Prisma, aucun accès cPanel, aucune opération Stripe, aucun e-mail envoyé. L'opérateur `phase2-claims-pay-window.js` n'a jamais été exécuté sur le serveur.**

## L6 — réclamation sur commande LIVRÉE, ancre `deliveredAt`, fidélité D-15 (aucun argent déplacé)

**Périmètre livré** (spec v2 §7.1, LOYALTY-REFUND-CONTRACT §24, invariants S-16/S-17/S-28). Aucune ouverture REFUNDS, aucun pay-window, aucune écriture Stripe, `CLAIMS_SURFACE_ENABLED` et `CLAIMS_INTAKE_ENABLED` toujours OFF, `CERTIFIED_SHAS` intouché, rail L5 intouché.

*Une seule liste d'éligibilité.* Nouveau `lib/claim-eligibility.ts` — pur, sans Prisma, sans `process.env`, sans Stripe, sans horloge propre : E1 propriétaire → `not_owner` · E2 payée → `not_paid` · **E3 `status === 'delivered'` → `not_delivered`** · E4 `deliveredAt` non nul et ≤ 48 h · E5 `createdAt` ≤ **30 jours** · E8 une réclamation détient `activeOrderKey` → `active_claim`. `createClaim` ET `getClaimEligibility` posent désormais la MÊME question dans le MÊME ordre avec les MÊMES codes. Avant ce lot elles portaient chacune leur copie des règles : **sept divergences mesurées**, dont deux où le formulaire invitait une réclamation que le serveur refusait ensuite. Les deux sont fermées : E6 (plafond à 0 €) refuse maintenant dans les deux, et E8 lit le fait que le POST fait respecter — qui détient la clé `@unique` — au lieu de « la réclamation la plus récente a l'air active », qui disait oui sur une commande dont une réclamation PLUS ANCIENNE tenait encore la clé.

*L'ancre.* `Order.deliveredAt` est écrit **dans la même écriture que la transition**, une seule fois, et seulement s'il est encore nul. Jamais `updatedAt` : il bouge à chaque écriture ultérieure (une note, une réconciliation, une réparation), donc une fenêtre de 48 h mesurée dessus se réouvre silencieusement des jours après le repas — un client refusé lundi pouvait déposer jeudi parce que quelque chose sans rapport avait touché la ligne. Une commande sans ancre **n'est pas en libre-service** : il n'y a aucune façon honnête de dater sa fenêtre, et en inventer une serait exactement le défaut. **Aucun backfill**, aucun repli sur `createdAt`. La matrice de transitions interdit déjà `delivered → delivered` (un second PATCH répond 422 avant toute écriture), donc l'ancre est structurellement écrite une fois ; le garde `null` la protège même si cette matrice changeait un jour.

*Le plafond dur.* `CLAIM_MAX_ORDER_AGE_DAYS = 30` sur `createdAt`, au-dessus de la fenêtre : une horloge, un fuseau ou une ancre future écrite par un bug ne peuvent pas ouvrir une réclamation sur une commande d'il y a un an.

*D-15 — le défaut que ce lot ferme.* Les points sont crédités une fois, à `delivered`. Un remboursement peut arriver AVANT : le client est remboursé lundi, la commande est marquée livrée mardi. Au moment du remboursement le webhook réconcilie, ne trouve aucune ligne `earn` et n'inverse donc rien — correctement ; puis la transition créditait la totalité des points et **aucun événement ultérieur ne revenait les reprendre**. Un client remboursé intégralement gardait tous les points d'un repas qu'il n'avait pas payé (résidu §23, mesuré contre `dab754d`).

Désormais : l'ancienne règle « une ligne `refund` ou `earn_reversal` annule l'earn » est remplacée. Seuls deux faits sautent le crédit — une ligne `earn` (déjà crédité) et le **marqueur LEGACY** (une ligne `refund` sans `sourceEventId`, d'un ordre réconcilié par du code antérieur au modèle à clé : *grandfathered*, laissé exactement tel quel). Sinon le gain nominal est crédité, puis **la même réconciliation que le webhook** est REJOUÉE — une seule règle d'arrondi (§9, la cible cumulative), une seule clé d'idempotence (le `re_` de Stripe), un seul chemin de code — sur les remboursements que la **base** prouve déjà.

*La source, et pourquoi jamais Stripe.* Nouveau `lib/loyalty-prorata.ts` : l'union, dédupliquée par `re_`, des lignes `Refund` `succeeded` portant un `re_` et des lignes `LedgerEntry` `type:'refund'` du `PaymentIntent` de la commande (montant = `-grossAmount`, une ligne de remboursement est écrite négative). Ni `charge.amount_refunded` (un cumul, qui double-compterait) ni aucun appel Stripe : **cette route tourne quand un livreur appuie sur un bouton** et ne doit pas dépendre d'un prestataire de paiement joignable. `T` = la ligne ledger `payment` du PI, sinon `round(order.total × 100)` — et le repli est **nommé** (`chargeSource`) plutôt que présenté comme une mesure. Quand le même `re_` figure dans les deux sources, **le ledger gagne** : son instant vient du `refund.created` de Stripe, tandis que notre `settledAt` est le moment où NOUS l'avons remarqué.

*L'ordre d'exécution (§24 (5)).* Le rejeu tourne sur le client RACINE, **après** que la transaction d'earn a commité — `reconcileLoyaltyOnRefund` ouvre les siennes, et une livraison qui a eu lieu n'est jamais annulée parce qu'une réconciliation de points a échoué. Une tentative, un retry ; en cas d'échec : `[LOYALTY MISS] earn_prorata_incomplete` dans le journal **et** une alerte `loyalty_prorata_incomplete` (dédupe `loyalty:<orderId>:prorata`) qui nomme la commande et la commande de réparation. Un ensemble déjà appliqué n'écrit rien (le `(re_, type)` unique rend chaque effet idempotent) ; une commande sans remboursement connu reste entière, ce qui est la bonne réponse. **L'ordre earn-puis-rejeu est épinglé par un test de source** : inversé, le rejeu ne trouverait rien à reprendre et plus rien ne reviendrait jamais — le défaut D-15 réintroduit par une simple permutation.

*La réparation.* Nouvelle route `POST /api/admin/loyalty/reconcile` — `resolveAdmin()`, base seulement, **aucun appel Stripe**, **gatée par aucun flag réclamations ou remboursements** : des points faux ne sont pas une fonctionnalité qu'on active, et refuser de les corriger parce qu'un flag bêta est fermé laisserait une erreur connue en place. Elle rejoue exactement le même plan, est idempotente, et dit ce qu'elle a fait (`applied`, `skipped`, `earnReversed`, `spentRestored`, `offsetAdded`, `grandfathered`, `chargeSource`, `drift`) plutôt que « ok ».

*La copie client.* Deux nouveaux refus rendus dans la langue du client, sous `eat.help.*` (jamais sous `claims.status.*`, un ensemble de clés gelé, et jamais en réutilisant `claims.reason.not_delivered`, qui est un MOTIF de réclamation, pas un refus) : `claimNotDelivered` et `claimNoRefundableAmount`, × 5 locales. Et les refus de `createClaim` portent désormais leur **code** à côté de la phrase, code que la route `POST /api/claims` **transmet** — donc la page les affiche traduits au lieu de recopier du français au client.

**Revue adversariale L6** (5 lentilles, 119 agents, 3 réfutateurs par constat) : **38 constats bruts, 20 confirmés** — tous corrigés AVANT le commit. Les cinq qui comptent :

1. **P1 — l'ancre silencieusement jamais écrite sur un client Prisma périmé.** Le garde était `order.deliveredAt === null`. Quand le client Prisma généré ne connaît pas encore la colonne, le champ vaut `undefined`, la comparaison stricte est **fausse**, et l'ancre n'était donc **jamais** écrite — définitivement, puisqu'il n'y a pas de backfill. C'est précisément le scénario que ce dépôt a déjà vécu (FTP excluant `node_modules` ⇒ client périmé invisible aux healthchecks 200). Corrigé : `== null` pour l'intention, `clientSchemaReady().ready` pour l'utilisabilité, et un `[CLAIM ANCHOR MISS]` bruyant nommant les colonnes manquantes quand le client est périmé. Une livraison n'est jamais bloquée ; une ancre perdue n'est jamais muette.
2. **P1 (4 lentilles) — « Rien n'a été écrit » alors qu'une partie du plan l'était.** `reconcileLoyaltyOnRefund` commite **une transaction par effet**. Un échec sur le troisième remboursement de quatre laisse les deux premiers **appliqués**. La route de réparation répondait « Rien n'a été écrit par cette tentative » et journalisait `replayed:false` dans `AdminAuditLog` — un administrateur qui le croit corrige un solde à la main **par-dessus** des lignes qui existent déjà. Corrigé : `replayLoyaltyProrata` **mesure** (compte avant / compte après) et retourne `applied: {earnReversal, refund} | null`, l'alerte porte `appliedRows`, la route dit « interrompue APRÈS avoir écrit … rappelez cette route » et l'audit écrit `replayed:'partial'`. `null` quand le comptage lui-même a échoué : on ne remplace pas une fausse certitude par une autre.
3. **P1 (3 lentilles) — « tout a déjà été remboursé » alors que l'argent est en vol.** Le plafond soustrait aussi un remboursement **PENDING** (`amount_refunded` de Stripe le compte déjà). Un client dont le remboursement était en cours de traitement lisait qu'il était terminé. Corrigé × 5 locales : « son total est déjà couvert par un remboursement **effectué ou en cours de traitement** », et la phrase serveur de `CLAIM_REFUSAL_TEXT` dit la même chose.
4. **P1 (territoire SPEC) — le résidu de préfixe de §24, désormais DÉTECTÉ.** Voir « Résidus nommés » ci-dessous : aucune règle changée, mais l'écart est mesuré, journalisé et alerté au lieu d'être découvert dans le solde d'un client.
5. **P2 — le test de propriété conduisait `delivered` avec un rejeu CASSÉ et passait quand même.** `tests/order-status-ownership-route.test.ts` n'avait ni `refund`, ni `ledgerEntry`, ni `loyaltyTransaction.findMany` dans son double Prisma : chaque appel du rejeu levait une `TypeError` que la ceinture de la route avalait. Le fichier serait resté vert si le rejeu avait été **supprimé**. Corrigé : les trois délégués, `@/lib/admin-alerts` mocké (rien ne peut plus atteindre un expéditeur réel), et trois assertions — le rejeu a bien lu les remboursements de la commande, il n'a alerté personne, et l'ancre est écrite dans la **même** écriture que la transition.

Les quinze autres, en une ligne chacun : le commentaire d'en-tête de `lib/loyalty-prorata.ts` affirmait « Same set, same order, same deltas » — **faux** dès que les deux chemins voient des ensembles différents, remplacé par le résidu nommé ; la doc de `claimWindowHours()` décrivait encore la fenêtre comme ancrée sur `Order.updatedAt` (et mon contrôle négatif déshabille les commentaires, donc il ne pouvait pas l'attraper) ; le refus E6 du POST ne portait pas de code et sortait en 400 alors qu'un plafond nul est l'**état** de la commande (409) ; la route `POST /api/claims` laissait tomber le code au passage de la frontière ; `active_claim` pouvait être rendu à côté d'une `existingClaim` **close** sans dire laquelle bloque (`blockingClaimId`) ; le champ d'échec de la route s'appelait `applied` comme le compteur du 200 (renommé `appliedRows` : deux formes sous un nom, c'est un client qui lit un objet pour un nombre) ; l'ordre earn-puis-rejeu n'était épinglé par aucun test ; le double de la répétition ne modélisait pas `loyaltyTransaction.findMany` ; et sept constats d'injection de faute dans les tests eux-mêmes — dont celui-ci, qui compte : mon premier harnais patchait `db.loyaltyTransaction.create` **par l'extérieur**, alors que `$transaction` passe au callback le client du monde ; l'injection n'atteignait donc **aucune** écriture transactionnelle et le test « passait » en n'injectant rien. L'injecteur vit maintenant dans le monde (`onLoyaltyCreate`), avant l'index unique.

**Preuves ciblées.** `tests/claims-dprime-l6-eligibility.test.ts` **35** (les règles pures ; l'écrivain de l'ancre conduit de VRAIS PATCH ; des épingles de source ; et un bloc de **PARITÉ** qui fait passer LES DEUX fonctions sur la MÊME commande, cas par cas, avec deux contrôles négatifs — une commande livrée, payée, récente : le formulaire dit oui ET le POST crée la réclamation) · `tests/claims-dprime-l6-loyalty-d15.test.ts` **40** (tous les nombres §24 calculés par le produit sur `tests/support/loyalty-world.ts`, un monde en mémoire qui modélise l'index `@@unique(sourceEventId, type)` et une transaction ATOMIQUE — un P2002 sur la ligne à clé ne doit laisser aucun changement de solde ; plus les trois cas d'échec partiel et les six cas de dérive) · `tests/order-status-ownership-route.test.ts` **9** (7 avant : le double Prisma conduisait `delivered` avec un rejeu cassé) · et les épingles ajoutées aux fichiers héritées (`claims.test.ts`, `claims-eligibility-stripe-truth.test.ts`, `t59-claim-ceiling-provenance.test.ts`, `claims-route-auth.test.ts`, `claims-routes.test.ts`, `claims-dprime-l1-flags.test.ts`, `claims-dprime-l3b-schema-ready.test.ts`, `loyalty-auto-account.test.ts`, `p1-reference-click-collect.test.ts`, `p7-livraison-terminale.test.ts`, `p10-claims-activation.test.ts`).

Suite complète **478 fichiers / 6 636 tests verts** (0 échec, 14 skipped, 17 todo ; L5 : 476 / 6 544) ; `tsc` **39 = 39** — ensemble d'erreurs IDENTIQUE à la baseline `dab754d` (`docs/ops/TSC-BASELINE-dab754d.txt`), comparé en ignorant les numéros de ligne ; `next lint` **0 erreur** ; `check:i18n` 5/5 (8 046 clés) ; `check:flags` OK ; **build à froid OK** ; `lib/refund.ts` byte-identique (blob `e2dc42b8`) ; `middleware.ts` inchangé ; **rail L5 intouché** (`lib/claims-pay-rail.ts`, `lib/claims-pay-token.ts`, `lib/claims-payable-core.js`, `app/api/admin/claims/pay-approved/route.ts`, `scripts/server/phase2-claims-pay-window.js` : aucun diff). `prisma/schema.prisma` n'est modifié qu'en **commentaires** (deux passages disaient encore que la fenêtre s'ancre sur `updatedAt` et que rien ne lit `deliveredAt`) — **aucun champ, aucun attribut, aucun index, donc aucune migration**.

**Et ce que le CRITIQUE DE COMPLÉTUDE a trouvé, sur le code COMMITÉ, que les dix lentilles avaient manqué.** Les dix lentilles ont dépensé leur effort à débattre de l'EXISTENCE des deux gardes (le plancher haut, `proofComplete`) ; aucune n'a demandé ce qu'ils FONT une fois qu'on les accorde. Les trois meilleurs constats du critique sont exactement dans cet angle mort, et les deux P1 sont corrigés avant clôture :

*P1 — un ensemble SUCCEEDED VIDE renversait tout, en se réclamant d'une preuve complète.* Le bail `refunds.length === 0` du webhook est à l'INTÉRIEUR de la branche `if (listFailed)` et AVANT le filtre `status === 'succeeded'`. Or `charge.refunded` part à la CRÉATION du remboursement (§9.4) : un appel de liste qui RÉUSSIT peut donc rendre zéro remboursement *succeeded*. Sur une commande à lignes héritées (plancher haut = 0, puisque les clés `re_` n'en fournissent aucun), ce seul événement rendait **la totalité de la reprise** au client et le rapportait comme un succès propre. Corrigé aux deux bouts : le webhook ne revendique plus la complétude sur un ensemble vide (`!listFailed && refunds.length > 0`) **et**, surtout, la règle est posée dans le réconciliateur — **un cumul prouvé NUL ne peut jamais rien diminuer**, quoi qu'il prétende sur sa complétude, parce que « je ne vois aucun remboursement sur une commande manifestement réconciliée » est la définition d'une preuve sur laquelle on n'agit pas.

*P1 — le plancher haut DOMINAIT `proofComplete`, et son entrée en jeu était invisible.* Le plancher est évalué avant la porte de complétude, donc un vrai rétrécissement devenait `delta === 0` et repartait en `converged: true`, `heldGiveBack: 0`, sans un mot : la sortie « retenu et rapporté » que le contrat promet n'était jamais atteinte sur une commande déjà keyée L6.1. Le plancher TIENT toujours (c'est son rôle), mais il est désormais **nommé** : `cumFlooredFromCents` dit ce que l'appelant avait prouvé, et une alerte dédiée `loyalty_proof_set_shrank` sort — un remboursement que Stripe ou la base prouvait et ne prouve plus est un fait dont un humain a besoin, pas un silence.

*P2 — le détecteur T-44 était D1-seulement, donc muet sur l'ordonnancement qui donne son nom à D-15.* Les deux affectations de `offsetTouchedT44` étaient dans la branche `sign === -1`. Or le cas CLASSIQUE de D-15 est un remboursement AVANT la livraison : la base est 0, rien ne bouge côté D1, et **seul** le côté D2 tourne. Corrigé des deux côtés — et le seuil passe de `offset > 0` à `offset !== 0`, parce qu'un offset NÉGATIF signifie que le registre de dette est corrompu (rien ne le plancherise, et la route de remise le décrémente sans verrou) et qu'un détecteur qui lit un registre corrompu comme « pas de dette » se taira précisément quand il faut parler.

*P3 — une fausse alerte MONEY REVIEW après un rollback.* `earnRepaidOffset` était affecté À L'INTÉRIEUR de la transaction d'earn ; un échec d'écriture (avalé comme non fatal) laissait le chiffre en place et l'alerte §24 (8) annonçait un remboursement de dette qui avait été défait. Publié désormais seulement APRÈS la résolution de la transaction. Et une écriture qui ne fait pas bouger le chiffre appliqué n'est plus répétée : deux passes qui mesurent le même appliqué après une écriture signifient que la ligne n'est pas relisible, et rejouer le même delta déplacerait le solde une fois par tentative pour une seule ligne revendiquée.

*Et trois défauts de TESTS, qui comptent autant.* (a) Le test « BASE PÉRIMÉE » — la parade du constat le plus grave de tout le panel — était **vide** : le monde portait `earnTx: true` de bout en bout, donc une base lue hors transaction l'aurait passé aussi. Réécrit pour que la ligne `earn` apparaisse PENDANT l'appel, plus une épingle de source qui exige que la base soit lue via `tx.` après le verrou. (b) Les deux faux Prisma ignoraient l'identifiant LIÉ du `SELECT … FOR UPDATE` : un verrou pointé sur la mauvaise ligne aurait rendu les chiffres du bon client et tous les tests seraient restés verts. (c) Le cas H du fondateur s'intitulait « un pending qui devient succeeded » sans qu'aucun remboursement `pending` n'apparaisse dans le test ; il est désormais asserté sur une vraie ligne `status:'pending'` à travers `buildDbKnownRefundSet`, la fonction qui décide ce que « prouvé » veut dire. Et le cas I ne bannissait qu'un second écrivain de `earn_reversal` : il bannit maintenant aussi celui du `refund` (D2).

**Résidus nommés — décision fondateur requise, RIEN n'a été changé dans la spec.**

1. **Le résidu de préfixe §9 / §24 (P1).** §9 est sans dérive pour un écrivain qui tient **tout** l'ensemble des remboursements en une passe : c'est pourquoi le webhook relit la liste de Stripe et répond 503 plutôt que d'avancer sur un ensemble partiel. L'ensemble de ce rejeu est « ce que la base prouve », strictement plus faible. Et comme chaque ligne `(re_, type)` écrite **fige** le delta qu'elle a calculé (rien ne recalcule jamais une ligne à clé), un ensemble qui grandit dans le mauvais ORDRE peut se stabiliser à un point de la cible §9 — définitivement, dans les deux sens. Démontré par le produit, pas décrit : T = 1410, E = 14, deux remboursements de 470 ; le plus TARDIF est visible seul d'abord (delta −5, prix calculé sur un cumul nul), le plus ANCIEN arrive ensuite (delta −5 aussi, car il est maintenant le premier du préfixe) ⇒ **booked 10 là où §9 veut 9**. Le client est un point trop bas, pour toujours. Fermer cela demande de toucher §9 ou §16 (un effet « ajustement à la cible », ou une précondition de complétude de préfixe explicite) — donc une décision fondateur, hors périmètre L6. Ce que L6 fait : après **chaque** rejeu, le total inscrit est comparé à la cible §9 de l'ensemble visible ; un écart produit `[LOYALTY MISS] earn_prorata_drift`, une alerte dédiée (`loyalty:<orderId>:drift`, distincte de celle d'échec) et un champ `drift` dans la réponse et l'audit de la route de réparation. Rien n'est réécrit : réécrire une ligne à clé casserait le modèle un-effet-par-remboursement sur lequel repose toute l'idempotence. Quatre tests, dont **deux contrôles négatifs** (les trois remboursements vus dans l'ordre, et un remboursement arrivant après un rejeu, ne produisent **aucun** écart et **aucune** alerte).
2. **La composition D3 / D-15 (P2).** Quand les points gagnés ont déjà été dépensés ailleurs, §9 borne la reprise au solde visible et loge le reste dans `recoveryOffsetPoints` (une dette remboursée par les gains FUTURS, jamais par le solde). Dans le cas D-15 le gain est crédité **puis** reprisé dans la même seconde : la reprise se règle donc sur le solde que le client vient de recevoir, et non sur une dette portée. C'est cohérent avec §9 telle qu'elle est écrite, et c'est le comportement que les tests épinglent — mais la question « un gain annulé doit-il pouvoir éteindre une dette d'offset antérieure ? » n'est pas tranchée par le contrat. Signalé, non modifié.

**Précision de terminologie sur L5, consignée à la demande du fondateur.** Le « READY FOR FINANCIAL REHEARSAL = YES » du rapport L5 doit être lu comme **« le rail L5 est techniquement prêt pour une répétition future »**, et **NON** comme « le flux D′ complet est prêt pour la répétition ». La répétition D′ complète reste **après L6 → L10** conformément au plan validé, et n'aura lieu que sur une autorisation fondateur séparée.

### L6 — déploiement staging vérifié (2026-09-25)
Deux commits : `2970edcbb81ce4eb25903706015b41dd7fbd890d` (le lot) puis `d82aeb1da61f01fd94f5a0122263a34ae0703789` (deux suivis de la même revue : la moitié CLIENT de `blockingClaimId` — la page préférait décrire une réclamation CLOSE à côté d'un refus qui dit « une réclamation est en cours » — et le comptage avant/après rendu PARESSEUX, donc hors du chemin chaud : une livraison sans remboursement coûte exactement ce qu'elle coûtait avant que cette mesure existe). Les deux poussés sur `develop`.

CI « Deploy to Staging » run **36119130469** (`2970edcb`) : job `test` **success**, job `deploy` **success** ; `version.json` servi = `2970edcb…` (build 09:39:54Z). CI run **36121588787** (`d82aeb1d`) : `test` **success**, `deploy` **success** ; `version.json` servi = **`d82aeb1da61f01fd94f5a0122263a34ae0703789`** (build 10:06:11Z, ciRunId 36121588787) — le SHA déployé est EXACTEMENT celui de la tête de `develop`. Santé : `/fr/eat` **200**, `/api/restaurants` **200** (la DB répond, donc le client Prisma déployé n'est pas périmé).

Recensement read-only `claims-census.yml` run **36123610349**, mesuré 2026-09-25T10:22:26Z sur le build déployé :
- `schema` = `{ready:true, clientReady:true, dbReady:true, missingClient:[], missingDb:[], why:null}` — **`Order.deliveredAt` est lisible par le client généré ET par la base** du build déployé. C'est la mesure qui compte pour ce lot : si le client était périmé, l'ancre ne serait jamais écrite, et L6 a précisément rendu ce cas BRUYANT (`[CLAIM ANCHOR MISS]`) au lieu de silencieux. Aucune migration, aucun regen n'a été nécessaire (la colonne existe depuis L3b, appliquée sur staging le 2026-09-22).
- `gates` = `{claimsEnabled:false, claimsGate:"CLOSED (flag_off)", claimsSurfaceEnabled:false, claimsIntakeEnabled:false, claimsSurfaceOpen:false, claimsIntakeOpen:false, refundsEnabled:false}` — **toutes fermées**, les deux flags produit toujours OFF, le bail REFUNDS jamais ouvert.
- **Population réclamations INCHANGÉE** depuis le recensement L0 (`dab754d`) et L5 : total **9**, `{refunded:4, refused:3, refused_final:2}`, `active:0`, `nonTerminal:3`, `approvedUnpaid:0`, `closure.terminalWithoutRecord:4`, **tous les compteurs d'anomalie héritée à 0** (dont `voidedRefundRows:0` et `pendingRowsOver20hWithSettledRoyalty:0`, les deux qui regardent les lignes `Refund`).

Sondes externes non authentifiées du build déployé : `POST /api/claims` → **403 `{gated:true}`** (la surface produit est fermée) · `POST /api/admin/loyalty/reconcile` → **403 « Accès refusé »** — c'est le contrat de cette route : `resolveAdmin()` répond AVANT tout, et elle n'est gatée par AUCUN flag réclamations, parce que des points faux ne sont pas une fonctionnalité qu'on active · `POST /api/admin/claims/pay-approved {"dryRun":true}` → **403 « Accès refusé »** (rail L5 inchangé) · `POST /api/admin/refunds/run` → **403 `{gated:true}`**.

**Suivi `0a364b3b2ac0c6d7b36193d81ef6dfab66057e10`** (les deux P1 du critique de complétude). CI run **36172619713** : `test` **success**, `deploy` **success** — « Verify deployed build », « Health check (staging @ expected SHA) » et « Client bundle integrity » les trois **success**. `version.json` servi = **`0a364b3b2ac0c6d7b36193d81ef6dfab66057e10`** (build 18:26:58Z) — le SHA déployé est la tête de `develop`. Santé : `/fr/eat` 200, `/api/restaurants` 200. Recensement run **36174688255**, mesuré 18:38:25Z : `schema.ready` **true** (clientReady, dbReady, missing []), gates **toutes fermées**, population réclamations **INCHANGÉE** (total 9, `{refunded:4, refused:3, refused_final:2}`, active 0, nonTerminal 3, approvedUnpaid 0, `voidedRefundRows` 0). Sondes non authentifiées : `/api/claims` 403 `gated`, `/api/admin/loyalty/reconcile` 403 « Accès refusé », `/api/admin/claims/pay-approved` 403, `/api/admin/refunds/run` 403 `gated`.

Portée de ce qui est mesuré, dite explicitement : le recensement compte les **réclamations** (et les anomalies qui touchent les lignes `Refund`), pas les totaux bruts de `Refund` / `LoyaltyTransaction` / `LedgerEntry` — aucun compteur read-only ne les expose. Ce qui est établi : aucun appel **authentifié** n'a été fait contre staging dans ce lot, aucune commande n'a été créée ni transitionnée, aucune réclamation n'a été déposée, et les quatre sondes ci-dessus se sont arrêtées au garde AVANT toute logique. **Aucune fenêtre ouverte, aucun flag modifié, aucune migration, aucun regen Prisma, aucun accès cPanel, aucune opération Stripe, aucun e-mail envoyé, aucune ligne écrite.** `scripts/server/phase2-claims-pay-window.js` n'a jamais été exécuté sur le serveur.

## L6.1 — convergence vers la cible cumulative (arbitrage fondateur option (a), aucun argent déplacé)

**Ce que le fondateur a tranché.** Le résidu §24 (7) de L6 — un remboursement PLUS ANCIEN qui devient prouvable APRÈS un plus récent laissait le total à un point de la cible §9, définitivement — est fermé par l'**option (a) : ajustement à la cible cumulative**. Refusés explicitement : l'écart permanent d'un point, toute dépendance à l'ordre d'apparition, et une précondition « préfixe complet » qui rendrait la correction impossible au moment même où l'on découvre un événement plus ancien. Le résidu §24 (8) — composition de D-15 avec la dette `recoveryOffsetPoints` — est **DIFFÉRÉ À T-44 PRE-LIVE** : comportement conservé, détection durcie, alerte MONEY REVIEW, aucune perte silencieuse, aucune réécriture automatique du contrat de dette.

**Le modèle, en une phrase.** La réconciliation ne somme plus des deltas par événement : à chaque passe elle réduit l'ensemble prouvé à UN nombre (Σ des centimes remboursés, **dédupliqué par `re_`**), calcule la cible §9 pour ce nombre, **lit l'effet RÉELLEMENT appliqué** dans le registre, et écrit **uniquement la différence** — une nouvelle ligne, jamais une réécriture.

*Pourquoi cela supprime la dépendance à l'ordre.* La cible dépend de la SOMME, pas de la séquence. Il n'y a plus de préfixe à trier, donc plus de préfixe à se tromper. La séquence du fondateur, exécutée par le produit : 470 prouvé seul ⇒ cible 5, on écrit −5 ; le 470 PLUS ANCIEN arrive ⇒ cible `round(14 × 940 / 1410)` = 9, appliqué 5, on écrit **−4** ⇒ total **9, pas 10** ; un troisième 470 ⇒ cible 14, appliqué 9, on écrit −5 ⇒ total 14. Et les trois prouvés d'un coup ⇒ **une seule** ligne de −14 : ce que le contrat fixe est le TOTAL, pas le nombre de lignes.

*Pourquoi un rejeu n'écrit rien.* Ce n'est plus une clé qui arrête le rejeu, c'est l'**arithmétique** : à la cible la différence est 0, donc il n'y a rien à écrire et rien à heurter. Dix rejeux consécutifs du même ensemble : 0 ligne, 0 centime de solde, 0 alerte (test D).

*L'idempotence, en trois couches indépendantes.* (1) le **garde de différence nulle** ci-dessus ; (2) le **verrou de la ligne client** — `SELECT pointsBalance, recoveryOffsetPoints … FOR UPDATE` pris **AVANT** la lecture de l'effet appliqué, de sorte que deux réconciliations du même client se sérialisent et que la seconde voit l'écriture de la première au lieu de courir contre elle ; (3) `@@unique([sourceEventId, type])` sur une clé dérivée de **l'état observé** — `prorata:v1:<orderId>:<seq>:<appliqué>:<cible>` — où `seq` (le nombre de lignes déjà présentes de ce côté pour cette commande) rend la clé **monotone** : une transition légitime ultérieure ne peut jamais être prise pour le rejeu d'une précédente, même si l'ensemble prouvé oscillait. Sur collision, l'écrivain **relit et re-converge** (borné par `CONVERGENCE_ATTEMPTS`) au lieu de rendre la main en sous-appliquant.

*La clé ne contient plus de `re_`, et c'est le fond du sujet.* Sous convergence, l'unité de travail est la CIBLE, pas l'événement : une clé qui nomme UN remboursement ne peut pas exprimer un total qu'un événement plus ancien découvert plus tard doit déplacer — c'est précisément ce qui gelait le delta. Les lignes écrites par le code d'avant L6.1 **gardent** leurs clés `re_`, comptent normalement dans l'effet appliqué, et ne peuvent jamais entrer en collision avec une clé nouvelle.

*Les corrections négatives, et l'inverse exact.* Une différence peut être NÉGATIVE — c'est le cas des commandes que l'ancien code a laissées sur-appliquées (10 au lieu de 9), et c'est réel en base. La reprise passe alors par `applyGiveBackAgainstOffset(rendu, dette)`, qui **retire d'abord la dette** (`min(rendu, dette)`) et ne crédite que le reste au solde dépensable. C'est l'**inverse arithmétique** de `applyReversalWithOffset` — prouvé par un test d'aller-retour : reprendre puis rendre le même montant ramène à l'état de départ — et non une réinterprétation de la règle D3, qui reste intacte (seuls les gains FUTURS remboursent une dette réellement due). Créditer le solde sans défaire la dette laisserait le client tenir les points ET les devoir encore. Une ligne `earn_reversal` peut donc porter des points POSITIFS ; l'effet appliqué est toujours la somme SIGNÉE, jamais une somme de valeurs absolues.

*T-44.* Chaque reprise qui touche une dette déclenche `loyalty_offset_t44_review` (commande, montants, `moneyMoved:false`). **Statut à citer : cible cumulative D-15 = CERTIFIÉE pour le chemin normal ; D-15 × `recoveryOffsetPoints` = DIFFÉRÉE À T-44 PRE-LIVE.** Le différé ne bloque ni L7 ni la bêta, puisque le cas est détecté et visible.

*Le détecteur de dérive de L6 reste — et change de sens.* Il n'est plus le rapport d'un écart accepté mais un **VÉRIFICATEUR INDÉPENDANT** de l'écrivain : il relit les lignes, recalcule §9 et compare, sans faire confiance au résultat retourné. Après L6.1, un écart n'est pas un résidu, c'est un **défaut** (une ligne écrite hors réconciliation, une cible non atteinte) et il est alerté comme tel. Un défaut réel y a été trouvé pendant ce lot : le détecteur sommait `Math.abs(points)`, donc il comptait une ligne de reprise (+1) comme un point de PLUS reprisé et signalait un écart sur exactement les commandes qu'il venait de corriger. Corrigé en somme signée, et épinglé par un contrôle négatif qui prouve que le vérificateur sait encore se déclencher (une ligne posée à la main hors réconciliation est bien détectée).

**Revue adversariale L6.1, en deux temps.** D'abord un **panel de conception** : une cartographie exhaustive de tout ce qui lit ou écrit une ligne `LoyaltyTransaction` dans le dépôt (routes, lib, scripts serveur, tests), quatre conceptions indépendantes de la convergence, puis trois juges adversariaux (convergence mathématique / concurrence et idempotence / sûreté de l'argent) chargés de CONSTRUIRE un scénario qui casse chacune. Ensuite une **revue à dix lentilles** sur le code réellement écrit — exactement les dix points du fondateur — chaque constat soumis à trois réfuteurs sous des angles distincts. Les défauts ci-dessous ont tous été trouvés et corrigés **AVANT le commit**.

*Ce que la cartographie a appris, et qui change la façon de mesurer.* `LoyaltyCustomer.pointsBalance` n'est **PAS** la somme des lignes `LoyaltyTransaction` et ne l'a jamais été : `/api/loyalty/validate` écrit le solde en ABSOLU sans aucune ligne de registre, et `/api/loyalty/register` crée un client à 10 points sans ligne non plus — sans parler d'une reprise plafonnée qui loge le reste dans la dette. « L'effet réellement appliqué » doit donc être mesuré sur les LIGNES, jamais sur le solde ni sur une variation de solde. C'est ce que fait le code, et un test de source l'interdit désormais autrement. La cartographie a aussi nommé un piège : une ligne de type `refund` avec `sourceEventId` NUL est le marqueur *grandfather* d'avant Phase 1 ; si la réconciliation en écrivait une, elle installerait ce marqueur et gèlerait la commande hors convergence pour toujours (et tuerait son crédit d'earn). Chaque ligne écrite ici porte la clé dérivée, et un test l'épingle.

*Défaut 1 — P0 potentiel : BASE PÉRIMÉE, 9 POINTS RENDUS.* La base (l'existence de la ligne `earn`) était lue **hors** transaction tandis que l'effet appliqué était mesuré **dedans**. Scénario construit par le panel, et ce n'est pas exotique : une redélivrance de `charge.refunded` commence et ne voit pas encore la ligne `earn` (base 0 ⇒ cible 0) ; le livreur appuie sur « livré », l'earn est crédité et le rejeu converge à 9 ; la redélivrance verrouille enfin, mesure 9 appliqués contre sa cible périmée de 0, et **rend 9 points sur une commande remboursée aux deux tiers** — puis bat indéfiniment. Corrigé : la base, l'effet appliqué, le plafond et la cible viennent tous du **même instantané sous le verrou**. Et la base est désormais la ligne `earn` elle-même, non `Order.pointsEarned` : seul ce qui a été CRÉDITÉ peut être reprisé, donc une édition ultérieure de la colonne ne peut pas reprendre plus que ce que le client a reçu.

*Défaut 2 — P1 : ENSEMBLE DE PREUVE QUI RÉTRÉCIT.* Si Stripe cesse de rapporter un remboursement comme `succeeded` (ou qu'une ligne est annulée), la somme prouvée DIMINUE, et une convergence signée rendait alors des points sur une reprise correctement appliquée — silencieusement. Défaire une reprise établie n'est pas une décision que cet écrivain peut prendre seul. Corrigé : le cumul utilisé est `max(somme prouvée, plafond haut de la commande)`, ce plafond étant relu dans les clés que ce module a lui-même écrites. Le champ `cumEffectiveCents` dit lequel a servi.

*Défaut 3 — P1 : OSCILLATION SUR UN CENTIME DE DÉNOMINATEUR.* Ma première clé portait un numéro de séquence (`…:<seq>:<appliqué>:<cible>`). Le panel a construit le battement : le webhook passe `T = charge.amount`, le rejeu D-15 se rabat sur `round(order.total × 100)` faute de ligne ledger `payment`, les deux arrondis tombent de part et d'autre (8×470/1504 ⇒ 3 ; 8×470/1505 ⇒ 2) — et comme `seq` donne une clé NEUVE à chaque répétition, le solde du client basculait d'un point à **chaque** événement, une ligne de registre à chaque passe, pour toujours. Corrigé en changeant la nature de la clé : `prorata:v1:<orderId>:<cumEff>`. **La clé EST l'état de preuve** — un seul ajustement peut exister par (commande, côté, cumul). Une seconde passe qui recalcule une cible différente pour la MÊME preuve heurte la clé, **refuse** et le dit (`blocked`, alerte) au lieu de battre. Et une passe qui n'écrit rien ne consomme pas la clé, donc la commande D-15 (base 0 avant livraison, 14 après) obtient bien son ajustement au même cumul.

*Défaut 4 — P1 : « inverse » revendiqué sans test, et faux sur une dette d'autrui.* Le code et le contrat qualifiaient `applyGiveBackAgainstOffset` d'inverse arithmétique « épinglé par un test d'aller-retour » — qui n'existait pas. Il existe maintenant (aller-retour sur toutes les formes : aucun débordement, débordement total, débordement partiel), **avec le contrôle négatif** qui montre que la simplification tentante (« rendre au solde et laisser la dette ») ne revient PAS à l'état de départ : le client tiendrait les points et les devrait encore. Et sa **précondition est désormais écrite** : c'est l'inverse de CETTE reprise seulement si la dette qu'on lui passe est celle que cette reprise a créée. `recoveryOffsetPoints` est un pot au niveau CLIENT : rendre depuis ce pot pouvait effacer une dette d'une AUTRE commande, réellement due. La libération est donc bornée par la reprise propre de la commande (`min(rendu, dette, appliqué)`) ; l'attribution complète exigerait de stocker le débordement ligne par ligne — élément T-44, signalé, jamais supposé résolu.

*Défaut 5 — P1 : UNE REPRISE SUR UNE PREUVE INCOMPLÈTE.* Le cas, construit par la revue : une commande correctement reprisée par l'ancien code pour DEUX remboursements, réconciliée à nouveau depuis un ensemble qui n'en prouve qu'UN (l'autre est un remboursement Dashboard sans ligne chez nous et sans ligne ledger encore). La différence est négative, et l'honorer rendait au client des points justement repris. Le plancher haut ne couvrait pas ce cas : les lignes héritées portent des clés `re_`, donc elles ne fournissent aucun plancher. Corrigé par un principe simple : **augmenter l'effet ne demande aucune preuve particulière** (un ensemble trop petit demande simplement moins) ; **le diminuer en demande une complète**. `proofComplete` vaut **false par défaut** — le rejeu D-15 et la réparation admin dérivent leur ensemble de la BASE (§24 (3) : la route statut ne doit jamais lire Stripe) — et seul le webhook `charge.refunded` passe `true`, encore uniquement quand l'appel de liste Stripe a RÉUSSI (`!listFailed` : la charge n'embarque que les dix plus récents). Une reprise retenue est rapportée (`heldGiveBack`) et alertée avec la raison en mots : jamais appliquée en silence, jamais abandonnée en silence.

*Défaut 6 — P2 : le vérificateur ne vérifiait qu'un côté.* `detectProrataDrift` ne comparait que `earn_reversal`. Un total de restitution (`refund`) faux était invérifiable par construction. Corrigé : les deux côtés, avec la MÊME base que l'écrivain (la ligne `earn`) et le MÊME plancher de cumul — sinon toute divergence entre la colonne et le crédit produisait un « DÉFAUT » permanent qu'aucune réparation ne pouvait effacer. Le champ `sides` nomme le ou les côtés en écart.

*Défaut 7 — P2 : UNE DETTE INVENTÉE du côté D2.* Une reprise de restitution (`refund`) au-delà du solde disponible logeait le reste dans `recoveryOffsetPoints` — une dette que le client n'a jamais contractée, qu'il aurait remboursée sur un gain futur, et que la restitution positive symétrique n'aurait jamais défaite. Une sur-restitution est NOTRE erreur d'arithmétique, pas des points gagnés puis dépensés. Corrigé : le côté D2 ne crée plus jamais de dette — il prend ce que le solde porte, RETIENT le reste (`heldGiveBack`) et le dit. Et la ligne de registre est désormais écrite en DERNIER, pour le mouvement RÉELLEMENT appliqué, donc le registre n'affirme jamais un mouvement qui n'a pas eu lieu.

*Défaut 8 — P1 : §24 (8) décrivait mal son propre cas, et son alerte obligatoire ne partait pas.* La séquence réelle est plus conséquente qu'elle n'y paraît : à `delivered`, `applyEarnWithOffsetRepay` fait d'abord REMBOURSER la dette antérieure par le gain (§14), donc seul le reste atteint le solde dépensable ; la reprise qui suit prend ensuite ses points sur ce solde déjà réduit. Le gain annulé a donc bien éteint une partie de la dette, et la reprise a été en partie payée par les autres points du client. Aucune moitié du code ne voyait l'autre — la transaction d'earn voit le remboursement, le rejeu voit la reprise — donc l'alerte que le fondateur a demandée pour ce cas précis ne partait jamais. Corrigée : la transition retient combien de dette le gain a remboursé et, si le rejeu reprise effectivement des points, émet `loyalty_offset_t44_review` depuis le seul endroit qui voit les deux moitiés. §24 (8) porte désormais le compte exact de ce qui se passe, et les trois formes d'alerte T-44 sont énumérées.

*Défaut 9 — P2/P3 : ce que l'administrateur ne voyait pas.* La route de réparation laissait tomber `converged`, `blocked`, `offsetReleased` et `offsetDeferredT44` de sa réponse ET de son `AdminAuditLog` : un mouvement de points causé par un administrateur n'était que partiellement lisible. Corrigé, avec une phrase en tête de réponse qui dit si la réconciliation a **atterri** et non seulement si elle a **tourné**. Corrigés aussi : `converged` rapporté faux (et une fausse alerte MONEY REVIEW émise) quand l'écriture tombait sur la dernière tentative disponible — une vérification finale en lecture seule tranche ; la cible rapportée à 0 sur un côté bloqué alors qu'elle valait 2 (une transaction qui lève emporte sa valeur de retour : elle est désormais retenue hors du callback) ; le message « les points restent entiers » sur la branche `no_refunds`, qui affirmait la justesse des points sans lire une seule ligne ; et les commentaires des trois appelants qui décrivaient encore la garantie d'avant L6.1 (« la clé `re_` rend le rejeu inoffensif »).

**Preuves ciblées.** Les neuf cas obligatoires du fondateur (A → I) sont dans `tests/loyalty-refund-apply.test.ts`, chacun conduisant le PRODUIT et relisant les lignes et le solde : A 470 seul ⇒ 5 · **B 470 puis un 470 PLUS ANCIEN ⇒ total 9 et second delta 4, jamais 10** · C 470 × 3 un par un ⇒ 14 · D le même ensemble rejoué DIX fois ⇒ pas une écriture · E ordre C, A, B ⇒ état final identique à A, B, C (lignes, solde ET dette) · F partiel puis total ⇒ cible exacte, et un cumul qui dépasse T ne pousse pas plus loin · G le même `re_` prouvé par les deux sources ⇒ compté UNE fois · H un `pending` qui devient `succeeded` ⇒ nouvelle cible appliquée une seule fois · I les trois appelants convergent parce qu'il n'y a qu'UN écrivain (épinglé à la source : aucun des quatre fichiers appelants ne crée de ligne de réconciliation), et le même ensemble passé dans deux ordres différents atterrit identiquement.

Plus, au-delà de la liste : une **propriété** sur 40 scénarios pseudo-aléatoires (montants, nombre de remboursements, ordre d'arrivée tirés d'une graine déterministe) qui vérifie la seule invariante qui compte — le total appliqué de chaque côté égale la cible §9 de l'ensemble final, recalculée par `loyaltyPointsCumulative` et jamais réécrite en constante — suivie d'une passe de plus qui n'écrit rien ; son **contrôle négatif** rejoue l'ancien modèle par événement et montre qu'il échoue à cette propriété. Et les guet-apens de la revue : base périmée, ensemble qui rétrécit, désaccord de dénominateur, reprise sur preuve incomplète, aller-retour de l'inverse D3 (avec le contrôle négatif de la simplification tentante), clé jamais nulle, effet lu sur les LIGNES et jamais sur le solde.

`tests/loyalty-refund.test.ts` **45** (l'arithmétique pure : dédup, indépendance d'ordre, delta signé, balayage exhaustif de tous les cumuls de 0 à T, l'inverse D3) · `tests/loyalty-refund-apply.test.ts` **33** (la couche DB, sur un faux Prisma qui applique l'index unique et annule la transaction interactive sur violation) · `tests/claims-dprime-l6-loyalty-d15.test.ts` **43** · suite complète **478 fichiers / 6 682 tests verts** ; `tsc` **39 = 39** vs baseline `dab754d`, ensemble d'erreurs IDENTIQUE (comparé par fichier + code, numéros de ligne ignorés) ; `next lint` **0 erreur** ; `check:i18n` 5/5 ; `check:flags` OK ; **build à froid OK** ; `lib/refund.ts` byte-identique (blob `e2dc42b8`) ; **rail L5, `middleware.ts` et `prisma/schema.prisma` INTOUCHÉS** — la clé dérivée tient dans `sourceEventId String?` existant, donc **aucune migration, aucun regen**.

**Contrat fidélité amendé** (`docs/ops/LOYALTY-REFUND-CONTRACT.md`) : en-tête (amendement L6.1), **§9** (la règle d'arrondi est INCHANGÉE ; la persistance devient la convergence), **§10** (l'affirmation « out-of-order webhook delivery is safe … a new one only appends in the sorted order » était **FAUSSE** et est retirée, avec la raison), **§11** / **§12** (clé = la transition, pas le `re_` ; convention signée), **§13** (l'inverse pour une reprise), **§16** (la source d'idempotence, réécrite en trois couches), **§17**, **§24 (3)(4)** et **§24 (7)(8)** (le (7) passe de OPEN RESIDUAL à CLOSED BY L6.1 ; le (8) à DEFERRED TO T-44 PRE-LIVE).

### L6.1 — déploiement staging vérifié (2026-09-25)
Commit **`9e56db23955d2bcca31feed70c786081083617c7`**, poussé sur `develop`. CI « Deploy to Staging » run **36165657522** : job `test` **success** (suite complète + porte de compilation exécutées aussi en CI), job `deploy` **success** — dont « Verify deployed build (version.json) », « Health check (staging @ expected SHA) » et « Client bundle integrity (served HTML → every `_next/static` asset 200) », les trois **success**. `version.json` servi par staging = **`9e56db23955d2bcca31feed70c786081083617c7`** (build 17:19:19Z, ciRunId 36165657522) — le SHA déployé est EXACTEMENT celui de L6.1. Santé mesurée depuis l'extérieur : `/fr/eat` **200**, `/api/restaurants` **200** (la DB répond, donc le client Prisma déployé n'est pas périmé).

Recensement read-only `claims-census.yml` run **36168242132**, mesuré 2026-09-25T17:37:09Z sur le build déployé :
- `schema` = `{ready:true, clientReady:true, dbReady:true, missingClient:[], missingDb:[], why:null}` — **aucune migration, aucun regen**, et il n'en fallait aucun : la clé dérivée de L6.1 tient dans la colonne `sourceEventId String?` existante.
- `gates` = `{claimsEnabled:false, claimsGate:"CLOSED (flag_off)", claimsSurfaceEnabled:false, claimsIntakeEnabled:false, claimsSurfaceOpen:false, claimsIntakeOpen:false, refundsEnabled:false}` — **toutes fermées**, les deux flags produit toujours OFF, le bail REFUNDS jamais ouvert.
- **Population réclamations INCHANGÉE** depuis L0 (`dab754d`), L5 et L6 : total **9**, `{refunded:4, refused:3, refused_final:2}`, `active:0`, `nonTerminal:3`, `approvedUnpaid:0`, `closure.terminalWithoutRecord:4`, **tous les compteurs d'anomalie héritée à 0** — dont `voidedRefundRows:0` et `pendingRowsOver20hWithSettledRoyalty:0`, les deux qui regardent les lignes `Refund`.

Sondes externes non authentifiées du build déployé : `POST /api/claims` → **403 `{gated:true}`** · `POST /api/admin/loyalty/reconcile` → **403 « Accès refusé »** (`resolveAdmin()` répond avant tout ; cette route reste gatée par AUCUN flag réclamations, par contrat) · `POST /api/admin/claims/pay-approved {"dryRun":true}` → **403 « Accès refusé »** (rail L5 inchangé) · `POST /api/admin/refunds/run` → **403 `{gated:true}`**.

Portée de ce qui est mesuré, dite explicitement : le recensement compte les **réclamations** (et les anomalies qui regardent les lignes `Refund`), pas les totaux bruts de `LoyaltyTransaction`. Ce qui est établi : **aucun appel authentifié** n'a été fait contre staging dans ce lot, aucune commande n'a été créée ni transitionnée, aucune réclamation déposée, les quatre sondes se sont arrêtées au garde AVANT toute logique, et un déploiement n'exécute par lui-même ni migration ni script serveur — donc **aucune ligne `LoyaltyTransaction` n'a pu être ajoutée par ce déploiement**. **Aucune fenêtre ouverte, aucun flag modifié, aucune migration, aucun regen Prisma, aucun accès cPanel, aucune opération Stripe, aucun e-mail envoyé.** `scripts/server/phase2-claims-pay-window.js` n'a jamais été exécuté.

## Lots suivants
(complété lot par lot : SHA, preuves, CI, SHA déployé)
