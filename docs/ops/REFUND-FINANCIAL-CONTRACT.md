# REFUND-FINANCIAL-CONTRACT — PHASE 2 (refund financial rail / franchise royalty)

> Phase 0 deliverable (read-only forensics: lead + 2 forensics agents + adversarial critic), then the implementation contract for Phase 2 on branch `a1/refund-rail`. All money in **integer cents**. Established @ `f0e3c2b` (= staging `app.grubano.com` + `business.grubano.com`, `/version.json` verified on both, 2026-09-03). **`REFUNDS_ENABLED=FALSE`, `CLAIMS_ENABLED=FALSE`, refund-initiation freeze ACTIVE** (Stripe TEST read-only probe at session start: 0 refunds in 24 h, account total 14 unchanged, 0 on the rehearsal order). `main` / production / Stripe LIVE never touched.

---

## 0 · Verdict of Phase 0 — the P0 is real, and it has TWO doors

**P0 — franchise royalty returned twice.** On a FRANCHISED order the application fee holds the franchisor's royalty (`royaltyChargedCents`, composed into `application_fee_amount` at `app/api/orders/[id]/pay/route.ts:272-276`, accrued as a `FranchiseRoyalty` `pending` row). A refund with `refund_application_fee:true` returns a prorated slice of that fee — royalty included — to the customer. The franchise settlement then pays `royaltyCents − refundedCents` (`lib/franchise-settlement.ts:83-84`). **Whoever refunds without bumping `FranchiseRoyalty.refundedCents` makes Grubano pay the royalty slice twice** (once to the customer, once to the franchisor).

Two doors leave `refundedCents` untouched today:

| Door | Path | Royalty-aware? | Evidence |
|---|---|---|---|
| **A — order route** | `POST /api/orders/[id]/refund` → `lib/refunds.ts refundPayment` | **NO** — never reads `FranchiseRoyalty`, never writes a `Refund` row | `app/api/orders/[id]/refund/route.ts:72-75`; `lib/refunds.ts` has 0 occurrences of royalty / prisma |
| **B — admin engine** | `POST /api/admin/refunds/run` → `lib/refund.ts executeRefund` | **YES** — split nests the royalty, `refundedCents` recomputed, settled royalty clawed back | `lib/refund.ts:294-365` |
| **W — external refund** | Stripe Dashboard refund → webhook `charge.refunded` | **NO** — ledger + loyalty + tip only | `app/api/webhooks/stripe/route.ts:623-756` (no `franchiseRoyalty` access) |
| tickets / deposits | `tickets/[id]/refund`, `reservations/[id]/refund-deposit` → rail A | n/a — **no Order row, no royalty possible** (`FranchiseRoyalty` keyed by `orderId`, channels pickup/delivery only) | `app/api/tickets/[id]/refund/route.ts:58`, `app/api/reservations/[id]/refund-deposit/route.ts:53` |

Doors A and B share the SAME `REFUNDS_ENABLED` flag (`app/api/orders/[id]/refund/route.ts:41`, `app/api/admin/refunds/run/route.ts:43`) → the instant the flag flips, door A is a double-payment on any franchised order. Door W is **ungated by design** (founder rule: the flag gates *initiation*, never reconciliation of an established Stripe refund — `LOYALTY-REFUND-CONTRACT.md §17`) → door W is reachable **today**.

The rejection note in both lib headers (« routing owner routes through the engine audited & REJECTED — tickets have no Order row, would take the live path dark ») is **obsolete for the ORDER route**: since P0-26 the order route is admin-only and gated by the same flag as the engine, and the ticket/deposit routes have no Order row so they are out of scope. No document with the « 10 divergences » exists in the repo (grep: 0 hits) — the divergences are re-derived in §4.

## 1 · Money decomposition on a routed order charge (facts)

`amount` (= `order.total`) = subtotal + deliveryFee + smallFee + tip − promo − loyaltyCredit.
`application_fee_amount` = `max(0, grossCommission − loyaltyCredit) + smallFee + tip + courierWithheld + royaltyCharged` (`pay/route.ts:272-276`); the restaurant's net = amount − fee. Stripe destination charge with `on_behalf_of` (`lib/stripe.ts:95-99`).

Inside the fee, Grubano is the **owner** of commission + smallFee only; it is the **custodian** of the tip (owed to the courier, `lib/courier-accrual.ts`), of courierWithheld (owed to the courier) and of royaltyCharged (owed to the franchisor, `FranchiseRoyalty`).

## 2 · Refund split — `computeRefundSplit` (facts, `tests/refund-split.test.ts` 10/10)

`T` charge total, `F` fee (clamped ≤ T), `R` royalty (clamped ≤ F), `Cprev` already refunded (live Stripe), `C = Cprev + amount`.
`feeCum(x) = round(F·x/T)`, `royCum(x) = round(feeCum(x)·R/F)`; per-refund deltas telescope → `restaurantReverse + applicationFeeRefund === amount` exactly; N partials sum exactly to F, T−F, R (`lib/refund.ts:116-153`). Both refund libs pass `refund_application_fee:true, reverse_transfer:true` only when `pi.transfer_data` is set (`lib/refund.ts:245`, `lib/refunds.ts:73`) — Stripe prorates the fee itself; our split is the *prediction* of Stripe's proration.

⚠️ **Prediction vs Stripe truth in the ledger.** The eager ledger line written by `finalizeRefund` uses OUR predicted `applicationFeeRefundCents` (`lib/refund.ts:329-341`), while the webhook backstop uses **Stripe's REAL fee refund** (`webhook:651-672`); both are keyed on the same `re_…`, whichever lands first wins → a rounding divergence between prediction and Stripe would be frozen in the ledger. Stripe's rounding rule: see §9.

## 3 · Rail B engine — what it guarantees today (facts, `tests/refund-engine.test.ts` 16/16)

- refundable derived live: `charge.amount − amount_refunded`; over-amount → 400; fully refunded → 409 (`lib/refund.ts:450-457`).
- cursor `Refund.idempotencyKey @unique = refund:<orderId>:<alreadyRefunded>` = Stripe idempotency key (`:472-497`); RESUME-FIRST of a `pending` row (`:433-447`).
- royalty: split slice recorded (`royaltyRefundCents`); `refundedCents` recomputed cross-rail via `recomputeRoyaltyRefundedCents` (Σ succeeded Refund slices + Σ lost-dispute slices, capped, monotone — `lib/royalty-refunded.ts:24-46`); settled/settling royalty with a locatable transfer → `transfers.createReversal` idempotent on `refund-claw:<rowId>` (`:294-325`); failure → row stays `pending`, 502 retryable.
- eager ledger line idempotent with the webhook on `(re_id,'refund')` (`lib/ledger.ts:52-83`).
- ⚠️ **Truthfulness gap:** `finalizeRefund` marks the row `succeeded`, writes the ledger line and the routes send « Remboursement effectué » **without checking `stripeRefund.status`** (`lib/refund.ts:348-351`; emails `admin/refunds/run/route.ts:119`, `orders/[id]/refund/route.ts:100`). `Refund.status='failed'` is never written anywhere (grep: 0 writers). No `charge.refund.updated` / `refund.*` webhook branch exists (`webhook:90-136` handles `account.updated`, `charge.refunded`, `charge.dispute.*`, `payment_intent.*`). Stripe refund status lifecycle: see §9.
- `Order.paymentStatus` is NOT mutated by either rail (stays `paid`); only the ghost-order branch writes `refunded` / `reconcile_manual` (`webhook:410-465`).

## 4 · Door A vs door B — divergences re-derived (the « 10 » never existed in-repo)

| # | Rail A (`refundPayment`) | Rail B (`executeRefund`) | Consequence of moving the ORDER route to B |
|---|---|---|---|
| 1 | keyed by PI | keyed by orderId | the route already holds the order → none |
| 2 | no DB row | `Refund` row (audit + cursor) | gain: audit trail + anti-double cursor |
| 3 | idempotency `refund-<pi>-<amt>-<already>` | `refund:<orderId>:<already>` | a concurrent same-cumul attempt → 409 instead of replaying the same refund: **safer** |
| 4 | no royalty | royalty-aware | **closes the P0** |
| 5 | ledger by webhook only | eager + webhook backstop | gain: immediate ledger visibility |
| 6 | no resume | RESUME-FIRST: a `pending` row is re-driven **before** the new amount is honoured | documented: the admin may get `resumed:true` with the pending amount; the response's `refundedCents` is that amount |
| 7 | response `{refundId: re_…, refundedCents, remainingCents, routed}` | `{refundId: row, stripeRefundId: re_…, amountCents, remainingRefundableCents, routed, …}` | the route maps to the SAME public shape (`refundId` = `re_…`) → existing consumers unchanged |
| 8 | 400/409 message texts | identical texts | none |
| 9 | `reconcile_manual` accepted | accepted | none |
| 10 | email dedupe `order:<id>:<cents>` | same | none |

**Decision D-A — ONE engine on the order path.** `POST /api/orders/[id]/refund` calls `executeRefund({orderId, amountCents})`. `lib/refunds.ts` stays for tickets/deposits only (no Order row) and both lib headers are corrected (the « UNGATED, LIVE » and « REJECTED » statements are stale). The route's guard order (rate-limit → flag → admin) is unchanged.

## 5 · Door W — external (Dashboard) refund reconciliation (decision D-B)

`charge.refunded` is already the single reconciliation point for the ledger (`webhook:672-698`) and for loyalty (`:709-729`). Phase 2 adds **royalty accounting** there, with the same properties: derived from the Stripe truth — **the cumulative sum of the `succeeded` refunds** of the charge (the list the handler already filters at `webhook:646`; **never `charge.amount_refunded`**, which counts a still-`pending` refund that may later fail — critic #2) — cumulative-target (`royCum(Σ succeeded)` = `computeRefundSplit({alreadyRefunded:0, amount: Σ succeeded}).cumulativeRoyaltyRefundedCents`), **monotone**, **cross-rail** (`max(Σ Refund rows, royCum(Σ succeeded)) + Σ dispute slices`, capped at `royaltyCents`). Telescoping identity (verified by the critic, #9): Σ engine slices == royCum(cumul) for the same (T,F,R), and engine and webhook read the same `charge.amount`, `charge.application_fee_amount`, `FranchiseRoyalty.royaltyCents` → an engine refund and an external refund of the same money reconcile to the **same** target — no double count, no under count. Idempotent by construction (a target, not a delta). Cross-rail tolerance: dispute slices are computed from a different `alreadyRefunded` origin → `royCum(X)+royCum(D)` may differ from `royCum(X+D)` by **±1 cent**; the cap absorbs the high side, the low side is a documented 1-cent tolerance (dispute-then-refund is mostly academic — Stripe normally refuses a refund on a disputed charge, `charge_disputed`, UNVERIFIED).

**No money movement from the webhook.** A `settled`/`settling` royalty hit by an **EXTERNAL** refund — identified as a Stripe Refund **without** `metadata.grubano_refund_row` (the engine tags every refund it creates, `lib/refund.ts:246`) — is **flagged** (`MONEY REVIEW` log + best-effort admin alert with the exact slice), never auto-reversed: the founder principle « no unattended external financial effect » (`lib/refund.ts:71-80`) applies to a clawback that no Grubano human initiated. Engine-originated refunds are not flagged (the engine's own step (a) claws back). Founder-visible residual: that external clawback is a human action (admin rail after Phase 3, or a Stripe Dashboard transfer reversal).

**Webhook as backstop for the engine (critic #11):** `finalizeRefund` steps (c) row `succeeded` and (d) `refundedCents` are not atomic; if (d) throws, the D-B target heals `refundedCents` on the next `charge.refunded`/`refund.updated` delivery (same value by the identity). Recorded, not wrapped.

## 6 · Stripe processing fee & courier tip on partial refunds (decisions D-D, D-E)

- **Stripe processing fee** (`stripeFeeAmount` on the payment line = real balance-transaction fee, `webhook:556-565`; `0` on refund lines, « zero movement, affirmed », `lib/ledger.ts:50-51`). Stripe does not return its processing fee on a refund (see §9). Model: Grubano's **exposure** on any refund includes the processing fee already paid (unchanged) — nothing moves, nothing to book. Decision: **keep `stripeFeeAmount = 0` on refund lines** (movement truth) and make the retained fee explicit in the pure exposure model + tests (no ledger change).
- **Courier tip.** The fee refund returns `tip·ΔC/T` to the customer (the tip is inside F), while `clawbackCourierTip` fires only on a TOTAL refund (`webhook:732-750`, `lib/courier-accrual.ts:208-227`). Decision (default; founder may override): **partial refund → the courier keeps the whole tip (service performed — mirrors « the COURSE stays acquired »), Grubano absorbs the returned tip slice**; full refund → existing clawback of unpaid tips (paid tips kept, logged). The absorbed slice is **computed explicitly** (`computeRefundExposure`) and tested, never silent.

## 7 · Tripartite conservation identity (decision D-C — new test)

For every refund event, and cumulatively, with `a` = amount refunded to the customer:
`customerΔ = +a` · `restaurantΔ = −restaurantReverse` · `franchisorΔ = −royaltySlice` (via `refundedCents` or clawback) · `courierΔ = −tipSlice` only when clawed (full refund, unpaid tip), else 0 · `grubanoΔ = −(applicationFeeRefund − royaltySlice − courierΔabs)` · `stripeΔ = 0`.
**Identity:** `customerΔ + restaurantΔ + grubanoΔ + franchisorΔ + courierΔ + stripeΔ = 0` at every step, integer cents, zero residue. After a FULL refund: customer +T, restaurant −(T−F), franchisor −R, courier −tip (if clawed) else 0, Grubano the rest, Stripe keeps its fee. Proven on STANDARD (R=0) and FRANCHISE (R>0) × full / partial / multiple partials with odd cents.

## 8 · Failed / pending refund truthfulness (decision D-F)

- `finalizeRefund` marks `succeeded` + eager ledger + returns `ok:true` **only when `stripeRefund.status === 'succeeded'`**. A `pending` Stripe refund → the row stays `pending` **with `stripeRefundId` recorded**, and the outcome is a **distinct non-ok variant** `{ok:false, status:202, pending:true, refundId, stripeRefundId, amountCents, error:'Remboursement en attente côté Stripe.'}` (critic #4: every existing caller tests `result.ok` as « money moved » — `lib/claims.ts:246`, `webhook:446` — so pending must never be `ok:true`). Routes answer **202 `{status:'pending', …}`** and send **no** email; claims keep the claim `refunding` with the `refundId` (no `refundError`); the ghost-order branch falls to `reconcile_manual` (conservative, documented). A later `executeRefund` call (RESUME-FIRST) or the `refund.updated` webhook finalizes once succeeded. **RESUME-FIRST + pending = an order-level lock** (any new partial on that order answers `pending`, up to Stripe's 30-day window) — accepted, documented; a resumed row ignores the caller's `amountCents` and says so (`resumed:true`, explicit message — critic #17).
- **Eager ledger = Stripe truth, not prediction (§2 ⚠️ closed):** `finalizeRefund` re-lists the charge's **succeeded** refunds and the fee's refunds (the fresh-path `charge` object is PRE-refund — critic #8) and resolves the real fee refund with ONE pure helper shared with the webhook: chronological index-matching **accepted only when counts match AND every `|feeRefund_i − round(F·amount_i/T)| ≤ 1`** (second-granular `created` makes same-second ties ambiguous); otherwise the eager line is **skipped** (the webhook backstop writes it, prorata fallback + `MONEY REVIEW`). The `Refund` row keeps the predicted split for audit; prediction ≠ truth is logged `MONEY REVIEW` (rounding evidence for the founder). The ledger never freezes a predicted cent.
- New webhook branch **`refund.updated` / `refund.failed`** (object = Stripe Refund, the status oracle per §9.4; `charge.refund.updated` is deprecated). Row matching order: `metadata.grubano_refund_row` first, `stripeRefundId` second (a lost create response leaves `stripeRefundId` null — critic #12). `succeeded` → finalize the matching `pending` row (idempotent, same `finalizeRefund`); `failed` / `canceled` → row `status='failed'`, `idempotencyKey` suffixed `:failed:<re_id>` (the cumul cursor is released), no ledger line (none was written), loyalty untouched (it reconciles succeeded refunds only), `MONEY REVIEW` log + admin alert. **These two events are NOT subscribed on the Stripe TEST endpoint today** (read-only probe: `we_…7ueK` = `payment_intent.amount_capturable_updated, payment_intent.canceled, payment_intent.succeeded, charge.refunded`; `we_…tpvK` = `account.updated`) → **founder readiness action** (§13). Until then the oracle is RESUME-FIRST on the next engine call — truthful (never « effectué »), just slower.
- **Fail-closed after a failure (§9.5):** because Stripe does not restore the reversed transfer on a failed refund, `executeRefund` **refuses** (409 « reprise manuelle requise ») any new refund on an order that carries a `failed` row with a `stripeRefundId` — a blind retry with `reverse_transfer:true` would debit the restaurant twice. **The lock is permanent and explicit** (critic #13): the human path is the Stripe Dashboard (re-transfer the reversed net to the restaurant, then refund without `reverse_transfer`), and the order stays visible in the failed-refund alert. No automation, no unblock route in Phase 2.
- Email wording unchanged (« effectué ») because it is now sent only after Stripe `succeeded`.

## 9 · STRIPE SEMANTICS FACT SHEET (agent A — docs.stripe.com fetched 2026-09-03; FACT = documented, UNVERIFIED = not stated in docs)

1. **Fee refund proration — FACT.** `refund_application_fee:true` refunds the whole fee on a full refund, else « an amount proportional to the amount of the charge refunded » (api/refunds/create; worked example 100/5/40 → 2). **Rounding rule — UNVERIFIED** (no doc states floor/half-up) → never trust a recomputation for the ledger; read the real `fee_refund.amount` (item 8).
2. **Where the money moves — FACT.** On a destination charge with `application_fee_amount`, the transfer to the connected account is the FULL charge amount and the fee is pulled back to the platform. `reverse_transfer:true` reverses **proportionally = the gross refund amount X** from the connected account; the fee refund then **credits the connected account** (« refunded to the Stripe account from which the fee was originally collected »); the customer is paid from the PLATFORM balance. Net: connected account −(X − feeRefund), platform −feeRefund → **our split's net assumption is CONFIRMED** (two balance transactions, not one netted debit). Refunding the fee on a destination charge **requires** reversing the transfer (connect/destination-charges#issue-refunds).
3. **Stripe processing fee — FACT.** Never returned on a refund (« processing fees from the original transaction aren't returned »). On destination charges the **PLATFORM bears Stripe fees, with or without `on_behalf_of`** (`on_behalf_of` only selects the fee schedule's country). → D-D confirmed: zero movement, Grubano's sunk cost.
4. **Refund status lifecycle — FACT.** `status ∈ {pending, requires_action, succeeded, failed, canceled}`; a card refund can be `pending` at creation (insufficient platform balance, unsettled charge) and later `failed` (bank failure up to 30 days; `insufficient_funds` after the pending window); `failure_balance_transaction` re-credits the platform. **Events — FACT:** `refund.created`, `refund.updated`, `refund.failed`, `charge.refunded` (« whenever a charge is refunded, including partial », emitted at refund **creation** → it CAN carry a still-`pending` refund), `application_fee.refunded`, `transfer.reversed`. `charge.refund.updated` is deprecated/limited. → the status oracle is **`refund.updated` / `refund.failed`** (object = Refund, carries `payment_intent`, `charge`, `metadata`, `status`, `failure_reason`). Whether `charge.amount_refunded` is decremented on failure — **UNVERIFIED** → never rely on it.
5. **Failed refund on a destination charge — FACT.** Funds return to the PLATFORM balance; **the reversed transfer is NOT auto-restored** (« Create a Transfer to move the funds to the connected account, as needed »). → after a failure the restaurant has been debited X and credited feeRefund while the customer got nothing; a blind retry with `reverse_transfer:true` would **debit the restaurant a second time**. Phase 2 policy: **fail-closed** (§8).
6. **External Dashboard refund — FACT** that it is possible; **UNVERIFIED** whether the Dashboard defaults to reversing the transfer / refunding the fee on a destination charge. → the webhook must never assume: detect via `refund.transfer_reversal` (nullable) and the real fee refunds list.
7. **`transfers.createReversal` — FACT.** Partial reversals allowed until fully reversed; requires the connected account's available balance ≥ amount (else `balance_insufficient`, no negative balance); refund+reverse_transfer with insufficient connected balance → the refund request itself **errors** (no pending refund created).
8. **Idempotency keys — FACT.** Pruned after ≥ 24 h; same key + different params = error.
9. **Real fee refund — FACT.** `GET /v1/application_fees/{fee}/refunds` → `fee_refund.amount` (integer); stripe-node `applicationFees.listRefunds(feeId)` (already used at `webhook:657`). The Refund object has no pointer to its fee refund → correlate by charge + chronological index (the webhook's existing rule).

## 10 · REPO INTERPLAY FACT SHEET (agent B — file:line, verified by the lead)

- **A. Settlement lifecycle** (`lib/franchise-settlement.ts`): `pending → settling` by one atomic `updateMany` (`:303-306`), `settling → pending` only by the below-threshold rollback (`:318-321`), `settling → settled` in `finalizePaid` (`:100-105`) or the net-0 path (`:163-166`). `refundedCents` is read at CLAIM time only (aggregate `:290-296`, snapshot `:309-311` passed to `finalizeBatch` `:326`), summed once (`:154`) and **frozen** into `Payout.amountCents` (`:176-179`) then transferred (`:225-234`).
  ⚠️ **Defect S1 — a `refundedCents` bump between claim and transfer is IGNORED on the fresh path** (no re-read, the `amount !== p.amountCents` guard is skipped when `fresh`, `:203`) → the franchisor is over-transferred by the refunded slice — exactly the case the engine « defers to settlement reconciliation » (`lib/refund.ts:317-324`, a promise that is FALSE for a claimed batch). ⚠️ **Defect S2 — on the resume path** the re-summed lines ≠ frozen amount → `amount_mismatch` → `failed`, **batch stuck in `settling` forever** (`:216-219`; nothing reverts it).
  **Decision D-G v2 (fail-closed DETECTION, no money re-plan — critic #3):** lowering `Payout.amountCents` before the transfer was rejected: the Stripe idempotency key `franchise:<op>:<settlementId>` is exactly what makes two overlapping runs (fresh + resume) converge on ONE transfer today; changing the amount under the same key → `idempotency_error`, and a per-amount key re-opens a double-transfer between a 300-attempt and a 260-attempt. A safe mutex needs a `Payout` freshness field (`updatedAt`) that the model does not have (schema `Payout`: `pending|paid|failed`, no `updatedAt`) — no schema change in Phase 2. So: **(fresh path)** after `transfers.create`, re-read the batch lines; if Σ live `netOwed` < transferred → `MONEY REVIEW` log + best-effort admin alert with the exact over-transfer delta (human clawback; the lines are still marked `settled`, the money did move). **(resume path)** `amount !== p.amountCents` with no transfer in the group → still **no transfer**, but the outcome becomes `failed:'amount_drift'` **with a `MONEY REVIEW` log + admin alert** (today it is a silent `console.error`); the batch is deliberately left `settling` — reverting it to `pending` is unsafe while a concurrent run could still transfer the frozen amount (the shared key is the only guard). **Residual (documented, founder-visible):** S1's over-transfer window (a refund landing between claim and `transfers.create`, ~1 s) and S2's stuck batch are **detected and alerted, not auto-corrected**; un-sticking a drifted batch is a human operator action. Both refund rails keep writing `refundedCents` with no status filter (`lib/refund.ts:364`, `lib/dispute.ts:292`).
- **B. Dispute ↔ refund cross-rail** (`lib/dispute.ts:228-234`, `:111`): a lost dispute takes `alreadyRefunded` from live `charge.amount_refunded` → refund-then-dispute never double counts. ⚠️ **Defect X1 — dispute-then-refund:** a lost dispute is not a Stripe refund, so `executeRefund` (`lib/refund.ts:462-467`) re-claims the same royalty slice; `refundedCents` is capped (`lib/royalty-refunded.ts:45`) but the **money** is not: `finalizeRefund` would fire a second `transfers.createReversal` (`refund-claw:<rowId>` ≠ `dispute-claw:<disputeId>`) against the same settlement transfer. **Decision D-H v2 (critic #1 — P0 fixed):** cap the clawback on the royalty **actually recovered from the franchisor**, never on `refundedCents` (which is an accounting target also raised by the webhook for settled rows, so a `charge.refunded` delivered before `refunds.create` returns would have zeroed a legitimate clawback → the P0 re-opened): `amount = min(row.royaltyRefundCents, max(0, royalty.royaltyCents − recovered))` with `recovered = Σ Refund.royaltyClawbackCents (status succeeded, other rows) + Σ Dispute.royaltyClawbackCents (splitReversed)` — one helper next to `recomputeRoyaltyRefundedCents`. On a resume of the same row its own clawback is 0 until step (c) and the Stripe key `refund-claw:<rowId>` returns the same reversal → no double, no zeroing. Unit-tested (sequential 150+150 on R=300; race with webhook target already at 300; dispute-then-refund). (`CHARGEBACKS_ENABLED` is OFF in beta; the guard is cheap and closes the class.)
- **C. Ledger check probe** (`app/api/admin/ledger/check/route.ts:142-185`, cron 24 h `scripts/cron/ledger-check-probe.js:94-96`): compares ledger refund sum vs **succeeded** Stripe refunds (`:158`) + per-id presence. ⚠️ An eager line for a `pending` Stripe refund (or one that later fails) → `refundsOk=false` → alert with **no diagnostic payload**. Closed by §8 (eager line only on `succeeded`, truth-matched fee).
- **D. `paymentStatus='refunded'`:** exactly one writer (`webhook:453`, ghost-order, gated). Neither rail mutates it. Consumer UI shows Claim status only (`eat/order/[orderId]/help/page.tsx:227-228`); admin UI is read-only (`admin/payments/LedgerClient.tsx`, `admin/reconciliation/ReconciliationClient.tsx:15` « no action », `:93`).
- **E. Email** (`lib/transactional-emails.ts:691-712`): subject « Remboursement {partiel }effectué — {resto} », body « …vient d'être effectué… délai bancaire 5 à 10 jours ». Dedupe `(trigger, dedupeKey)` on `EmailDispatch` (`:141-149`, compensating delete `:178-181`); key `order:<id>:<cents>` → two distinct refunds of the same amount collapse to one email (**documented intentional**, `admin/refunds/run/route.ts:100-104` — kept). **Nothing checks the Stripe refund status** before sending (closed by §8).
- **F. Tests:** ⚠️ **`handleChargeRefunded` has ZERO tests** (grep `charge.refunded` / `handleChargeRefunded` in `tests/` = 0; no fixture builds a Charge with `refunds.data` / `application_fee`). Adjacent coverage exists only around it (`refund-engine`, `finance-ledger-writers`, `loyalty-refund*`, `courier-tip`). Phase 2 adds the webhook suite.
- **G. Courier tip:** `Order.tipCents` at creation (`orders/route.ts:489`), folded into the fee (`pay/route.ts:272`), PI metadata `courier_tip_cents` (`:423`), held-tip ledger line (`:322-331`); accrual at mission `delivered` (`logistics/missions/[id]/step-handler.ts:60,76` → `lib/courier-accrual.ts:139-195`); **nothing adjusts the tip on a partial refund** (only `clawbackCourierTip`, all-or-nothing, webhook-only, total refund only).
- **H. Admin UI:** **NONE** calls either refund endpoint — both are API-only (curl / cron token / admin session).
- **I. Webhook branches:** `account.updated`, `charge.refunded`, `charge.dispute.*` (gated), `payment_intent.*`; **no `refund.*` branch anywhere**.
- **J. `Refund` model:** `status` `pending|succeeded|failed` (`schema:2042`); **`failed` never written**; the only readers are `lib/refund.ts` (resume/create/update) and `lib/royalty-refunded.ts` (aggregate) — no route, UI or cron reads the table.

## 11 · Test matrix (Phase 2 harness — mocked Stripe/DB; the live Stripe TEST refund is the founder rehearsal AFTER PASS, freeze respected)

| Axis | Values |
|---|---|
| restaurant | STANDARD (R=0) · FRANCHISE (R>0: royalty `pending` / `settling` / `settled`) |
| refund | full · partial · multiple partials (odd cents, 3×3.33 + 0.01) |
| door | order route → engine · admin run → engine · EXTERNAL Dashboard → webhook only |
| properties | cash ≤ captured · fee reversal policy (full 100 %, partial prorata) · reverse_transfer = amount − feeRefund (net) · royalty never double (settlement pays `royaltyCents − refundedCents`, identical target through both doors, live re-read before transfer D-G, clawback capped D-H) · ledger `gross = fee + net` with negatives, eager line = Stripe truth · tripartite identity · webhook idempotent (replay, out-of-order, `pending` refund in `charge.refunded` ignored until succeeded) · admin retry idempotent (cursor, resume, fail-closed after a failed refund) · truthfulness (pending never « effectué », failed frees the cursor) · loyalty Phase 1 unchanged (38/38 keep passing) |
| suites (as implemented) | `tests/refund-split.test.ts` (kept, 10) · `tests/refund-engine.test.ts` (36: + status truth, fee truth, D-H v2 cap, fail-closed lock, reversal adoption, fail-closed listing, `finalizeRefundRowFromStripe`, adopt-only webhook path, row/refund mismatch guard) · `tests/order-refund.test.ts` (22: route → engine, same public shape, pending 202 + audit, ⭐ negative control « `lib/refunds` never called from the order path ») · `tests/refunds-run-route.test.ts` (+2: pending 202, no email) · `tests/claims.test.ts` (+1: pending variant keeps `refunding`) · `tests/p1-p10/p5-refund-humain.test.ts` (rewritten against the real engine) · `tests/webhook-refund-reconciliation.test.ts` (**new**, 23: PI↔row mismatch guard, ledger real-fee vs prorata fallback, loyalty on succeeded only, tip clawback on Σ succeeded (+ A2 negative control), **royalty D-B** standard/franchise, external Dashboard refund with and without a `Refund` row, telescoping 333/333/333/4001 → 300 monotone, engine-originated vs external settled → alert not money, #16 control, cross-rail, list-failure 503 + alert, PI-metadata fallback, `refund.updated` succeeded → full re-run + row finalize (ledger once), `refund.failed` row/external, pending ignored, charge unavailable 503) · `tests/refund-conservation.test.ts` (**new**, 21: tripartite identity on STANDARD/FRANCHISE/courier-B × 5 sequences, exposure model, ⭐ floored-split negative controls (ii)/(ii-bis), tamper detection) · `tests/refund-fee-truth.test.ts` (**new**, 8) · `tests/franchise-settlement-refund.test.ts` (8: + D-G v2 fresh/adopt/drift + 2 negative controls) · `tests/franchise-settlement.test.ts` (`amount_drift`) · `tests/courier-tip.test.ts` (14: + A12 guard refunded / partial / ledger_unknown ×2) |
| test flips (D-A, critic #7) | `tests/order-refund.test.ts` (mock `@/lib/refund.executeRefund` instead of `@/lib/refunds`; pass-through assertions become `{orderId, amountCents}`; public shape preserved) · `tests/p1-p10/p5-refund-humain.test.ts:143-153` (drives the route through the real `lib/refunds` and asserts the rail-A key → rewritten against the engine outcome) · `tests/refund-routes-gate.test.ts:29` (mock of `@/lib/refund` must expose `executeRefund`) — no other suite imports the order route |
| negative controls | a harness that cannot fail proves nothing: (i) the OLD rail-A order route behaviour (royalty untouched, no `Refund` row) asserted → must FAIL after D-A; (ii) **independent invariants, not the tautological sum** (critic #6): over odd partials `[333,333,333,1]` assert Σ fee slices == F, Σ royalty == R, restaurant total == T−F, and per-refund fee slice == the Stripe `fee_refund.amount` fixture — run once against a deliberately **floored** split and show it fails; (iii) a webhook fixture that would regress `refundedCents` is rejected by monotonicity **and** (critic #16) `refundedCents` stays 0 while the only refund in the fixture is `pending`; (iv) an email asserted on a `pending` Stripe refund must FAIL; (v) D-G: the fixture where a line's `refundedCents` rises after the claim → the fresh path must raise the over-transfer alert (assert it fails when the re-read is removed) and the resume path must not transfer; (vi) D-H: with the webhook target already at `royaltyCents` (race), the clawback must still equal the slice — the old `refundedCents`-based cap gives 0 (shown failing) |

## 12 · Scope boundary (recorded)
- **In scope, small guards added by the critic round:** (#15) `accrueCourierTipEarning` skips (reason `refunded`) when the order is already fully refunded per the ledger (`refund` lines on the order's PI summing to the charge) — a tip must never accrue on money the customer got back; (#14) the identity of §7 is also evaluated on **Stripe truth values** (real fee refunds) in the webhook suite, and `computeRefundExposure` names the `courierWithheld` slice next to the tip slice (both absorbed by Grubano on a partial); loyalty credit and promo are absent from the cash identity by construction (points rail / already netted in `T`).
- **Out of scope, untouched:** tickets/deposits stay on rail A (no Order row — `computeFranchiseRoyalty` is called only from the orders pay route). Claims (Phase 3) call `executeRefund` and inherit every fix; the only Phase 2 touch in `lib/claims.ts` is the `pending` variant handling (§8). Dispute rail unchanged (`lib/dispute.ts`). No schema change (`Refund.status` is a free String; the `idempotencyKey` suffix fits `String @unique`; no `Payout` field added). `REFUNDS_ENABLED` is **never** flipped by the agent.

## 13 · REFUNDS_ENABLED readiness — items outside the code (for the founder's decision, NOT done by the agent)

**Operational preflight status (2026-09-04, see `PHASE2-PREFLIGHT-ONE-COMMAND.md` and the closeout « PRÉFLIGHT OPÉRATIONNEL PHASE 2 »):** item 1 **DONE by Claude Code via the Stripe TEST API** (events read back from Stripe, `livemode=false`); the two §18 residuals classified **NON-BLOCKING** by an independent financial reviewer; real-signature routing suite `tests/webhook-refund-signature.test.ts` PASS (unsigned/wrong-secret/tampered → 400 before any handler; duplicate deliveries idempotent; unknown refund fail-safe); `ALERT_EMAIL`, flags, order facts, franchise reachability and the read-only ledger check are established by the one-command operator `scripts/server/phase2-preflight.js`. **Calendar gate discovered:** the Rehearsal restaurant's Connect TEST account has **0 € available** (two payments pending, available 2026-09-08 and 2026-09-09; payout schedule switched from `daily` to `manual` in TEST so cleared funds stay available) — Stripe reverses a transfer only against the connected account's **available** balance and a refund request carrying `reverse_transfer` **errors** otherwise → earliest rehearsal date 2026-09-08 (partial ≤ 13,34 €), full refund of GR-BZE1X from 2026-09-09.

1. ~~**Stripe TEST webhook endpoint `we_…7ueK` (app.grubano.com/api/webhooks/stripe): add the events `refund.updated` and `refund.failed`**~~ — **DONE 2026-09-04 (API, TEST, read back: `payment_intent.amount_capturable_updated, payment_intent.canceled, payment_intent.succeeded, charge.refunded, refund.updated, refund.failed`).** The same applies to the LIVE endpoint when Stripe LIVE is configured (much later).
2. **Franchise settlement scheduling:** `.github/workflows/cron.yml` runs from `main` only and the settlement is monthly → on staging the settlement is `workflow_dispatch`-only; the human refund rehearsal must trigger it by hand if it wants to observe `royaltyCents − refundedCents` being paid.
3. **Human paths that Phase 2 deliberately does not automate:** clawback of a settled royalty after an EXTERNAL refund; clawback of a settlement over-transfer detected by D-G; re-transfer after a FAILED refund (fail-closed lock). Each raises a `MONEY REVIEW` log + admin alert with the exact cents.

## 15 · Round 2 — BINDING AMENDMENTS (override any earlier sentence they contradict)

- **A1 (F1) Clawback >24 h backstop.** Before `transfers.createReversal`, `finalizeRefund` lists the settlement transfer's reversals (`transfers.listReversals(transferId, {limit:100})`) and **adopts** one whose `metadata.refundId === row.id` (recording its real `amount` as `royaltyClawbackCents`); only if none exists does it create (key `refund-claw:<rowId>`). Same for the dispute rail? — untouched (out of scope, gated OFF), recorded. Test: resume with a pruned key adopts, never doubles.
- **A2 (F2) Tip clawback predicate.** `isTotalRefund` = Σ amounts of the **succeeded** refunds ≥ `charge.amount` (never `charge.refunded` / `amount_refunded`, which include a pending refund that may fail). Test: pending full refund → tip rows untouched.
- **A3 (F3) Resume maps Stripe `failed|canceled`.** RESUME-FIRST retrieving a refund whose `status ∈ {failed, canceled}` performs the SAME transition as the `refund.failed` branch (row `failed`, `stripeRefundId` written, key suffixed `:failed:<re_id>`, `MONEY REVIEW` + alert) and returns non-ok 409 « reprise manuelle requise ». RESUME-FIRST is the only oracle until §13.1.
- **A4 (F4) Pending branch on both routes.** 202 `{status:'pending', refundId, stripeRefundId, amountCents}` (no `error` key), **plus** `recordAdminAudit('refund.run', {…, pending:true})` — an admin-created Stripe refund always leaves an audit trail. No email.
- **A5 (F5) D-G detection on every disbursement path.** The Σ live `netOwed` vs `p.amountCents` comparison + over-transfer alert runs on the fresh post-transfer path AND on both resume-adopt branches (`p.status==='paid'`, existing transfer in the group). Never a false positive: a delta can only come from a post-claim `refundedCents` rise.
- **A6 (F6) Alerts never silent.** Every new `MONEY REVIEW` helper `console.error`s the full payload BEFORE `sendOnce`, so a missing `ALERT_EMAIL` (→ `skipped`) still leaves the log line. §13 gains: « verify `ALERT_EMAIL` is set on staging `.env.local` and a test alert is received ».
- **A7 (F7) Claims on pending.** `triggerClaimRefund`: pending → claim stays `refunding` with `refundId`, **no** `approved`/`refundError` write (the current else-branch would make it un-retriable). Moving `refunding → refunded` when the refund later succeeds = **Phase 3 item** (recorded).
- **A8 (F8) Test flips added:** `tests/refund-engine.test.ts:64-65` fixtures gain `status:'succeeded'` (else every happy path becomes pending — that flip is itself a negative control for §8); `tests/franchise-settlement.test.ts:173` `amount_mismatch` → `amount_drift`.
- **A9 (F9) Engine origin asymmetry (documented):** the engine derives `Cprev` from `charge.amount_refunded` (conservative ceiling, includes a pending external refund); if that refund later fails, engine slices vs `royCum(Σ succeeded)` may differ by ≤ 1 rounding cent; the webhook `max(…)` never under-counts.
- **A10 (F10) Residual:** dispute reversal done but `splitReversed` not yet set while an engine refund runs (~100 ms) → `recovered` stale; bounded by A1's live reversal listing (Σ reversals on the transfer ≤ R). `CHARGEBACKS_ENABLED` OFF in beta.
- **A11 (F11) Fail-closed lock detail:** the failed transition always writes `stripeRefundId` (rows matched by `metadata.grubano_refund_row` included) so the lock engages; the released Stripe key returns the same failed refund ≤ 24 h — harmless only because of the lock.
- **A12 (F12) Tip accrual guard predicate:** `Mission.orderId → Order.stripePaymentIntentId → LedgerEntry` rows on that PI: fully refunded iff Σ(−gross of `refund` lines) ≥ gross of the `payment` line (integer ledger cents, never `Order.total` Float); **fail-closed** when the `payment` line is missing → `skipped:'ledger_unknown'` + log (tip stays held, no loss).
- **A13 (F13) §13 additions:** `GHOST_ORDER_AUTO_REFUND_ENABLED` must stay OFF; founder decision on the D-E default (courier keeps the whole tip on a partial); the ledger-check probe cron runs from `main` only → on staging run `GET /api/admin/ledger/check` manually after each rehearsal refund; the door-W rehearsal = one Stripe TEST Dashboard partial refund on a franchised order to observe D-B and the « settled royalty → alert, not money » path.

## 16 · Round 3 — BINDING AMENDMENTS

- **B1 (#2, #8 — P1) Pending-then-succeeded reconciliation.** `charge.refunded` fires at refund CREATION and never re-fires; a refund `pending` at creation that later succeeds would leave tip / loyalty / royalty (and an EXTERNAL Dashboard refund with no row) unreconciled. Amendment: the `refund.updated` / `refund.failed` branch, when the Refund's `status === 'succeeded'`, first `charges.retrieve(refund.charge)` and **re-runs `handleChargeRefunded(charge)`** (idempotent by construction: ledger unique keys, loyalty unique keys, royalty target, tip predicate on Σ succeeded), THEN finalizes the matching `Refund` row if any. Test: « pending-at-creation total refund → on `refund.updated` succeeded: tip clawed, loyalty reconciled, royalty target reached, ledger line written once ». **§13.1 is upgraded**: subscribing `refund.updated` + `refund.failed` on the Stripe endpoint is **REQUIRED before any Dashboard (door W) rehearsal or any `REFUNDS_ENABLED` decision** — RESUME-FIRST covers engine rows only.
- **B2 (#1) Fail-closed listing on resume.** On a RESUME, if the Stripe list used to adopt (refunds by `metadata.grubano_refund_row`, reversals by `metadata.refundId`) is **unavailable**, return 502 retryable and never create — a pruned key (>24 h) + a transient list error must not produce a second refund/reversal. This also corrects the pre-existing fall-through at `lib/refund.ts:237-239`.
- **B3 (#3)** `has_more` re-list failure in `handleChargeRefunded` → `MONEY REVIEW` log (tip/royalty may be under-reconciled until the next event — fail-safe direction).
- **B4 (#4)** Default mapping of any Stripe refund status ∉ {`succeeded`, `failed`, `canceled`} (incl. `requires_action`) → the non-ok `pending` variant. Never `ok:true` by default.
- **B5 (#5)** The fail-closed `failed`-row check runs **before** RESUME-FIRST in `executeRefund`; tested.
- **B6 (#6)** A pending refund finalized by the webhook sends no customer email (webhook imports no order-email sender — invariant); the admin re-calling the route (resume) sends it. Founder-visible, honesty not money.
- **B7 (#7)** Fresh path order: `transfers.create → finalizePaid → trace → D-G detection (try/catch, never throws)`.
- **B8 (#9–#11) §13 additions:** Stripe TEST connected-account available balance must cover the rehearsal's `reverse_transfer` (else the refund request itself errors — fail-safe but blocks); `CLAIMS_AUTO_APPROVE_ENABLED` must stay OFF (the only machine path into `executeRefund` besides the ghost flag); perf note `@@index([orderId,status])` on `Refund` for the lock query — **not** added in Phase 2 (no schema change), recorded.

## 17 · Round 4 — precisions (P2 only, folded in; §15/§16/§17 override any earlier sentence they contradict)

- **R4-1** `Refund.charge` is `string | Charge | null`, `Refund.status` is `string | null`: the `refund.*` branch derives the Charge via `paymentIntents.retrieve(refund.payment_intent, {expand:['latest_charge']})` when `charge` is not a string (and prefers that path anyway — R4-2), and a null status maps to the `pending` variant (B4).
- **R4-2** Charge metadata inheritance from the PI is established empirically (the live `charge.refunded` handler already reads `charge.metadata.restaurantId/orderId`; Golden Money proved the split from it) but has no docs FACT line: fallback — if `charge.metadata.restaurantId` is missing, read `pi.metadata` (expanded `latest_charge` path) before giving up `matched:false`.
- **R4-3** A retrieved Charge omits `refunds` by default (API ≥ 2022-11-15) → the handler's `refunds.list` path is the norm; an EMPTY list after a list failure is a silent no-op → `MONEY REVIEW` log + webhook **5xx** (Stripe retries), extending B3.
- **R4-4** A non-ok `finalizeRefund` inside the `refund.updated` branch (e.g. clawback 502) answers **5xx** so Stripe retries; the row stays `pending`. Money-safe either way.
- **R4-5** `locateSettlementTransfer` swallowing a list failure as « not found » (clawback skipped, row `succeeded`) is a **deliberate fail-safe** landing on the D-G over-transfer alert (human path §13.3) — recorded; not changed in Phase 2.
- **R4-6** §13.1 is REQUIRED (B1), not « slower ».

## 18 · Independent reviews of the IMPLEMENTATION (a1/refund-rail)

- **Security reviewer (independent, adversarial) — PASS**, no P0/P1; three P2 hardening items, **all implemented**: (P2-a) the webhook finalize path is **adopt-only** — `finalizeRefundRowFromStripe` → `driveRefund(…, adoptOnly)` never `refunds.create`s (the event proves the refund exists; not found → 503 retry); the royalty clawback reversal on that path remains (it completes an ADMIN-initiated refund, keyed `refund-claw:<rowId>`, adopted first) — recorded; (P2-b) `escHtml` also escapes `'`; (P2-c) a row named by Stripe metadata is finalized/failed **only if its order's PaymentIntent equals the event's** (`[refund_row_mismatch]` log, row ignored) and `markRefundRowFailed` refuses a row that already holds a different `re_…`. Confirmed: gate order unchanged on both routes; `refund.*` branch reachable only after signature verification; alerts deduped and log no secrets; Refund rows written only by the engine.
- **Financial reviewer (independent, adversarial) — PASS**, no P0/P1. Verified with numeric traces (T=5000, F=900, R=300): the franchise double-return is **closed on all three doors** — external 2500 → engine 2500 and engine 2500 → external 2500 both end at `refundedCents = 300` (settlement pays 0), settled variant recovers exactly R (150 engine clawback + 150 flagged human); no double movement, no zeroed clawback; status truth holds on every path (routes, claims, ghost-order); D-B fields identical engine/webhook; D-G v2 no false positive; fail-closed lock before resume; `REFUNDS_ENABLED` OFF still 403 on both initiation routes. Eight P2s — **implemented:** F1 (`prorata` fallback now `[MONEY REVIEW]`), F3 (step (d) `refundedCents` write wrapped → a DB blip no longer masks a completed refund as 500; MONEY REVIEW + webhook heals), F4 (`refund.failed` flips only a `pending` row; a succeeded→failed transition is a MONEY REVIEW alert, row untouched), F5 (dead duplicate `no_tip` line removed), F6 (tests added: admin route pending 202 + audit, claims pending variant), F7 (tautological asserts relabelled as documentation / replaced by fixture-evaluated controls). **Recorded residuals:** F2 (an EXTERNAL refund issued WITHOUT `refund_application_fee` → prorata attributes a phantom fee-back in the ledger — ledger truth only, no money; detection via `refund.transfer_reversal` not implemented, now surfaced by the `[fee_attribution_prorata]` MONEY REVIEW), F5 (tip guard `ledger_unknown` = held tip on legacy pre-ledger orders, log only), F8 (engine RESUME with an *available but empty* list still creates — correct when the first create never reached Stripe; the webhook path is adopt-only).

## 19 · FINAL HARDENING (2026-09-04, `a1/refund-hardening`) — F2 and F8 CLOSED, evidence rule

- **Evidence rule (mandatory from now on):** every runtime/config/financial fact in a verdict carries `SOURCE` (documentation · code · tests · staging runtime · database · Stripe · email provider — layers never collapsed), `OBSERVED` (the value read, or `NOT MEASURED`), `REQUIRED`, `STATUS`. A code default is never a staging fact until read on the server. **Incident recorded:** `REFUNDS_ENABLED` was reported `FALSE` in verdict blocks while the measured staging value was `true` (historical constant since ≤ 2026-08-29); no refund occurred (Stripe TEST: only the Z1 refund of 2026-08-29; DB measured by the operator v2). The **technical** refund freeze is restored by the operator v2 (`REFUNDS_ENABLED=false` written and re-read); the **operational** freeze (no Dashboard / admin / API / manual refund) stays in force until an explicit rehearsal authorisation.
- **F2 — CLOSED.** Founder rule: Stripe ACTUAL financial truth == Grubano ledger truth. `lib/refund-fee-truth.ts` rewritten: modes `none` (no fee refund → 0 everywhere), `stripe` (unambiguous index match), `matched` (each fee refund attributed to the earliest unassigned refund created at/before it whose proration — naive or cumulative — is within ±1 c; refunds without one book 0), `residual` (a fee refund matching nothing is still booked on the latest refund at/before it so Σ == Stripe Σ, flagged). `refundLedgerLine({amount, reversal, feeRefund})`: `net = −(reversal − feeRefund)`, `fee = −(amount − reversal + feeRefund)`, `gross = fee + net`. The webhook ALWAYS re-lists refunds with `transfer_reversal` expanded and books the ACTUAL reversal (0 when none — the platform bore it — `[refund_without_reverse_transfer]` MONEY REVIEW); the proration survives only as `EXPECTED (proration, not booked)` in the `[fee_attribution_<mode>]` log. The engine's eager line is written only on exact truth (`stripe`/`none`) and with the actual reversal (`recordRefundLedgerEntry.reversalCents`). Royalty note (residual, FRANCHISE OFF): `refundedCents` follows Σ succeeded refunds regardless of who funded the refund — recorded for the franchise train.
- **F8 — CLOSED.** Founder rule: one intended refund action ⇒ at most one Stripe refund economic effect. `driveRefund` on RESUME: `has_more` → fail-closed 502; tagged refund → adopted; otherwise the create is re-sent **only inside `RESUME_CREATE_WINDOW_MS` (20 h from `Refund.createdAt`)** — Stripe holds the original idempotency key ≥ 24 h, so the same key returns the same refund even if the list omitted it; **after the window → `ResumeIdempotencyExpired` → 409 fail-closed + MONEY REVIEW, never a create** (human reconciliation). Webhook path stays adopt-only. Matrix in `tests/refund-engine.test.ts` (e-bis): lost response, timeout, crash after acceptance, list omission, retry before/after the window, admin replay, partial headroom, truncated list, two distinct partials — at most one create per intended action, one key; negative control: the pre-hardening age-blind create is rejected.
- **Independent financial review of F2/F8 — PASS** (`F2 VERDICT: FIXED`, `F8 VERDICT: FIXED`, `DUPLICATE MONEY MOVEMENT POSSIBLE: NO`). Verified: `refundLedgerLine` signs on four traces; the F8 matrix (11 cases) yields at most one create and one idempotency key per intended action; concurrent webhook-finalize ∥ engine-resume converge on one reversal (`refund-claw:<rowId>`); distinct partials get distinct cursor keys. Findings **implemented in the same branch**: #10 (P1) a FAILED `applicationFees.listRefunds` no longer books 0 — it fails closed (503 + MONEY REVIEW, Stripe redelivers); #12 (P1, franchise rail gated OFF) the clawback resume now has the same `has_more` + idempotency-window guards as the refund create (`ResumeIdempotencyExpired` → 409, never a second reversal); #2 the fast path index-matches only when every pair is within ±1 c of Stripe's proration (else general attribution / residual); #3 an unexpanded `transfer_reversal` id (payload fallback) is logged `[reversal_unexpanded]` — the inferred amount is exact because `transfer_data[amount]` is never used in this codebase; #11 the engine's eager line defers to the webhook when its own routed refund shows no fee refund (read lag ≠ truth 0); #9 the vacuous cumulative test replaced by a real fast-path attribution control. **Recorded, not changed:** #5 an external refund funded by the platform (no fee refund) still raises `refundedCents` (franchisor paid on net-of-refund sales — a distribution decision for the franchise train, FRANCHISE OFF); #13 the human procedure after a `[resume_idempotency_expired]` (verify in the Stripe Dashboard that no refund exists for the order before any re-issue; a re-issue needs a new cursor because the pending row holds `refund:<orderId>:<already>`) is documented in the closeout runbook and must be executed by a human with the Dashboard open — never by re-running the engine blindly. Per-line fee attribution can still be imperfect while Σ is exact (ledger lines only, same restaurant, same charge — never money).

- **Preflight corrective pass (v3, 2026-09-04).** Evidence rule applied to the freeze itself: `REFUNDS_ENABLED` FILE = false (measured by the v2 operator) but RUNNING PROCESS = true (measured from outside: the refund routes answer 401 to an unauthenticated POST; 403 `{gated:true}` would mean false — the kill-switch precedes auth). The v2 run never restarted (ledger check 401 aborted step 9). Root-cause class: two parsers of `.env.local` — the runtime loader is strict (`server.js`: canonical `KEY=value` lines, first occurrence wins) while the v2 operator was lenient (last occurrence, trimmed) — explaining both the TIPS_ENABLED discrepancy (file "true" for v2 / `tipsEnabled:false` live / strict read 2026-08-30 OFF) and the 401 (the process compares a token value the file line does not deliver to it). v3 restarts before every read-only check, proves the reload with live probes (`403 {gated}` = the process reads false), sends the token as the runtime loads it, and never touches route auth. Founder scope corrections recorded: courier WAITLIST is IN beta (`LOGISTICS_SIGNUP_ENABLED=true`), operational delivery OUT (`LOGISTICS_COURIER_ACTIVATION_ENABLED` never enabled); `ALERT_EMAIL` = `m.maazouz@grubano.com` (measured previous value, monitored mailbox). Technical freeze = NOT proven until the post-restart probe returns `403 {gated}`; operational freeze ACTIVE; no refund; no Stripe LIVE.

- **Final preflight (v4, 2026-09-05) — ledger reconciliation decoupled from the internal HTTP auth.** The reconciliation of `GET /api/admin/ledger/check` is extracted UNCHANGED into `lib/ledger-check-core.js` (pure, shipped) and executed directly on the server by the read-only operator (two windows, `FINANCIAL LEDGER RECONCILIATION = PASS/FAIL`, never forced: missing Stripe object / extra ledger line / duplicate refund line / broken equation / sum mismatch all FAIL; unavailable refund listing = NOT MEASURED). The HTTP 401 is a SEPARATE verdict (`INTERNAL LEDGER HTTP AUTH`), diagnosed by a value-free provenance snapshot taken in the deployed entry before Next loads any env file (hosting-injected token that differs from the file is the measured-or-refuted hypothesis; Next never overrides a pre-existing process.env). Loader truth corrected: `@next/env` (dotenv) — not the repo-root `server.js`. Route auth contract untouched. Contract: one runtime secret source per environment + one synchronised GitHub secret + one rotation procedure (`RUNTIME-SECRET-SOURCE-MATRIX.md`). Freeze intact; no refund; Stripe LIVE untouched.

## 14 · Adversarial critic — round 1 (INCOMPLETE → resolutions) · round 2 (INCOMPLETE F1–F6 → §15) · round 3 (INCOMPLETE #2/#8 → §16) · **round 4 = PASS** (no P0/P1; P2 → §17). **PHASE 0 GATE = PASS → implementation authorised on `a1/refund-rail`.**
Round 1 (P0 #1, P1 #2–#8, P2 #9–#17): #1 → D-H v2 (§10.B); #2 → Σ succeeded refunds (§5); #3 → D-G v2 detection-only (§10.A); #4 → non-ok `pending` variant (§8); #5 → §13.1 readiness + §13.2; #6 → controls (ii) (§11); #7 → test-flip inventory (§11); #8 → tolerance ≤ 1 c + re-list (§8); #9 → ±1 c cross-rail tolerance stated (§5); #10 → D-G v2 post-transfer detection (§10.A); #11 → webhook backstop stated (§5); #12 → matching order (§8); #13 → permanent explicit lock (§8); #14 → §12; #15 → §12; #16 → controls (iii); #17 → resumed message (§8).

## 20 · T-49 round 13 — J-M24 exactly-one binding rehearsal on a real database (C10, binding rule 7)

Run 2026-09-12 (slice W4, fixer round 1), before any window. Nothing in this run touched staging, production or Stripe.

- **Server:** MariaDB **12.3.2** (`mariadbd.exe --version` → `Ver 12.3.2-MariaDB for Win64 on AMD64`), the o2switch major version. Disposable instance: a fresh datadir initialised with `mariadb-install-db` in a session scratch directory, bound to `127.0.0.1:3310`, database `claims_race` created by `prisma db push` (Prisma 5.22.0), stopped after the run. Server variables were not read (no raw SQL): the settings are those of a fresh install.
- **Target guard (always-run test):** loopback host only; refuses the o2switch account prefix (a hosted database reached through a tunnel), any database name that is not `claims_race…`, and any URL or database name equal to a `DATABASE_URL*` of `process.env`, `.env.local` or `.env`.
- **Command:** `CLAIMS_RACE_DATABASE_URL=mysql://root@127.0.0.1:3310/claims_race CLAIMS_RACE_NEGATIVE=1 npx vitest run tests/claims-attribution-race.db.test.ts` → exit 0, 4/4 tests.
- **Invariant, 20 iterations** (two PrismaClient connections, both `attributeWithEvidence` calls released by one barrier; one unstamped succeeded row R; two FV claims on its order): exactly one claim `{refunded, refundId R}` in **20/20**; the other unchanged (FV, original refundError) in 20/20; the loser never ok. Winners: claim 1 ×7, claim 2 ×13. Loser answer ×20: 409 « La liaison n’a pas pu être enregistrée (écriture concurrente ou erreur de la base) — rien n’a été écrit. Relisez sa ligne dans la file, puis réessayez. ». Logged abort codes: `P2034` ×19, `PrismaClientUnknownRequestError` (no code) ×1 — both caught by C7 and answered from the claim re-read.
- **Negative control** (`CLAIMS_RACE_NEGATIVE=1`, isolationLevel omitted = the server default REPEATABLE READ): R bound to **both** claims in **20/20** iterations. Serializable is required.
- **Break/restore:** the binder read moved out of the transaction (`db.claim.findFirst` instead of `tx.claim.findFirst`) → the rehearsal fails at iteration 0 with two refunded claims; `lib/claims.ts` restored byte-identical (sha256 checked) to the version that passed.
- **Residual:** a local Windows build with fresh-install settings. The o2switch server's `innodb_deadlock_detect`, `innodb_snapshot_isolation` and lock-wait settings were not read. Per the ARCHITECTURE DECISION, every non-deadlock outcome (P2028, 1020, both sides timing out) is a full rollback, which C7 reports as nothing written after the re-read.
- **Re-run on commit `d555f6f`** (slice W4 final tree, run by the orchestrator): a local disposable MariaDB **12.3.2**, server default isolation REPEATABLE-READ; `tests/claims-attribution-race.db.test.ts` with `CLAIMS_RACE_NEGATIVE=1`. Invariant: in **20/20** iterations exactly one claim was refunded and bound and the other stayed unchanged (`financial_verification`, original `refundError`); the loser was always a 409 « La liaison n’a pas pu être enregistrée (écriture concurrente ou erreur de la base) — rien n’a été écrit. Relisez sa ligne dans la file, puis réessayez. » (winners 9 / 11, as reported). Negative control (isolation level omitted): R bound to **both** claims in **20/20** iterations.
- **Re-run before any Claims window (binding, §24):** this rehearsal is re-run on the final certified release-candidate SHA before any Claims window opens on any environment, and recorded here with that SHA, the server version and the counts. It never targets staging or production: the always-run target guard refuses a non-loopback host, the o2switch account prefix, a database name that is not `claims_race…`, and any URL or database name equal to a `DATABASE_URL*` of `process.env`, `.env.local` or `.env`.

## 21 · T-49 round 13 — AMF-1: reversal of a settled refund whose failure event was lost (E-09)

`reverifySettledClaimRefunds({ lookbackDays = 35, take = 100 })` (lib/claims.ts) re-reads, read-only toward Stripe, the refunded claims settled in the last `lookbackDays` whose bound row (own order) is `succeeded` or `pending`, and marks — claim only (G11) — the ones whose refund Stripe now reports `failed` or `canceled`, with the I-01 alert and the audit `claim.reconcile_evidence {moneyMoved: false}`. It never calls the engine, never writes to Stripe, never writes a Refund row and sends no customer e-mail. `recoverStrandedClaimReconciliations` runs it after its stranded pass: the daily cron call in production, and `POST /api/admin/claims/reconcile-refunds` as an admin on demand (staging has no cron).

- **Detection:** at the next pass (daily in production, on demand elsewhere).
- **Visibility:** the pass summary (`settledReverify: { checked, reverted, standing, unreadable, unproven, truncated }`) and the I-01 alert at marking.
- **Residual:** a failure that Stripe reports more than `lookbackDays` after settlement, and whose event was lost. Such a claim keeps reading « Remboursée » and appears in no list, count or alert; no money moves from it (the claim is terminal, the engine refuses at E2 / E6, and later claims on the order meet T2 H1).
- **Mode-A precheck:** before any window, the operator runs the re-verification on staging and records `settledReverify.reverted = 0` (procedure: `docs/ops/CLAIMS-R13-OPERATOR-PRECHECK.md`).

## 22 · T-49 round 13 — F11: customer copy that stays wrong, disclosed (REG-7, E-08)

Copy cannot fix the two states below. They are stated here and in the founder report; they are never presented as compliant.

- **A-S31e-1 / A-S31e-2 (REG-7, E-09):** a refunded claim on a `succeeded` bound row whose Stripe failure event was never processed keeps reading « Remboursée » after Stripe reversed the refund. Until the next re-verification pass this state is NOT fail-visible: before that pass no list, card, count or alert shows it, and the census cannot count it without a Stripe read. It needs explicit founder acceptance. Since AMF-1 (§21) the re-verification pass detects it within `lookbackDays` of settlement (it marks the claim and sends the I-01 alert); beyond that window the residual stays NOT fail-visible (no list, card, count or alert shows it) and still needs founder acceptance.
- **A-S31f-2 / A-S31f-3 (E-08):** a failure event was received for the succeeded bound row of a refunded claim, and the claim marking failed (the webhook answers 503). The customer keeps reading « Remboursée » although a failure signal was received. This is a C6 breach, bounded by Stripe redelivery: live mode retries for up to 3 days; test mode, where the beta runs, retries only a few times over a few hours. The I-05 alert (with claimIds) is sent before the marking, and AMF-1 backstops an exhausted redelivery.
- A-S31d (a pending bound row, no failure signal read) and A-S10 / A-S21 (a pending bound row, the Stripe read succeeded) are not in this list: the code has read no failure for them.
- No money moves from either state: the claim is terminal, the engine refuses at E2 / E6, and later claims on the order meet T2 H1.

## 23 · T-49 round 13 — F09 caveat and the closure-notice residuals (H10, stated)

- **F09:** the customer copy « Le restaurant l’examine » (claims.client.success and description, 5 locales) is true only while `CLAIM_AUTO_RESOLVE_ENABLED` is off. Reword it before that flag opens.
- **Closure notices (H05–H08):** closure e-mails go out only from arbitrate (`refuse_final`, whose decision e-mail is the closure notice of kinds `refused_confirmed` and `refused_by_grubano`, and the engine's CAS-won `refunded` result), resolve-stuck, reconcile (outcome `refunded`), attribute (an observed `refunded` commit), the per-claim resend `POST /api/admin/claims/[id]/closure-notice`, and the `auto_small` path of `POST /api/claims` (`sendClaimDecisionEmail` with decision `refunded` when `autoResolveSmallClaim` returns `refunded`, after triggerClaimRefund's T4 compare-and-set wrote the closure record; behind `CLAIM_AUTO_RESOLVE_ENABLED`, off in the beta — W8 correction). The closure-notice sender acts only for a claim closed by this build (its `claim_closure_record`). A notice is SENT only while claims are open; an attempt made while they are closed sends nothing and is traced as `claims_disabled` (EmailLog `(non envoyé : claims_disabled)`). The webhook, the recovery sweep, a cron and a reversal send none (H09). Two residuals: a dispatch row left by a crash mid-send makes the resend answer `duplicate`; a record-write failure makes the claim unsendable (it is counted in the census `closure.terminalWithoutRecord`).
- **W6 → W7 ordering (verifiable since W7):** the « Avis client non envoyés » section, its « Envoyer l’avis au client » button and `counts.closureNoticesMissing` (H10) landed with the console slice W7 (commit `4d3e442`), and the frozen `stripeNotConfirmed` toast names that button. No CLAIMS lease opens on an environment whose deployed build does not contain that commit: its `version.json` `commit` has `4d3e442` as an ancestor, and `GET /api/admin/claims/financial-verification` carries `counts.closureNoticesMissing` (`docs/ops/CLAIMS-R13-OPERATOR-PRECHECK.md`, Étape 0). The section itself renders only while a notice is missing or its list is unreadable.

## 24 · Résidus round 13 (C11, binding rule 14, J-M27)

The Claims-side quiescence rules replace the withdrawn engine guard (ARCHITECTURE DECISION, « Engine guard disposition »). They leave these residuals, stated verbatim here, in C11, and — R1 to R4 — in the skip reasons of the stalled-attempt race test (`tests/claims-r13-stale-attempt.test.ts`; `tests/claims-r13-residuals.test.ts` compares the three):

- **(R1)** a same-claim double refund when an attempt's pre-insert path (refund.ts 724-808) stays suspended longer than ATTEMPT_QUIESCENCE_MS while a proof and a re-approval happen;
- **(R2)** a refund made outside the system during a stalled attempt: Dashboard, admin rail, or attribution of an unstamped row;
- **(R3)** a Dashboard refund landing between T2(f)'s reads and the engine's charge read (refund.ts 755);
- **(R4)** on a routed payment, E2 passed just before a concurrent markRefundRowFailed, which can reverse the restaurant transfer twice;
- **(R5)** an engine key collision is not guaranteed when the charge cursor moved through something other than an engine create between two attempts' charge reads: a Dashboard refund, a failure renamed by markRefundRowFailed, or an intermediate engine create whose own charge read followed such a move (verifier A P3).

- **Engine guard:** the engine guard (`exclusiveReason`) is recorded here as a founder decision for a later round, not part of round 13: `lib/refund.ts` stays byte-identical, with no `exclusiveReason` input and no E5b.
- **Real-database rehearsal (C10, J-M24):** on a local disposable MariaDB 12.3.2 (the o2switch major version), with two real connections, 20/20 iterations ended with exactly one claim refunded and bound and the other unchanged; the negative control (isolation level omitted, server default REPEATABLE READ) bound the Refund to both claims in 20/20 (§20: the W4 fixer run and the `d555f6f` re-run). This rehearsal is re-run on the final certified release-candidate SHA before any Claims window, and it never targets staging or production (the target guard, §20).
- **Stated elsewhere:** the settled reversal beyond `lookbackDays` (§21, AMF-1); the failure-event window bounded by Stripe redelivery (§22); the closure-notice residuals (§23); the W8 statements (§25).

## 25 · T-49 round 13 — W8 consistency sweep: residuals and deviations stated

- **J-M23 — two attributions of DIFFERENT rows:** the binder read inside the Serializable transaction scans claim rows (there is no index on `Claim.refundId`) and so share-locks every claim row it scans. Two concurrent attributions of different Refund rows can therefore deadlock as well: InnoDB rolls the victim back completely, the victim writes nothing and answers the C7 409 « La liaison n’a pas pu être enregistrée (écriture concurrente ou erreur de la base) — rien n’a été écrit. Relisez sa ligne dans la file, puis réessayez. », and a retry commits (`tests/claims-r13-attribution.test.ts`). No money moves from it.
- **AMF-1 — what `truncated` means:** `reverifySettledClaimRefunds` re-verifies at most `take` = 100 eligible claims (bound row on the claim's own order, `succeeded` or `pending`), oldest first (`decidedAt`, `createdAt`, `id`), reading the 35-day window in pages of 101 settled claims, up to 50 pages. `truncated = true` means the window held more than 100 eligible claims, or the page bound was reached; in the second case every eligible claim found was re-read (possibly fewer than 100), but settled claims past those pages were not examined. The pass has no cursor and its summary carries counts only (no claim id): each pass re-reads the 100 oldest eligible claims of that moment. While the claims already re-read stay eligible, a new pass re-reads the same ones and not the more recent ones; while more than 100 eligible claims remain in the window (or the page bound is reached again), `truncated` stays true, so relaunching does not lift it. A pass reaches more recent claims only once older ones leave the selection (marked after a reversal, or aged out of the window). The operator therefore verifies in the Stripe Dashboard the refund of every refunded claim settled in the window, at least those after the oldest 100 by `decidedAt` then `createdAt`; this residual stands for round 13 (the console toast and `docs/ops/CLAIMS-R13-OPERATOR-PRECHECK.md` Étape 2 say so).
- **Census — the two Track B definitions (I-06):** read from the Track B source (« §M Read-only census ») and implemented with the same predicate in `lib/claims-census.ts` and `scripts/server/phase2-claims-gate.js` (parity test in `tests/claims-t49-round13-census.test.ts`). `terminalDeclarationWithArbitrationReason` = terminal claims (`refunded`, `refused_final`) whose `refundError` is not null and whose `arbitrationReason` is not null; it has no registry entry and is reported for review. `refundedAfterContradictionAttribution` = `null` while admin audit is off; otherwise the `refunded` claims that carry both an AdminAuditLog `claim.attribute_refund` row and the EmailDispatch of the contradiction park alert (`admin_money_review_claim_financial_verification` / `claim_fv:<id>:stripe_refund_contradiction`). It is a LOWER BOUND: sendOnce releases the key of an alert that was not sent, and relabels into that reason sent no alert before round 13. A count above 0, or `null`, requires founder review (E-15) before a lease opens.
- **Census reads are unpaged:** `refundedBoundToFailedRow`, `refundedRowUnproven`, `ownRowResumeMismatch`, `refundedAfterContradictionAttribution`, `refundedBoundToOtherClaimStamp`, `rowsBoundToMultipleClaims` (one `claim.groupBy`), `pendingRowsOver20hWithSettledRoyalty` and `claims.closure` (`missing`, `terminalWithoutRecord`) each read their whole population with unpaged queries (one to three per field), with no page and no cap; `legacyPayableProofs`, `terminalDeclarationWithArbitrationReason` and `approvedUnpaid` are single `count` queries. `scripts/server/phase2-claims-gate.js` reads the same populations the same way. That is sized for the beta volume; a read that throws or times out makes its field `null` (NOT MEASURED), never 0.
- **H10 — id-cursor paging of `listRefundedClaimsWithUnprovenRow`:** pages of 500 follow an id cursor with `skip: 1` over `{status refunded, refundError null}`. When the claim that ended a page leaves that set before the next page is read, Prisma still places the next page after that claim's position, and `skip: 1` then skips the first matching claim instead of the cursor row: that one claim is neither listed nor counted in that load (the heading total can be one short per such event), and the next load lists it. No money moves from it. `listMissingClaimClosureNotices` pages the same way without this residual: a `claim_closure_record` dispatch row never leaves its filter.
- **H05 site 2 — the noNoticeSource console line:** reworded in W8 so it names every path that reaches it: « settled on its refund row by a path that sends no customer notice (the Stripe webhook, the recovery sweep, or a reconciliation run for another claim bound to the same row) ». In that last case the reconcile route ran for claim A, `reconcileClaimForRefund` settled claim B on the shared row and logs this line for B, and A is parked in financial verification, not settled. Log wording only.
- **W7 deviations, recorded:** (1) AMF-1 — the « Revérifier les remboursements soldés (35 jours) » button is rendered at the top of the « Vérification financière requise » card whatever the queue holds (a settled claim to re-verify is in no list); (2) F14 — the reconcile toast of `reverted_after_refund` keeps G10's text (R0a's evidence may be our failed row, not a Stripe read); (3) F13 — `claims.admin.approvedResumeMismatch` keeps « Ne relancez aucun remboursement. » (only its section-naming clause is deleted, so the never-retry instruction stays); (4) H10 — a claim whose bound row has two or more binders gets the blocker `refunded_row_ambiguous` (« Non envoyable : la ligne de remboursement liée est liée à plusieurs réclamations — rien ne peut être annoncé au client pour aucune d’elles. … »), which H10 did not list.
- **H17:** the paid-cancellation e-mail sent while claims are closed (`claimEmails.orderCancelledPaidOff.body`, 5 locales) no longer says that every request is handled by a team member during the beta — a process the code cannot establish.
- **Rule coverage:** `docs/ops/CLAIMS-R13-RULE-COVERAGE.md` has one row per rule id of sections A–J of the frozen specification (315 rows). Each row gives the implementing code, message or doc site, the test files that pin the rule, and where its residual or debt is stated. Two rows are NOTE-ONLY, with no test file of their own: H18, which is mapped by its note to the J tests of each item, and J-C19, which is a build / i18n gate. `tests/claims-r13-residuals.test.ts` checks that every row resolves.
- **J-M28:** the approve path runs through the real `executeRefund` on every J-M01 state and every D1 pre-image (`tests/claims-r13-no-false-exit.test.ts`): the engine is reached only where approve is offered, it then accepts and creates exactly once, and every rendered text passes the phrase and money-truth checks.

## 26 · D′ L5 — the financial rail rehearsed on a real database, in TWO PROCESSES (S-08, S-09)

Run 2026-09-23, before any window and before the L5 commit. Nothing in this run touched staging, production or Stripe: the refund engine and the Stripe client are replaced at BUNDLE time (`tests/support/race-stubs/*`), so the rehearsal has no key, no client and no request — the « engine » only inserts into the disposable database the refund row the real one would insert.

- **Why two processes and not two connections.** §20's rehearsal proved exactly-one BINDING with two `PrismaClient` instances in one process. The L5 invariants are about two *rails* — two HTTP requests, two node processes on a shared host — so each contender here is a separate operating-system process running an esbuild bundle of `tests/support/pay-race-child.ts` with its own Prisma client. Nothing is shared but the database and one append-only log file; the code under test is the product's own `lib/claims` (`triggerClaimRefund` with its whole T1/T2/T4, `withdrawClaimApproval` with its transaction), never a re-implementation.
- **Server:** MariaDB **12.3.2** (`mariadbd.exe --version` → `Ver 12.3.2-MariaDB for Win64 on AMD64`), the o2switch major version; a fresh datadir initialised with `mariadb-install-db`, bound to `127.0.0.1:3310`, database `claims_race` created by `prisma db push` (Prisma 5.22.0), stopped after the run. Server variables were not read (no raw SQL): fresh-install settings.
- **Target guard:** the SAME function §20 uses, now in `tests/support/rehearsal-target.ts` and imported by both rehearsals — one definition of « never point this at real data ». It refuses a non-loopback host, the o2switch account prefix (a hosted database behind a tunnel), any database name that is not `claims_race…`, and any URL or database name equal to a `DATABASE_URL*` of `process.env`, `.env.local` or `.env`. Its always-run tests stay in the file that first needed it.
- **Command:** `CLAIMS_RACE_DATABASE_URL=mysql://root@127.0.0.1:3310/claims_race npx vitest run tests/claims-pay-race.db.test.ts` → exit 0.

**A — S-08, two rails on ONE claim, 20 iterations.** Both processes released by one absolute instant, each calling `triggerClaimRefund` on the same payable claim. In **20/20**: exactly **one** engine call across the two processes (counted in a log both append to, not inferred from either), exactly one `refunded` answer and exactly one `already_handled`, the claim `refunded` and bound to the single refund row carrying `claim:<id>`. Winners: process A ×9, process B ×11 — the test ASSERTS that both processes won at least once, because « exactly one winner » would also hold if one contender always started first.

**B — S-09, a withdrawal against the rail, 20 iterations.** One process pays, the other calls `withdrawClaimApproval`. In **20/20** exactly one won, and the test ASSERTS that each side won at least once — « exactly one winner » would also hold if the withdrawal never worked at all. When the rail won (×5) the claim is `refunded`, the engine was called once, the withdrawal answered a 409 and the paid amount was left in place; when the withdrawal won (×15) the claim is back in `arbitration` with `approvedAmountCents` and `arbitrationDecision` null, **zero** engine calls, **zero** refund rows, and exactly one `AdminAuditLog` row — the reversal and its audit are one transaction (S-30). Never both, never neither.

**C — negative control.** A SYNTHETIC broken rail (read the claim, then write by primary key — the shape the compare-and-swap exists to forbid) run through the same harness. Both contenders hold after their read until the same absolute instant, so both are guaranteed to have read before either writes: a control that only sometimes reproduces the defect is not a control. It produced more than one engine call on one claim and the harness caught it. Without this, A and B would prove nothing.

**Residual, stated.** A local Windows build with fresh-install settings; the o2switch server's `innodb_deadlock_detect`, `innodb_snapshot_isolation` and lock-wait settings were not read. The rehearsal is re-run on the final certified release-candidate SHA before any Claims window opens on any environment, and recorded here with that SHA and the counts.
