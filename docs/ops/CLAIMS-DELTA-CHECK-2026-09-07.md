# CLAIMS — DELTA CHECK (2026-09-07) contre le rapport d'inventaire Claims

> **Programme** : CLAIMS DELTA CHECK — lecture seule, **aucune implémentation**, aucun refund, aucune mutation Stripe/DB/Claim/flag, aucun e-mail. Compare **HEAD `997e07a`** (develop) au rapport Claims existant ; ne retourne que les **deltas** et reconfirme les 4 trous.
> **Baseline retrouvée** : `docs/ops/BETA-CLAIMS-REFUND-FACTUAL-INVENTORY.md` **§3 + §3.g + §8 P3** (commit `06685c8`, **2026-09-02**), banké dans l'inbox Notion le 2026-09-02 (« ⑤ Claims : rapport d'arbitrage — module bâti, rejoint le rail admin prouvé (executeRefund), 4 trous… Cible bêta = OFF »), **réaffirmé le 2026-09-04** par `REFUND-FINANCIAL-CONTRACT.md` (§A7 « Claims on pending » = item Phase 3 ; « Claims (Phase 3) call executeRefund and inherit every fix »). La date « 2026-09-04 » du brief correspond à cette réaffirmation ; le rapport à 4 trous lui-même date du 2026-09-02.
> **Méthode** : 1 agent diff (`git diff 06685c8..HEAD` sur le domaine Claims + moteur hérité), 12 agents de reconfirmation (une question chacun, preuves `fichier:ligne` verbatim à HEAD), 2 réfutateurs adversariaux par verdict (23 réfutations : 21 « non réfuté », 2 corrections de **libellé** sans changement de fond), agents plan + répétition + critique de complétude. Mesure runtime staging (POST vide non authentifié, la garde flag répond avant tout code) : `claims/auto-approve` → 403 `gated` ; `refunds/run` → 403 `gated` ; `GET /api/claims` → `{enabled:false}` ; `/fr/admin/claims` → 307 login. Staging sert `15bb7db` ; HEAD `997e07a` = +1 commit docs-only (`REFUND-REHEARSAL-RUNBOOK.md`) → code identique.

## 1 · Delta de code depuis la baseline (matériel vs cosmétique)

Commits touchant le périmètre depuis `06685c8` : `013a089`/`f585fe3` (Phase 1 fidélité, 09-02), `4ce8f53`/`2e8f640` (Phase 2 moteur refund, 09-04), `d25fc3b`/`ca0e19a` (P0 pré-pilote + hotfix véracité e-mails, 09-06). **26 fichiers Claims inchangés** (sécurité, zod, anti-IDOR, respond, contest, liste resto, GET admin, stale-alerts, UI).

| Fichier | Delta | Matériel ? |
| --- | --- | --- |
| `lib/claims.ts` (+21/−2) | **A7 (Phase 2)** : nouveau variant `{state:'pending', reason:'stripe_pending', refundId}` — quand `executeRefund` renvoie son nouveau résultat non-ok `pending` (Stripe a accepté, pas encore `succeeded`), la claim **reste `refunding`**, seuls `{refundId, refundError:null}` sont écrits (ni retour à `approved`, ni `refunded`). **Hotfix e-mails** : le variant `refunded` porte `amountCents` = montant **réel** du moteur. Claims passent toujours `amountCents: claim.requestedAmountCents` (:248) et **ignorent** `resumedIgnoredAmount` (la ligne Claim n'a aucun champ « montant réellement remboursé », seulement `refundId`). | **OUI** (machine à états, e-mails, montant) |
| `app/api/claims/route.ts` (+3/−1) · `app/api/admin/claims/[id]/arbitrate/route.ts` (+5/−1) | e-mail de décision = montant moteur (plus `requestedAmountCents`). Logique d'arbitrage/audit/réponse inchangée ; le variant `stripe_pending` est passé tel quel au client (`AdminClaimsArbitration.tsx` ne lit pas `refund.state`). | OUI (e-mails) |
| `lib/refund.ts` (moteur hérité par toute claim, raison `claim:<id>`) | **Phase 2** : vérité de statut Stripe (ligne `Refund` `succeeded` + ledger eager **seulement** si Stripe `succeeded` ; `failed/canceled` → `markRefundRowFailed` + alerte MONEY REVIEW) ; **verrou fail-closed** (409 sur toute nouvelle demande si une ligne `failed` avec `stripeRefundId` existe → claim `failed` + retour `approved` + `refundError`) ; RESUME-FIRST + `resumedIgnoredAmount` ; F8 (jamais de 2ᵉ création Stripe hors fenêtre 20 h) ; ledger eager depuis la vérité Stripe (fee réel) ; clawback royalty plafonné (D-H v2) ; `finalizeRefundRowFromStripe`. Flags inchangés (`REFUNDS_ENABLED` gate l'initiation, jamais la réconciliation). | OUI (argent, hérité) |
| `app/api/webhooks/stripe/route.ts` | oracle `refund.updated` / `refund.failed` → finalise/échoue la ligne **Refund** ; **aucune ligne Claim lue ni écrite** (0 hit). | OUI (vérité Refund) — Claim non touchée |
| `app/api/orders/[id]/refund/route.ts` · `app/api/admin/refunds/run/route.ts` | rail A commande → `executeRefund` (double-versement franchise fermé) ; `pending` → 202 sans e-mail + audit `pending:true`. | OUI (rails admin, même moteur) |
| `lib/refunds.ts` (`refundPayment`) | commentaire seul : réservé tickets / empreintes résa, **plus jamais sur le chemin commande**. | cosmétique |
| `lib/admin-alerts.ts` | `sendAdminMoneyReviewAlert` (idempotent) — un refund issu d'une claim qui échoue chez Stripe lève désormais une alerte. | signal seulement |
| `prisma/schema.prisma` | `model Claim` **inchangé** ; Phase 1 additive fidélité seulement (`recoveryOffsetPoints`, `sourceEventId`/`actorId`, `@@unique`). | non pour Claims |
| tests | `claims.test.ts` +1 (variant pending), `claim-emails-routes.test.ts` (montant moteur), `email-truthfulness-hotfix.test.ts` (gardes source). | test-only |

**MATERIAL DELTA SINCE BASELINE = YES** — confiné à (a) le comportement « Stripe pending » (A7), (b) les montants d'e-mails, (c) tout ce que les claims **héritent** du moteur Phase 2. Aucun changement de porte d'entrée, de sécurité, de zod, de scoping ni de machine à états hors A7.

## 2 · Reconfirmation (vérité à HEAD, preuves verbatim)

| Item | Vérité à HEAD `997e07a` | Changé depuis baseline |
| --- | --- | --- |
| **CANONICAL REFUND ENGINE** | **`executeRefund`** (`lib/refund.ts`, rail B royalty-aware). `lib/claims.ts:22` n'importe que `executeRefund, isRefundsEnabled` ; site unique `triggerClaimRefund` :246-250 derrière la garde `isRefundsEnabled()` :231 et le CAS :234-238 ; tous les chemins argent (approveClaim ← sweep `auto_timeout` :373 / `auto_small` :441 ; re-drive :390 ; arbitrate approve :527) y convergent. 4 appelants du moteur dans le repo (claims :246, webhook :458, admin run :79, orders/[id]/refund :79) ; `refundPayment` (rail A) = tickets/empreintes seulement. | NON (identité) ; le moteur lui-même a changé (Phase 2) |
| **RESTAURANT SILENCE HOLE** (trou 1) | **STILL PRESENT — P1.** Aucune route/fonction admin ne sort une claim de `restaurant_review` : `arbitrateClaim` refuse tout sauf `arbitration` ou legacy `approved+refundAttempted:false` (:491-494) ; `approveClaim` est privé (:280) et n'est appelé que par le sweep (`auto_timeout`, exige `CLAIMS_AUTO_APPROVE_ENABLED` = interdit) et `auto_small` ; `listPendingRestaurantClaims` = lecture ; `stale-alerts` = e-mail sans action ; les 10 écrivains `prisma.claim.*` sont tous dans `lib/claims.ts`. `activeOrderKey` reste posé → la **commande reste verrouillée** (nouvelle claim → 409). | NON |
| **ADMIN IMMEDIATE VISIBILITY** | **OUI, lecture seule, immédiate** : bloc ambre « En attente du restaurant » de `/admin/claims` via `GET /api/admin/claims` → `pending` (statut `restaurant_review`, plus ancien d'abord, **cap 200, sans pagination**, `force-dynamic`). Liste actionnable = `arbitration` OU `approved+refundAttempted:false`. **Invisibles à l'admin** : `refunding` (dont `stripe_pending`), `refunded`, `refused`, `refused_final`, `approved+refundAttempted:true`. Gaté **uniquement** par `CLAIMS_ENABLED` (OFF → `{enabled:false}` avant auth ; page → redirect) → **rien de visible aujourd'hui**. | NON |
| **CONSUMER REFUND AMOUNT AUTHORITY** | **Serveur autoritaire sur le plafond** — libellé de la baseline (« re-dérivé serveur ») **corrigé** par la réfutation : `POST /api/claims` accepte exactement `orderId`, `reason` (enum 5 valeurs), `description ≤1000`, `requestedAmountCents` (optionnel), `imageBase64+mediaType` ; **aucun champ article/quantité/prix/« remboursable »** n'existe. Le consommateur **choisit** `requestedAmountCents` (estimation article ou saisie libre) ; `createClaim` le borne à `[1, round(Order.total×100)]` depuis la DB (:139-143, :154) ; le moteur re-plafonne en direct contre Stripe (`charge.amount − amount_refunded`). Pas de régression. **L'autorité serveur par article/quantité (priorité 4 du brief) n'est pas modélisée** (à construire). | NON |
| **RESTAURANT REFUND AMOUNT AUTHORITY** | **Aucune** : `respond` = `action ∈ {accept, refuse}` + `reason ≤1000` ; `respondToClaim` ne lit ni n'écrit `requestedAmountCents` (select :306) ; accept = CAS `restaurant_review → arbitration`, `refund` toujours `null` ; refuse terminal sans argent ; rails admin 403 pour un rôle restaurant. | NON |
| **AUTO REFUND** | **UNREACHABLE** aujourd'hui, 4 chemins machine énumérés : (1) sweep `auto-approve` : rateLimit → `CLAIMS_ENABLED` 403 → `CLAIMS_AUTO_APPROVE_ENABLED` 403 → token cron/admin → `approveClaim('auto_timeout')` → `triggerClaimRefund` inerte sous `REFUNDS_ENABLED=false` (:231) ; (2) `auto_small` : `CLAIMS_ENABLED` + `CLAIM_AUTO_RESOLVE_ENABLED` + cap `>0` ; (3) claim système à l'annulation payée → `arbitration` **sans argent** ; (4) ghost-order webhook `GHOST_ORDER_AUTO_REFUND_ENABLED` — **indépendant** de CLAIMS/REFUNDS, fichier `false` (v5, 09-05), **aucune sonde publique** (non mesuré au runtime). | NON |
| **SAFETY ALLERGEN AUTO REFUND** | **NO** — aucune catégorie/sévérité/priorité sécurité alimentaire (`Claim.reason` = 5 valeurs `missing_item|wrong_order|quality|not_delivered|other` + `system_order_cancelled`, `schema:2130`, `lib/claims.ts:81`) ; toutes les files sont `createdAt asc`. **Trou 3 toujours ouvert.** | NON |
| **PENDING STRIPE TREATED AS SUCCESS** | **NO** — A7 : `pending` est `ok:false` (:252 non pris), branche `:263-269` écrit `{refundId, refundError:null}` sans `status` → claim reste `refunding`, aucun e-mail « remboursé ». **Résiduel P2** : rien ne fait jamais `refunding → refunded` quand Stripe réussit ensuite (`refund.updated` ne touche pas la Claim) — item Phase 3 enregistré (contrat §A7). | **OUI** (variant nouveau) |
| **DUPLICATE REFUND PROTECTION** | **Intacte + durcie** : CAS `refundAttempted` false→true en un `updateMany` (:234-238, count≠1 → `already_handled`) ; CAS d'arbitrage (:517-526) ; `activeOrderKey @unique` ; moteur : RESUME-FIRST + `idempotencyKey` + verrou fail-closed + fenêtre F8 ; webhook adopt-only. **Résiduel P2 (nouveau)** : classe « `refunding` bloquée » si la ligne Refund passe `failed` ensuite (`refund.failed` bascule la Refund, pas la Claim) — pas de double argent. | **OUI** (durci) |
| **CURRENT FLAGS** (règle SOURCE / OBSERVED / REQUIRED) | `CLAIMS_ENABLED` : OBSERVED **false** (403 gated ×2, `{enabled:false}`) · REQUIRED false. `REFUNDS_ENABLED` : OBSERVED **false** (`refunds/run` 403 gated) · REQUIRED false. `CLAIMS_AUTO_APPROVE_ENABLED` / `CLAIM_AUTO_RESOLVE_ENABLED` : **non mesurables séparément** (masqués par la garde CLAIMS) · SOURCE code défaut OFF · REQUIRED OFF. `GHOST_ORDER_AUTO_REFUND_ENABLED` : SOURCE fichier `false` (v5, 09-05) · OBSERVED **non mesurable** (pas de sonde publique). `ADMIN_AUDIT_ENABLED` / `RATE_LIMIT_ENABLED` / `INTERNAL_CRON_TOKEN` : inconnus depuis le repo. | — |

### 2.b · Les 4 trous d'origine

1. **Resto qui ignore → bloqué indéfiniment** : **OPEN, P1, inchangé** (voir ci-dessus).
2. **Échec moteur après `refundAttempted=true` → `approved+refundError` exclue de la file et du sweep, aucun re-drive** : **OPEN, périmètre changé** — le cas « Stripe pending » est extrait vers `refunding` (A7) et est **tout aussi non re-drivable** ; l'échec dur (:272-276) reste byte-identique et laisse `activeOrderKey` posé (commande verrouillée). P2 (P3 à la baseline ; injoignable au runtime tant que CLAIMS/REFUNDS sont OFF).
3. **Aucune taxonomie/sévérité sécurité alimentaire ni priorisation** : **OPEN, inchangé** (conception Phase 3).
4. **Label stale `RestaurantClaimsPanel`** (« Acceptée — remboursement en attente d'activation », clé `claims.restaurant.refundPending` ×5 locales, branche `data.refund?.state === 'pending'` :59) : **OPEN mais mort** — inatteignable car `respond` renvoie toujours `refund:null` ; nettoyage P2/cosmétique.

### 2.c · Nouveaux constats depuis la baseline

- **NEW P0 = aucun.**
- **NEW P1 (latent, sécurité)** : dans `POST /api/claims`, la **modération + upload Cloudinary de la photo s'exécutent AVANT** la vérification de propriété/éligibilité de la commande, **sans rate-limit** ni quota LLM (l'`operatorId` n'est pas passé) → coût/DoS authentifié dès `CLAIMS_ENABLED=true`. Injoignable aujourd'hui (403 gated). La bêta ne prévoit **pas** de photo → réordonner (propriété → éligibilité → photo) ou désactiver l'upload en bêta.
- **NEW P2** : classe « `refunding` bloquée » (A7) sans vue admin (les statuts `refunding`/`refunded`/`failed` sont invisibles de `/admin/claims`) ; la Claim ne stocke pas le montant réellement remboursé (`resumedIgnoredAmount` ignoré ; e-mail véridique via le moteur, mais la ligne DB ne porte que `requestedAmountCents`).
- **Sévérité des trous** : inchangée sauf le trou 2 (périmètre élargi par A7). Aucune régression de sécurité (26 fichiers inchangés ; anti-IDOR 404, 403 commande étrangère, zod, scoping resto par `restaurantIds`, audit admin conservés).

## 3 · Séquence d'implémentation minimale — APRÈS le PASS de la répétition refund Phase 2

Séquence minimale (agent plan, corrigée par le critique de complétude ; **aucun changement de schéma** dans les 9 étapes ; rail `executeRefund` préservé partout ; auto-refund jamais introduit ; pas de photo en bêta). Ordre = priorités du brief.

| # | Étape | Fichiers (réels) | Changement minimal | Tests | Décision fondateur |
| --- | --- | --- | --- | --- | --- |
| 0 | **Opérateur fenêtre `CLAIMS_ENABLED`** (préalable à toute répétition ; aucun script n'écrit ce flag aujourd'hui — `phase2-refund-gate.js` n'écrit que `REFUNDS_ENABLED`, `phase2-preflight.js` que `REFUNDS/LOGISTICS/ALERT_EMAIL` ; le fondateur n'édite jamais `.env.local`) | `scripts/server/claims-window.js` (nouveau, pattern `phase2-refund-gate.js` : flip + restart + sonde live `GET /api/claims` → `{enabled:true}` + **re-gel inconditionnel**) + preflight read-only prouvant l'existence de la table `Claim` sur staging (`prisma.claim.count().catch(()=>-1)`, déjà imprimé par `phase2-refund-gate.js:165`) | opérateur fail-closed, PASS/FAIL, 0 secret | tests de garde en sous-processus (pattern `clean-room-guard`) | D8 (logistique) |
| 1 | **Fermer le silence resto** (trou 1, P1) | `lib/claims.ts` (`arbitrateClaim` :485-530), `app/api/admin/claims/[id]/arbitrate/route.ts`, `components/claims/AdminClaimsArbitration.tsx`, `lib/admin-alerts.ts`, `messages/*.json` (`claims.admin.*`), `tests/claims*.test.ts` | 3ᵉ origine admissible dans `arbitrateClaim` : `status='restaurant_review'` si `responseDeadlineAt < now` (ou `override` explicite) ; CAS `{id, status:'restaurant_review', responseDeadlineAt:{lt:now}}` ; `decidedBy:'admin'` ; argent toujours et uniquement via `triggerClaimRefund → executeRefund` ; bouton « Trancher » sur le bloc ambre après échéance ; **corriger l'audit** `metadata.refunded = result.refund != null` (vrai pour `pending`/`failed`/`already_handled` → mensonge) par `refundOutcome: state` | (f) admin résout `restaurant_review` échue → CAS + `executeRefund` appelé 1× ; avant échéance sans override → 409 ; **contrôle négatif** : l'ancien 409 doit désormais ÉCHOUER ; inversion du verrou `claims-stale-alerts.test.ts:152-157` | D1 (échéance = `CLAIM_RESPONSE_HOURS` 24 h ?), D2 (résolution avant échéance ? motif obligatoire ? resto informé ?) |
| 2 | **Autorité de résolution admin visible** (trou 2 visibilité) | `lib/claims.ts` (`listStuckClaimRefunds`), `app/api/admin/claims/route.ts`, `AdminClaimsArbitration.tsx`, `lib/admin-overview.ts:69` | liste read-only `refunding ∪ approved+refundAttempted:true` (cap 200) **avec le statut de la ligne `Refund`** par carte (`pending` = en vol, rien à relancer ; `failed`/absente = bloquée) ; badge `/admin` = ensemble actionnable (aujourd'hui `arbitration` seul) | WHERE exact, select sans PII, badge | D7 (définition du badge) |
| 3 | **Préserver le rail canonique** (garde-fou, 0 comportement) | `lib/claims.ts` en-tête (:3-6 **et** :13-18, stales : « accept déclenche le moteur / auto-approved by cron » = faux depuis P0-24), `tests/claims.test.ts` | tests d'invariants source : `lib/claims.ts` n'importe que `executeRefund, isRefundsEnabled` ; aucun `lib/refunds`/`refundPayment` dans `app/api/claims/**` + `app/api/admin/claims/**` ; chaque appel porte `reason: claim:<id>` | invariants source (pattern `claims-stale-alerts.test.ts:137-158`) | — |
| 4 | **Autorité serveur article / quantité / remboursabilité** | `lib/claims.ts` (`createClaim` :108-168, `getClaimEligibility` :188-217), `app/api/claims/route.ts` (:24-31), page help (:194-199), `ClaimSection.tsx` | `items: [{index, qty}]` optionnel → montant **dérivé** de `order.items` (Json `{itemId,name,qty,price}`), `qty > commandé` → 400, index inconnu → 400, plafond total conservé ; sans `items` = chemin actuel byte-identique | dérivation, bornes, contrôle négatif (montant client ignoré) | D3 (table éligibilité `status × reason`, montant libre conservé ?) |
| 5 | **Sécurité alimentaire / allergène : taxonomie + priorité, JAMAIS d'auto-refund** (trou 3) | `lib/claims.ts` (:81 `CLAIM_REASONS`, :424-441, :366-378, :563-574, :635-648), `lib/admin-alerts.ts` (`sendAdminSafetyClaimAlert`), `app/api/claims/route.ts`, `ClaimSection.tsx:15`, `AdminClaimsArbitration.tsx`, `messages/*.json` ×5 | 1 id de motif (libellé = D4) + `SAFETY_REASONS` ; `autoResolveSmallClaim` → `not_eligible` **avant** le plafond ; sweep exclut `SAFETY_REASONS` ; files triées sécurité d'abord ; alerte admin immédiate idempotente. **Ne PAS toucher `prisma/schema.prisma`** même pour un commentaire (piège déploiement schéma) | `tests/claims-safety.test.ts` : auto_small + cap 100000 + motif sécurité → jamais `executeRefund` (**contrôle négatif** : même claim motif `quality` → appelé) | D4 (id/libellés ×5, bypass `restaurant_review` ?, délai plus court ?) |
| 6 | **Idempotence / sécurité** (P1 latent) | `app/api/claims/route.ts` (:29-30, :46-51), `contest`, `respond`, `ClaimSection.tsx` (:16, :127-133, :210-215), tests routes | retirer le chemin photo en bêta (champs zod + bloc `processDishImage` ; `photoUrl:null` ; colonne conservée) ; `rateLimit` sur POST claims/contest/respond ; **idempotence de la branche legacy** : `approved+refundAttempted:false` est ré-admis par `arbitrateClaim` (:491) → une 2ᵉ décision réécrit l'arbitrage ou bascule en `refused_final` → verrouiller | photo jamais traitée ; rate-limit appelé ; 2ᵉ arbitrage → 409 | D6 (400 ou 201 sans photo), D11 (ré-arbitrage legacy interdit ?) |
| 7 | **Véracité e-mails / statuts** | `lib/claims.ts` (`reconcileClaimForRefundRow`, `redriveClaimRefund`), `lib/refund.ts` (points de finalisation), `app/api/webhooks/stripe/route.ts`, `app/api/admin/claims/[id]/redrive/route.ts` (nouveau), `lib/claim-emails.ts`, `RestaurantClaimsPanel.tsx:59`, `messages/*.json` | **7a** `refunding → refunded` / `failed` : brancher la réconciliation Claim **partout où une ligne `Refund` passe `succeeded`/`failed`** (finalisation moteur — y compris RESUME-FIRST depuis un autre appelant — et `refund.updated`/`refund.failed`), clé = `Claim.refundId = Refund.id`, **non gaté par `CLAIMS_ENABLED`** (le flag gate l'initiation, jamais la vérité Stripe établie — D10) ; **7b** re-drive admin d'une claim `approved+refundError` : vérifier `result.resumed` / ligne `pending` étrangère sur la commande (RESUME-FIRST reprend la **plus ancienne** ligne de la commande, pas celle de la claim — D12) ; **7c** copies : `refused_final` sur provenance sans refus resto (« a confirmé le refus » = faux), sous-chemin échec clawback (:527) → e-mail « approuvée » alors que le cash est remboursé, page help « sous 3–5 jours » (promesse non adossée — D13), e-mail annulation OFF quand déjà remboursé ; supprimer la branche morte `refundPending` | webhook → claim `refunded` ; row déjà `succeeded` → hook quand même ; re-drive sans 2ᵉ création Stripe ; copies | D5, D9, D10, D12, D13 |
| 8 | **Tests** | toute la surface claims + refund (`tests/claims*`, `claim-emails*`, `system-claim`, `p3-annulation-resto-payee`, `webhook-refund-reconciliation`, `refund-engine`, `refund-routes-gate`, `refunds-run-route`) | contrôles négatifs inversés (l'ancien 409 échoue ; auto_small sécurité), régression OFF byte-identique, build à froid, tsc baseline | — | — |
| 9 | **Répétition staging** (§4) | docs + opérateurs read-only | après PASS refund Phase 2, opérateur fenêtre CLAIMS, table `Claim` prouvée, session admin | matrice §4 | D8 |

**Hors périmètre (explicite)** : upload/modération photo ; e-mails resto ; base contractuelle CGV (fondateur) ; sweeps `CLAIMS_AUTO_APPROVE` / `CLAIM_AUTO_RESOLVE` (restent OFF, rendus inapplicables aux motifs sécurité) ; rail A (`refundPayment` tickets/empreintes) ; litiges ; réconciliation fidélité Phase 1 (intacte) ; pagination/caps 200 ; tout changement Prisma (le seul candidat `@@index([refundId])` est différé → pattern opérateur additif si jamais) ; production / `main` / Stripe LIVE.

**Décisions fondateur requises (jamais inventées)** : D1 échéance ; D2 résolution avant échéance / motif / information resto ; D3 critères de remboursabilité ; D4 taxonomie sécurité ; D5 copies refus final + échec Stripe post-approbation ; D6 photo ; D7 badge admin ; D8 logistique répétition ; D9 re-drive ≤ demandé ; **D10** couplage webhook non gaté par CLAIMS ; **D11** ré-arbitrage legacy ; **D12** ligne `Refund` pending étrangère ; **D13** promesse « 3–5 jours ».

## 4 · Plan de répétition Claims sur staging (préparé, NON exécuté)

**Blocages globaux aujourd'hui (tous scénarios)** : (1) **aucun opérateur ne peut poser `CLAIMS_ENABLED=true`** sur le processus staging (→ étape 0 du plan) ; (2) **table `Claim` sur staging = INCONNUE** (mesure : `phase2-refund-gate.js precheck` imprime `-1` si absente ; flipper sans table → 500 au 1ᵉʳ POST et annulation payée cassée) ; (3) **session admin staging = INCONNUE** et la route `arbitrate` n'a **pas** de chemin token (session seule) ; (4) **fixtures neuves obligatoires** : `GR-N5TSM0` réservée à la répétition refund et probablement hors fenêtre 48 h (ancrée sur `Order.updatedAt` — non mesurable depuis le repo), `GR-GBZE1X` réservée au remboursement total ; `activeOrderKey @unique` + curseur moteur par commande → **jamais partager une commande entre scénarios** ; (5) **séquence** : après le PASS refund Phase 2 (funding gate WAIT, disponible 0 c) et après le backup frais (`staging-backup.js` livré par ce commit).

**Préconditions mesurées, pas supposées** : `/version.json` = SHA attendu ×2 domaines ; `CLAIMS_ENABLED=true` (sonde `GET /api/claims` → `{enabled:true}`) ; `CLAIMS_AUTO_APPROVE_ENABLED`/`CLAIM_AUTO_RESOLVE_ENABLED` = OFF (mesurables seulement une fois CLAIMS ON : `auto-approve {}` → 403 `flag:'CLAIMS_AUTO_APPROVE_ENABLED'`) ; `REFUNDS_ENABLED` via la **fenêtre bornée** `phase2-refund-gate.js window` (phrase exacte, 15 min, re-gel inconditionnel) pour A/B/F/G/H seulement, OFF pour C/D/E ; `ADMIN_AUDIT_ENABLED=true` (sinon 0 ligne d'audit) ; 3 sessions (conso pilote-client@ app, resto pilote-resto@ business, **admin**) ; funding gate : `reverse_transfer` exige un solde **disponible** Connect ≥ reversement, sinon = scénario G1 ; observabilité read-only : `phase2-refund-gate.js precheck` AVANT/APRÈS chaque scénario, `rehearsal-verify.js order`, `phase2-preflight.js` (DB ↔ Stripe) ; **jamais** d'`imageBase64` (P1 latent) ; attribuer tout 429 au WAF hébergeur (Tiger Protect) vs `RATE_LIMIT_ENABLED` avant d'interpréter.

| Scén. | Fixture | Étapes (routes exactes) | Attendu | Contrôle négatif | Statut |
| --- | --- | --- | --- | --- | --- |
| **A** article manquant → remboursement **partiel** admin | commande payée retrait **neuve** F-A, ≥2 articles, `delivered` (ligne `earn` présente) | precheck AVANT ; `GET /api/claims?orderId` → `canClaim:true, maxRefundableCents:T` ; `POST /api/claims {orderId, reason:'missing_item', description, requestedAmountCents:A_c}` → 201 `restaurant_review` ; resto `POST /api/claims/[id]/respond {action:'accept'}` → `arbitration`, `refund:null` ; fenêtre REFUNDS ouverte ; admin `POST /api/admin/claims/[id]/arbitrate {decision:'approve'}` → 200 | claim `restaurant_review → arbitration → approved → refunding → refunded`, `activeOrderKey` null ; `Refund` `succeeded` `re_…`, `amountCents=A_c`, fee `round(F·A_c/T)`, reversement `A_c − fee` ; 1 `LedgerEntry` refund ; loyalty `earn_reversal = round(E·A_c/T)`, `refund = round(P·A_c/T)` ; audit `claim.arbitrate` ; e-mail `claim_decision_refunded` (montant moteur) ; webhook `refund.updated` sur ligne déjà `succeeded` → `finalized:null` (pas `noop:409`, réservé à la course) | `requestedAmountCents: T+1` → 400 ; conso non propriétaire → 403 ; `respond` en doublon → 409 | exécutable après levée des blocages + funding |
| **B** resto fermé → annulation payée → claim **système** → remboursement **total** | F-B payée, `received`/`preparing` (jamais `delivered` : annulation inatteignable depuis `delivered`) | precheck ; resto `PATCH /api/orders/[id]/status {status:'cancelled'}` → même transaction : commande `cancelled` + `Claim{reason:'system_order_cancelled', status:'arbitration', requestedAmountCents=T}` ; admin `arbitrate approve` | 1 seule Claim, sans `restaurant_review` ; `Refund` `amountCents=T`, fee = F, reversement `T−F` ; e-mails : variante ON annulation + alerte admin ; loyalty `earn_reversal 0`, `refund = P` | annuler une commande **impayée** → 200, 0 Claim, e-mail générique | exécutable (reversement plus grand → funding) |
| **C** silence resto → **admin peut-il résoudre ?** | F-C payée (REFUNDS OFF suffit) ; option `CLAIM_RESPONSE_HOURS=1` staging | `POST /api/claims` → 201 ; resto ne répond pas ; admin `GET /api/admin/claims` → dans `pending`, pas dans `claims` ; `arbitrate approve` → **409** ; `arbitrate refuse_final` → **409** ; `stale-alerts` après échéance → 1 e-mail idempotent ; `auto-approve {}` → 403 `gated` | claim reste `restaurant_review` **indéfiniment**, commande verrouillée ; bloc ambre sans bouton | après le fix (plan étape 1) : le même `arbitrate` → 200 = preuve que le 409 était la garde de statut | **BLOQUÉ comme test positif** (trou 1) — la répétition **prouve le trou** ; devient positif après l'étape 1 |
| **D** allergène / sécurité → priorité, **sans** auto-refund | F-D payée ; REFUNDS OFF | `POST /api/claims {reason:'quality', description:'ALLERGÈNE — …', requestedAmountCents:T}` → 201 identique à toute claim ; `GET /api/admin/claims` → tri `createdAt asc` seulement | comportement byte-identique à une claim ordinaire ; 0 Refund / 0 Stripe après échéance (auto flags OFF) ; seule alerte = stale | `reason:'allergen'` → 400 (enum) = preuve qu'aucune catégorie n'existe | **BLOQUÉ comme feature** (trou 3) — preuve d'absence seulement ; après l'étape 5 : tri sécurité d'abord + alerte immédiate + auto_small `not_eligible` |
| **E** doublon / replay | F-C (active), F-A (terminale), F-E (`received` + claim active) | 2ᵉ `POST /api/claims` même commande → 409 `P2002 activeOrderKey` ; `respond` ×2 → 2ᵉ 409 ; **arbitrage ×2 séquentiel sous REFUNDS OFF → aujourd'hui les deux passent** (branche legacy `approved+refundAttempted:false` ré-admise :491 — **D11**) ; `contest` hors fenêtre → 409 ; annulation payée d'une commande avec claim active → pas de 2ᵉ claim ; Stripe Dashboard **resend** `charge.refunded` sur F-A → ledger/loyalty/e-mail idempotents | ≤1 Claim active/commande ; `executeRefund` ≤1 par claim ; lignes `(re_,type)` uniques ; EmailLog 1 ligne par (trigger, claim) | même conso, **autre** commande → 201 ; `count(LedgerEntry re_)==1` après resend | exécutable REFUNDS OFF (+ resend Dashboard par le fondateur) ; **ne jamais** utiliser `refunds/run` comme « replay » (curseur cumul = 2ᵉ partiel légitime) |
| **F** Stripe **pending** → pas de succès, pas d'e-mail « émis » | F-F, claim en `arbitration`, fenêtre ouverte | si levier pending disponible : `arbitrate approve` → 200 `{claim:{status:'refunding', refundId}, refund:{state:'pending', reason:'stripe_pending'}}` ; sinon **fixture webhook signée** `refund.updated` (secret staging = fondateur) | claim `refunding`, `refundAttempted:true`, `refundError:null`, commande verrouillée ; `Refund` `pending` + `re_` ; 0 ledger / 0 loyalty ; e-mail `claim_decision_approved` seulement ; **claim invisible de `/admin/claims`** (P2) ; audit `refunded:true` trompeur (P2) | verrou unitaire `tests/claims.test.ts:143-155` ; inverser localement une attente = preuve que le banc peut échouer | **BLOQUÉ sur staging** : aucun levier Stripe TEST connu produisant un refund carte `pending` (un déficit de solde Connect **erreur**, ne pend pas) |
| **G** Stripe **failed** → fail-closed | G1 : F-G1, solde disponible < reversement (déterministe tant que 0 c) ; G2 : ligne `Refund` → `failed` | G1 : `arbitrate approve` → `stripe.refunds.create` **rejeté** → claim `approved+refundError`, `Refund` `pending` sans `re_`, 0 argent ; G2 : `refund.failed` → ligne `failed` + clé suffixée + MONEY REVIEW + verrou 409 sur toute nouvelle demande | 0 mouvement ; alerte admin ; e-mail `approved` seulement | spécificité du verrou : `refunds/run` sur F-G2 → 409 « Remboursement précédent en échec », sur une autre fixture → pas ce 409 | G1 : la fenêtre **refuse de s'ouvrir** sur `WAIT` (:278) → override négatif explicite à prévoir ; laisse une claim bloquée + ligne `pending` reprise-éligible 20 h (trou 2 reproduit) ; G2 : aucun levier TEST connu |
| **H** Stripe **succeeded** → réconciliation refund + ledger + loyalty + e-mail | F-A après A, F-B après B, fenêtre re-gelée (403 prouvé) | Stripe read-only (refund, `transfer_reversal`, fee refunds, `amount_refunded`, balance transactions) ; `Refund` row ; `LedgerEntry` unique `(re_,'refund')` ; `LoyaltyTransaction` par `planLoyaltyRefund` ; `LoyaltyCustomer` avant/après ; EmailLog/EmailDispatch ; `phase2-preflight.js` DB ↔ Stripe | identité tripartite au centime (client +A ; resto −(reversement − fee) ; Grubano −fee ; Stripe 0) ; loyalty télescopant | invariants indépendants (compte ledger == refunds Stripe `succeeded` == 1 ; Σ) ; **pas** la somme tautologique | exécutable après A/B |
| **I** (ajout critique) **contestation** : resto **refuse** → conso `POST /api/claims/[id]/contest` dans `CLAIM_CONTEST_HOURS` → `arbitration` → admin `refuse_final` | F-I payée | refus resto → e-mail `claim_decision_refused` (indice contestation) → contest → CAS `refused → arbitration` (`activeOrderKey` re-acquis) → `refuse_final` | copie « a confirmé le refus » **véridique sur cette provenance seulement** (fausse après accept/silence/système — plan 7c) | contest hors fenêtre → 409 | exécutable REFUNDS OFF |

**Preuve navigateur (ajout critique)** : chaque scénario ci-dessus est piloté par API ; la répétition doit aussi prouver **au vrai clic** les 3 surfaces bêta : page help conso (motif fixé `missing_item`, montant calculé client), `RestaurantClaimsPanel` accept/refuse sur business `/orders` (monté seulement si CLAIMS ON), console admin `decide()`. Règle bancée : seul un vrai `mouse.click` fait foi (obstructions CSS invisibles au pixel).

## 5 · Retour final

```
PROGRAM = CLAIMS DELTA CHECK
BASELINE REPORT FOUND = YES
BASELINE DATE = 2026-09-02 (inventaire §3/§3.g/§8 P3, commit 06685c8 ; réaffirmé 2026-09-04 par REFUND-FINANCIAL-CONTRACT.md §A7)
CURRENT HEAD = 997e07a (staging = 15bb7db, +1 commit docs-only)
MATERIAL DELTA SINCE BASELINE = YES (confiné : variant A7 « Stripe pending », montants d'e-mails = moteur, tout ce que les claims héritent du moteur Phase 2 ; 26 fichiers Claims inchangés)
CANONICAL REFUND ENGINE = executeRefund (identité inchangée ; moteur durci Phase 2 ; rail A hors du chemin commande)
FOUR ORIGINAL GAPS = 1 OPEN P1 (inchangé) · 2 OPEN P2 (périmètre élargi par A7 : pending + échec dur non re-drivables, commande verrouillée) · 3 OPEN (conception) · 4 OPEN mort/cosmétique
RESTAURANT SILENCE HOLE = OPEN
NEW P0 GAPS = aucun
NEW P1 GAPS = (a) photo : modération + upload Cloudinary AVANT contrôle de propriété, sans rate-limit ni quota LLM (latent, injoignable CLAIMS OFF) ; (b) aucun opérateur ne peut poser CLAIMS_ENABLED sur staging (bloque toute répétition) ; (c) ré-arbitrage legacy non idempotent (approved+refundAttempted:false ré-admis → réécriture / bascule refused_final)
NEW P2 GAPS = classe « refunding » bloquée (A7) invisible admin ; Claim sans montant réellement remboursé (resumedIgnoredAmount ignoré, reprise d'une ligne Refund étrangère possible) ; audit claim.arbitrate `refunded` trompeur ; copie refused_final fausse hors provenance refus ; e-mail « approuvée » alors que le cash est remboursé (échec clawback) ; page help « sous 3–5 jours » ; badge /admin = arbitration seul ; en-têtes lib/claims.ts stales
IMPLEMENTATION PLAN AFTER REFUND PASS = 0 opérateur fenêtre CLAIMS → 1 silence resto (arbitrate sur restaurant_review échue + audit vrai) → 2 autorité/visibilité admin (stuck list + badge) → 3 invariants rail executeRefund → 4 autorité serveur article/qty → 5 taxonomie sécurité + priorité sans auto-refund → 6 sécurité/idempotence (photo retirée, rate-limit, legacy) → 7 véracité (réconciliation Claim sur Refund succeeded/failed non gatée, re-drive sûr, copies) → 8 tests + contrôles négatifs → 9 répétition staging ; 0 changement de schéma ; 13 décisions fondateur (D1–D13)
CLAIMS REHEARSAL PLAN = READY (A–I documentés) — EXÉCUTION BLOQUÉE par 5 blocages nommés (opérateur fenêtre CLAIMS, table Claim inconnue, session admin inconnue, fixtures neuves, séquence refund PASS + backup) ; C/D prouvent des trous, F/G partiellement non simulables en TEST
CODE MUTATION = NO (aucun code produit ; ajoutés : opérateur de backup + son test de garde + 2 docs)
DB MUTATION = NO (staging intouché ; clones locaux jetables seulement)
STRIPE MUTATION = NO
CLAIMS IMPLEMENTATION STARTED = NO
```
