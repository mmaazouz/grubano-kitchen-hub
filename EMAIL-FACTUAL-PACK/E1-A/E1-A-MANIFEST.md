# E1-A-MANIFEST — the 3 representative emails (rows extracted verbatim from the core manifest, post-P0 fixes 2026-09-05)

> Status A = live + reachable in the closed beta. Every fact is file:line-anchored in the core pack.

| EMAIL ID | Human name | Audience | Status | Maturity | Trigger → source | Send function | Template source | Subject (verbatim) | Preheader | Locale | Recipient source | CTA | Dynamic contract | Reachable now | Beta | Visual | Tranche | Notes · Evidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| AUTH_MAGIC_LINK | Lien de connexion | consumer + every partner role | **A** | WIRED_CURRENT_PRODUCT | `POST /api/auth/magic-link` with an **active** account → `app/api/auth/magic-link/route.ts:142-165` | inline `transporter.sendMail` `:107` (own transport, no EmailLog, no dedupe) | inline HTML `:84-95` + **text ✓** `:96-106` | `Ton lien de connexion Grubano` | none | FR (tutoiement) | `Operator.email` (body email, active only; anti-enum generic response) | « Me connecter » → `{base}/{locale}/eat/magic?token=` (space eat) or `/auth/magic` | name?, link, code? | YES (5 UI callers) | IN | ✓ | E1 | Link TTL 15 min single-use (`lib/magic-link.ts:17`). `SMTP_PASS` absent → link logged, not sent (`:167`). Base = request host if allow-listed else `NEXTAUTH_URL` (`:56-68`). |

| EMAIL ID | Human name | Audience | Status | Maturity | Trigger → source | Send function | Template | Subject | Preheader | Locale | Recipient | CTA | Contract | Reachable | Beta | Visual | Tranche | Notes · Evidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CONSUMER_ORDER_READY | Commande prête | consumer | **A** | WIRED | status → `ready` | same (`order_ready`) | `:237-244` | `Commande {ref} prête — {resto}` | none | FR | same | none | + fulfillmentType | YES | IN | ✓ ×2 (pickup / delivery-dormant) | E1 | Pickup (CURRENT): "venir la récupérer". Delivery wording ("part bientôt en livraison") = DORMANT / OUT_OF_BETA — double-gated since `d25fc3b`, unreachable in the closed beta. |
| PARTNER_NEW_ORDER | Nouvelle commande reçue (resto) | restaurant owner | **A** | WIRED_CURRENT_PRODUCT | same `/confirm` `:63-86` (before consumer short-circuit) + sweep `:104` | `sendRestaurantNewOrderEmail` `:280` (sendOnce `resto_order_received` / `order:<id>`) | `:295-298` | `Nouvelle commande {GR-…} — {resto}` | none | FR | `order.restaurant.operator.email` | none (text "Retrouvez-la dans votre tableau de bord") | restaurantName, orderRef, fulfillmentType, items, totalCents | **YES — server-side, no browser required** (since `d25fc3b`: a server-side notification path consumes `paymentStatus='paid'`; the checkout poll is only a fast path). PRE-FIX / HISTORICAL: before `d25fc3b` the send depended on the consumer's browser poll and the sweep had no active scheduler. | IN | ✓ | E1 | Cash orders would bypass `/confirm` but non-card payment is **refused** server-side (`orders/route.ts:109-118`) → no cash gap. No "urgent"/reminder email exists. Mode label = « Click & collect » in the beta (delivery wording double-gated). |

## Fact classification (2026-09-06) — CURRENT · DORMANT · OUT_OF_BETA · PRE-FIX / HISTORICAL
- **CONSUMER_ORDER_READY** (CURRENT = pickup state only): « votre commande … est prête — vous pouvez venir la récupérer ». The delivery state is DORMANT / OUT_OF_BETA (delivery is not sold in the closed beta). A pickup order can no longer receive any « en route » email.
- **PARTNER_NEW_ORDER** (CURRENT): reachable **server-side — no browser tab needs to stay open**; the checkout poll is only a fast path. Mode label = « Click & collect » in the beta. Cancelled/expired paid orders never receive it. Any "depends on the browser poll" wording is PRE-FIX / HISTORICAL.
- **AUTH_MAGIC_LINK** (CURRENT): link valid 15 min, single use. The optional 6-digit-code state is DORMANT (feature switch off); its current copy says « 15 minutes » while the code is valid 10 — design the code state with the correct value.
