# E1-MANIFEST — rows extracted from `../EMAIL-MANIFEST.md` (same columns, same evidence)

> E1 — GLOBAL EMAIL DESIGN SYSTEM · AUTH · CONSUMER ORDER LIFECYCLE · PARTNER CORE ORDER
> Status legend: A live+reachable · B code exists, gated/unreachable/uncalled · D dead. Maturity + reachability in the row. Read `../EMAIL-MANIFEST.md §0` once for definitions.

| EMAIL ID | Human name | Audience | Status | Maturity | Trigger → source | Send function | Template source | Subject (verbatim) | Preheader | Locale | Recipient source | CTA | Dynamic contract | Reachable now | Beta | Visual | Tranche | Notes · Evidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| AUTH_MAGIC_LINK | Lien de connexion | consumer + every partner role | **A** | WIRED_CURRENT_PRODUCT | `POST /api/auth/magic-link` with an **active** account → `app/api/auth/magic-link/route.ts:142-165` | inline `transporter.sendMail` `:107` (own transport, no EmailLog, no dedupe) | inline HTML `:84-95` + **text ✓** `:96-106` | `Ton lien de connexion Grubano` | none | FR (tutoiement) | `Operator.email` (body email, active only; anti-enum generic response) | « Me connecter » → `{base}/{locale}/eat/magic?token=` (space eat) or `/auth/magic` | name?, link, code? | YES (5 UI callers) | IN | ✓ | E1 | Link TTL 15 min single-use (`lib/magic-link.ts:17`). `SMTP_PASS` absent → link logged, not sent (`:167`). Base = request host if allow-listed else `NEXTAUTH_URL` (`:56-68`). |
| AUTH_MAGIC_LINK_WITH_OTP | Lien + code 6 chiffres | idem | **B** | WIRED_CURRENT_PRODUCT (flag) | same route when `AUTH_EMAIL_OTP_ENABLED=true` (`:155-161`) | same | same template, `code` branch | same | none | FR | same | same + code | NO — flag default OFF (staging value NOT MEASURED) | IN (flag) | ✓ | E1 | Same email, extra code block; OTP TTL 10 min (`lib/email-otp.ts:27`). |
| AUTH_PASSWORD_RESET | Réinitialisation mot de passe | password accounts (consumer/operator) | **A** | WIRED_CURRENT_PRODUCT | `POST /api/auth/forgot-password` when the account has a password (`:60-86`) | `sendPasswordResetEmail` `lib/transactional-emails.ts:651` via rail (no dedupe, deliberate) | shell() `:661-670` | `Réinitialisation de votre mot de passe Grubano` | none | FR (vous) | `operator.email` | « Choisir un nouveau mot de passe » → `{NEXTAUTH_URL}/fr/eat/reset-password?token=&email=&space=` | name, resetUrl | YES (`ForgotPasswordModal`, `/eat/account/password`) | IN | ✓ | E1 | Token hashed in `VerificationToken`, TTL 1 h (`forgot-password:31`). ⚠️ email + token in query string (see truthfulness/security register). Locale path hardcoded `/fr/`. |
| AUTH_PASSWORD_CHANGED | Mot de passe changé (sécurité) | idem | **A** | WIRED_CURRENT_PRODUCT | `POST /api/auth/reset-password` success `:88` | `sendPasswordChangedEmail` `:674` (no dedupe) | shell() `:682-685` | `Votre mot de passe Grubano a été changé` | none | FR | `operator.email` | none (text instruction) | name | YES | IN | ✓ | E1 | Security notice; "répondez à cet email" relies on contact@ mailbox being read. |
| CONSUMER_WELCOME | Bienvenue (compte créé) | consumer | **A** | WIRED_CURRENT_PRODUCT | `POST /api/auth/register` after `operator.create` `:124-148` | inline `transporter.sendMail` `:58` + own EmailLog row `consumer_welcome` | inline HTML `:43-56` | `Bienvenue sur Grubano — ton compte est prêt` | none | FR (tu) | body `email` (new account) | « Découvrir les restaurants » → **hardcoded `https://grubano.com/eat`** (prod URL even on staging) | name | YES (`/eat/auth` register tab) | IN | ✓ | E1 | Sent regardless of verification (account active at creation). Name injected **unescaped** (`${name}`). |

| EMAIL ID | Human name | Audience | Status | Maturity | Trigger → source | Send function | Template | Subject | Preheader | Locale | Recipient | CTA | Contract | Reachable | Beta | Visual | Tranche | Notes · Evidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CONSUMER_ORDER_CONFIRMATION | Commande confirmée (payée) | consumer | **A** | WIRED_CURRENT_PRODUCT | `POST /api/orders/[id]/confirm` (browser poll after Stripe Elements) when `paymentStatus==='paid'` `:52-121`; server backstop `lib/order-email-sweep.ts:132` via `POST /api/admin/orders/confirm-sweep` (admin/token; **scheduler inert**, see §8) | `sendOrderConfirmation` `lib/transactional-emails.ts:716` (sendOnce `order_confirmation` / `order:<id>`) | shell() `:740-751` | `Commande {GR-XXXXXX} confirmée — {resto}` | none | FR | `Operator.email` of `order.consumerId` | « Suivre ma commande » → `{base}/eat/account` | customerName, restaurantName, orderRef, fulfillmentType, items[{name,qty}], paidCents | YES | IN (pickup) · delivery wording OUT | ✓ ×2 (pickup/delivery) | E1 | **Never from the Stripe webhook** (golden rule, test-guarded). Amount = SERVER `order.total`. Delivery variant text "arrive chez vous" exists in code though delivery is OUT. |
| PARTNER_NEW_ORDER | Nouvelle commande reçue (resto) | restaurant owner | **A** | WIRED_CURRENT_PRODUCT | same `/confirm` `:63-86` (before consumer short-circuit) + sweep `:104` | `sendRestaurantNewOrderEmail` `:280` (sendOnce `resto_order_received` / `order:<id>`) | `:295-298` | `Nouvelle commande {GR-…} — {resto}` | none | FR | `order.restaurant.operator.email` | none (text "Retrouvez-la dans votre tableau de bord") | restaurantName, orderRef, fulfillmentType, items, totalCents | YES — **depends on the consumer's browser poll** (tab closed before webhook ⇒ only the unscheduled sweep would send) | IN | ✓ | E1 | Cash orders would bypass `/confirm` but non-card payment is **refused** server-side (`orders/route.ts:109-118`) → no cash gap. No "urgent"/reminder email exists. |
| CONSUMER_ORDER_ACCEPTED | Commande acceptée | consumer | **A** | WIRED | `PATCH /api/orders/[id]/status` → `preparing` (restaurant/admin) `:259` | `sendOrderStatusEmail` `:208` (sendOnce `order_accepted` / `order:<id>`) | `:231-236` | `Commande {ref} acceptée — {resto}` | none | FR | `Operator.email` | none | customerName, restaurantName, orderRef | YES | IN | ✓ | E1 | Status-only, no amount. |
| CONSUMER_ORDER_READY | Commande prête | consumer | **A** | WIRED | status → `ready` | same (`order_ready`) | `:237-244` | `Commande {ref} prête — {resto}` | none | FR | same | none | + fulfillmentType | YES | IN | ✓ ×2 | E1 | Pickup: "venir la récupérer"; delivery: "part bientôt en livraison" (delivery OUT). |
| CONSUMER_ORDER_ENROUTE | Commande en route | consumer | **A** | WIRED | status → `picked_up` (allowed from `ready` for **any** fulfillment type, `TRANSITIONS :17-25`) | same (`order_enroute`) | `:245-250` | `Commande {ref} en route — {resto}` | none | FR | same | none | same | YES (reachable on a pickup order too) | **OUT** (delivery) | ✓ | E1 | ⚠️ P1 truthfulness: a pickup order marked `picked_up` emails "en route — elle arrive bientôt". |
| CONSUMER_ORDER_COMPLETED | Commande récupérée / livrée | consumer | **A** | WIRED | status → `delivered` | same (`order_delivered`) | `:251-257` | `Commande {ref} récupérée — {resto}` (pickup) / `… livrée — …` | none | FR | same | none (text invite to rate "depuis votre espace") | same | YES | IN | ✓ ×2 | E1 | Loyalty points are credited in the same PATCH but **never mentioned** (no loyalty-earned email exists). |
| CONSUMER_ORDER_CANCELLED_GENERIC | Commande annulée (non payée) | consumer | **A** | WIRED | status → `cancelled` when `paymentStatus!=='paid'` `:256-267` | same (`order_cancelled`) | `:258-264` | `Commande {ref} annulée — {resto}` | none | FR | same | none ("contactez directement le restaurant") | same | YES | IN | ✓ | E1 | Shares trigger+key with the two paid variants → exactly one cancellation email per order. |
| CONSUMER_ORDER_CANCELLED_PAID_CLAIMS_OFF | Commande payée annulée — au sujet de votre paiement | consumer | **A** | WIRED_CURRENT_PRODUCT (beta D4 path) | status → `cancelled` with `paymentStatus==='paid'` **and `CLAIMS_ENABLED` false** `:244-257` | `sendOrderCancelledPaidOffEmail` `lib/claim-emails.ts:171` (rail, `order_cancelled` / `order:<id>`) | `claimShell()` + i18n `claimEmails.orderCancelledPaidOff.*` | `Commande {ref} annulée — au sujet de votre paiement` | none | **×5 (Operator.locale, null⇒fr, RTL ar)** | `Operator.email` | none (mailto by text: contact@grubano.com) | ref, resto, name? | YES (current beta config) | IN | ✓ | E1 (wording constraints from E2) | Truthful: refund "traité par un membre de l'équipe pendant la bêta", no promise, no delay. Same event also raises ADMIN_PAID_CANCELLATION. |
